= AGENT — codex-gradle
:toc:

_Date de creation_ : 2026-05-06
_Derniere mise a jour_ : 2026-06-08 (Architecture boundary review Codex↔Document)
_Projet_ : `foundry/public/codex-gradle/`
_DAG_ : N2 — Plugin Gradle d'acquisition de documents READ (conformement a la Regle 4bis)
_Boundary_ : Codex = READ (acquisition documentaire → corpus + chunks). Le RAG appartient a codebase N1 (socle du contexte augmente). Codex est un producteur de corpus, pas un hub de contexte. Les briques de creation de contenu (document, plantuml, slider, bakery, hyperframes, training, capsule) consomment le RAG via codebase, jamais directement via codex. Document (New Orleans) = WRITE + PUBLISH. Les deux sont paralleles N2, pas empiles.

'''
== Regle 1 — COMMITS/GIT : INTERDICTION FORMELLE sans permission explicite

*AUCUN commit, push, merge, amend, ou force push sans que l'utilisateur ait explicitement demande :*
"commit", "commit and push", "c'est bon tu peux commit", ou formulation equivalente.
Le script de build PEUT etre modifie en session active — le code PEUT tourner —
mais RIEN ne doit etre persisté dans Git sans instruction humaine claire.
Exception : les taches Gradle peuvent generer des fichiers dans `build/` (ignoré par .gitignore).

'''
== Regle 2 — PAS de publishToMavenLocal automatique

`publishToMavenLocal` = publication locale. OK si explicitement demandé. JAMAIS implicite.

'''
== Regle 2bis — Classification Vision/Opinion/Stratégie

Tout contenu genere ou modifie par le LLM DOIT etre classe epistemiquement :
* `[VISION]` — speculations, futurs souhaités, intuitions sans implementation
* `[OPINION]` — avis, critiques, hypothèses non validees
* `[STRATEGIE]` — plans validés par execution, code compile, build vert

PROMPT_REPRISE (injecte par opencode depuis configuration/) ne doit contenir AUCUNE section OPINION.
Les sections VISION doivent etre taguées `[VISION]`.
Les sections STRATEGIE sont les seules a pouvoir contenir des commandes Gradle pretes a executer.

'''
== Regle 3 — SECRETS via configuration/ — jamais en dur dans build.gradle.kts

ZERO credential (API key, token, password, secret) dans `build.gradle.kts` ou `gradle.properties`.
Tout parametre sensible remonte depuis `configuration/settings/` ou `configuration/.agents/`.

'''
== Regle 4 — Codex est le contrat READ

Les types partages (BlockType, ExtractedBlock, ExtractedDocument, DocExtractor)
vivent dans `codex-gradle/src/main/kotlin/`.
Les sous-projets (codex-pdf, codex-epub) importent `codex` comme dependance via mavenLocal.
Pas de module doc-extractor separe — `codex` est le contrat d'acquisition documentaire.

Le RAG (store vectoriel, contexte augmente) n'appartient PAS a codex. Apres
l'EPIC CDX-RAG-SOCLE (RAG-1→4), le store vectoriel migre vers codebase N1
(domaine `codebase.store`). Codex ne fait que produire le corpus (PDF/EPUB →
AsciiDoc → chunks), codebase l'ingere et l'expose aux briques de creation de
contenu via son hub de contexte augmente.

'''
== Regle 4bis — DAG : N2, publiable

*IMPORTABLE par* : N3 runner-gradle (assembleCompositeContext → contexte augmente via codebase N1)
*IMPORTE* : N0 graphify-gradle (knowledge graph pour extraction d'entités)
*Importe aussi* : `office/books-collection/` (source PDFs, jamais modifié)
*Apres CDX-RAG-SOCLE (RAG-1→4)* : `implementation(libs.codebase.plugin)` — codex
  delegue le RAG au socle N1. Le store vectoriel (`RagVectorStore`) vit dans
  `codebase.store`, plus dans `codex.store`.
*Publie* : MavenLocal + Sonatype (Apache 2.0)
*NE JAMAIS IMPORTER* : N3 runner-gradle (dependance cyclique interdite)
*NE JAMAIS IMPORTER document-gradle* : Codex et Document sont paralleles en N2.
  Codex = READ (acquisition documentaire), Document = WRITE + PUBLISH. Aucune
  dependance directe. Les deux exposent leur output a runner-gradle (N3) via
  metadata.json. Le RAG est actionne par codebase N1, pas par codex.

'''
== Regle 5 — Non-Répétition des Opérations Coûteuses — Loi de l'Économie d'Encre

*PRINCIPE* : Une opération coûteuse (LLM, embedding, inférence cloud, extraction OCR) qui a déjà produit un résultat *valide et persistant* NE DOIT PAS être ré-exécutée sur la même entrée. Le résultat existant est la vérité ; le re-calcul est un gaspillage de tokens/crédits API/temps de build. L'IA n'est pas un générateur idempotent gratuit — c'est un service metered. Répéter une vectorisation, un embedding, une extraction LLM déjà validée sur une entrée identique consomme du token sans produire de nouvelle connaissance. L'économie d'encre est la condition de viabilité économique du pipeline agentique MVP0.

*RÈGLE* :
. Avant toute opération coûteuse, **vérifier l'existence d'un artefact résultat** (embeddings stockés en pgvector, `composite-context.json`, `*.json` généré, cache disque).
. Si l'artefact existe et couvre l'entrée demandée → **réutiliser sans ré-invoquer**.
. Seul le **delta** (fichiers source modifiés, vecteurs absents, sections nouvelles) doit être calculé — jamais l'ensemble.
. Ne **jamais écraser** un résultat validé par un re-calcul identique. L'écrasement est un symptôme de non-idempotence économique.
. L'invalidation est **explicite et ciblée** (fichier source modifié, hash changé), jamais globale par défaut.

*ANTI-PATTERNS SPÉCIFIQUES CODEX* :
. *Re-embedding global* : re-vectoriser tout le corpus PDF/EPUB parce qu'un seul fichier source a été édité. Coût × N fichiers × N exécutions.
. *Ré-extraction OCR systématique* : relancer le LLM sur un PDF déjà OCRisé dont le `OcrResult.structuredText` est persistant et valide.
. *Cache-busting silencieux* : un `writeText()` qui écrase `composite-context.json` avant même d'avoir lu l'existant.
. *Ré-ingestion inconditionnelle* : `collectIngest` rejoué sur tout `books-collection/` sans comparer les hash sources avec les vecteurs déjà stockés.

*PROTECTION* :
. Lecture avant écriture : charger `composite-context.json` existant avant de le régénérer ; comparer hash sources vs vecteurs stockés avant `collectIngest`.
. Delta vectoriel : ingérer uniquement les fichiers dont le hash diffère de celui stocké en pgvector metadata.
. `DisableCachingByDefault` ne dispense pas de l'idempotence applicative — Gradle ne sait pas ce qu'est un "embedding déjà valide", seul le domaine le sait.

*RÉFÉRENCE* : Source canonique de la loi — `bakery-gradle/bakery-plugin/AGENT.adoc` section "Non-Répétition des Opérations Coûteuses — Loi de l'Économie d'Encre" (Session 153). Portée transverse : 10 boroughs LLM metered (bakery i18n, codex RAG, training SPG/SPD, capsule TTS, plantuml diagrammes, readme, document, agile, ticket, review). Potentiel 50-80% d'économie LLM sur le pipeline MVP0 complet.

'''
== Mission

Industrialiser l'acquisition de documents non-structurés (PDF, EPUB) pour produire
le corpus qui alimente le RAG du pipeline d'augmentation de contexte :

- `collectBookStructure` — extraction hierarchique PDF→AsciiDoc avec detection typographique
- `collectEpubStructure` — extraction EPUB via Tika → AsciiDoc structure
- `transformToMarkdown` — conversion AsciiDoc vers Markdown
- `transformChunk` — decoupage semantique par section
- `deployKnowledgeBase` — export vers JSON-L / Markdown / AsciiDoc
- `collectIngest` — vectorisation ONNX → pgvector (thin wrapper deleguant au socle
  codebase `codebase.store.RagVectorStore` apres CDX-RAG-SOCLE RAG-3)
- `collectRetrieve` — recherche semantique cosine similarity (thin wrapper deleguant
  au socle codebase apres RAG-3)
- `generateCompositeContext` — composite-context.json pour N3 runner-gradle (canal
  Docs alimente par le store socle codebase apres RAG-3)
- `persistLearnerProfile` — pont RAG SessionMemoryContract (domaine `codex.profile`,
  memoire de session stagiaire, separee du corpus documentaire)

Sans Codex, le pipeline d'augmentation de contexte est borgne sur la connaissance
livresque. Codex est le sens de la lecture du pipeline — il produit le corpus,
codebase l'ingere et l'expose aux briques de creation de contenu.

*Ce que Codex ne fait PAS* :
. La conversion multi-format vers HTML/PDF/EPUB/DocBook/ManPage — c'est le role
   de document-gradle (New Orleans), le pair N2 WRITE + PUBLISH.
. Le RAG (store vectoriel, hub de contexte augmente) — c'est le role de codebase
   N1. Apres CDX-RAG-SOCLE, codex delegue le store au socle. Les briques de
   creation de contenu (document, plantuml, slider, bakery, hyperframes,
   training, capsule) consomment le RAG via codebase, jamais directement via
   codex.

'''
== Portefeuille Transverse — Relations avec les autres agents

|===
| Projet | Niveau DAG | Relation
| `graphify-gradle` | N0 | Fournit le knowledge graph pour extraction d'entités
| `codebase-gradle` | N1 | Socle RAG (store vectoriel `codebase.store.RagVectorStore` apres CDX-RAG-SOCLE), hub de contexte augmente, contrats ContextChannel/CompositeContext. Codex delegue le RAG a ce socle.
| `runner-gradle` | N3 | Consommateur final — contexte augmente via codebase N1 (assembleCompositeContext)
| `document-gradle` (New Orleans) | N2 | *Pair parallele* — Codex READ (acquisition documentaire), Document WRITE + PUBLISH. Pas de dependance directe. Un meme AsciiDoc source est consommable par les deux. Voir `.agents/ARCHITECTURE_BOUNDARY.adoc`.
| `plantuml-gradle` | N2 | Pair — diagrammes de la chaine documentaire
| `office/` | Zone 2 | Source des PDFs (`books-collection/`), jamais modifie
|===

'''
== Fichiers du Projet

|===
| Fichier | Etat
| `AGENT.adoc` | ✅ Cree (bootstrap 2026-05-06)
| `.agents/INDEX.adoc` | ✅ Cree (bootstrap 2026-05-06)
| `PROMPT_REPRISE` | ✅ Injecte par opencode (configuration/.agents/PROMPT_REPRISE.adoc)
| `.agents/SESSION_PROCEDURE.adoc` | ✅ Cree (bootstrap 2026-05-06)
| `.agents/SESSION_CHECKLIST.adoc` | ✅ Cree (bootstrap 2026-05-06)
| `.agents/SESSIONS_HISTORY.adoc` | ✅ Cree (bootstrap 2026-05-06)
| `AUGMENTED_CONTEXT_PIPELINE.adoc` | ✅ Spec canonique (1806 lignes)
| `build.gradle.kts` | ✅ Squelette (readme + conventions)
| `settings.gradle.kts` | ✅ rootProject.name = "codex-gradle"
| `buildSrc/` | ✅ Convention plugin + version catalog
| `codex-plugin/build.gradle.kts` | ✅ Plugin education.cccp.codex.doc-pipeline
| `codex-plugin/src/` | ✅ CodexPlugin.kt + ExtractTextTask.kt + ExtractBookStructureTask.kt
|===

[.text-right]
*Document cree le 6 mai 2026 — Bootstrap gouvernance agent.*
*Premier document de gouvernance de Codex.*
