= Backlog — Brooklyn (codex-gradle)
:description: Backlog de publication Maven Central — EPIC PUB
:date: 2026-06-08
:toc: left
:sectnums:

_Dernière mise à jour : 2026-08-19 (session 073 — EPIC CDX-CR3 TERMINÉ 3/3 US + US-CDX-13-1 cache OCR + US-CDX-13-2 tests intégration Ollama)_

_Source : configuration/EPIC_PUB.adoc (Session 096)_

'''
== EPIC PUB — Publication Maven Central

Plugin N2 de pipeline documentaire (Asciidoctor → PDF). VAGUE 4 (plugins metier).

== EPIC OCR-3 — LLM OCR Pipeline (DOC-11 alignement) ✅ TERMINÉ (session 072)

. `LlmOcrEngine` — OCR LLM via Ollama vision (gpt-oss:120b-cloud) → AsciiDoc structuré
. `OcrPipeline` — fallback chain LLM → Tesseract (chaîne 2 engines)
. `HttpOllamaChatClient` — port/adapter HTTP (java.net.http.HttpClient, `/api/chat`)
. `CollectOcrTask` — task Gradle `collectOcr` (dossier images → AsciiDoc multi-pages)
. BDD Cucumber `ocr-pipeline.feature` — 5 scénarios (LLM success, fallback, all-fail, pages, empty)
. 167 tests (+32 : 22 unit + 5 functional + 5 cucumber)

== 🔴 EPIC CDX-CR3 — Revue CR-GLOBALE-003 : Création de Contenu Solide ✅ TERMINÉ (session 073)

_Source_ : `GLOBAL_CODE_REVIEW.adoc` (workspace root) — session CR-GLOBALE-003 (2026-08-04).
_Focus_ : codex alimente document DOC-11 (bookPipeline) ; les bugs critiques bloquent la chaîne amont.

[cols="1,3,2", options="header"]
|===
| EPIC | Description | Statut

| CDX-CR3-1
| **SQL injection `CodexIngestTask.kt:118`** — `$chunkId` interpolé directement dans le SQL
(`UPDATE codex_chunks SET embedding = '[$vec]'::vector WHERE id = $chunkId`). Parameterizer comme
les autres requêtes (`$1`, `$2`). _(Rappel de CR-GLOBALE-002, toujours non fixé.)_
| ✅ TERMINÉ (S-073)

| CDX-CR3-2
| **`CodexCompositeContextTask` hardcode `localhost:5432`** — `CodexCompositeContextTask.kt:39`
instancie `CodexVectorStore()` sans pgHost/pgPort lus de l'extension `codex`. Inutilisable hors localhost.
Ajouter les propriétés de tâche + wiring depuis l'extension.
| ✅ TERMINÉ (S-073)

| CDX-CR3-3
| **`chunk_index` incorrect pour contenu dupliqué** — `CodexIngestTask.kt:110` : `chunks.indexOf(chunk)`
retourne la première occurrence → deux chunks identiques reçoivent le même index. Utiliser un compteur
parcouru, pas `indexOf`.
| ✅ TERMINÉ (S-073)
|===

=== US-CDX-13-1 — Persistance OcrResult (cache disque, hash image) ✅ TERMINÉ (session 073)

. *Valeur* : Loi de l'Économie d'Encre — ne pas re-OCRoiser une image déjà
   traitée si son hash n'a pas changé. Économie de tokens LLM metered.
. *Implémentation* :
  - `OcrResultCache` (objet pur, codex.ocr) + `DiskCacheStorage` (port/adapter)
  - `CollectOcrTask` propriété `cacheDir` optionnelle, cache hit → skip LLM
  - Hash SHA-256 du contenu image comme clé d'invalidation
  - Dégradé silencieux : erreur stockage → miss (pas de crash)
  - 13 unit tests (OcrResultCacheTest)

=== US-CDX-13-2 — Tests intégration réelle Ollama (assumeTrue) ✅ TERMINÉ (session 073)

. *Valeur* : Valider le contrat HTTP réel avec Ollama sur plage 11437-11465.
. *Implémentation* :
  - `OllamaProbe` (codex.ocr) — sonde /api/tags, isReady/isModelAvailable/findReadyPort
  - `LlmOcrEngineIntegrationTest` — 5 tests @Tag("integration") avec assumeTrue
  - Plage 11437-11465 (AGENTS.adoc), ports 11434-11436 INTERDITS
  - Résultat local : 5/5 PASSED (port 11437, gpt-oss:120b-cloud)

=== US-CDX-13-3 — Exposition REST via runner-gradle N3 — 🔴 SUSPENDU

. *Valeur* : Exposer le pipeline OCR via runner-gradle N3 pour consommation
   HTTP par document-gradle DOC-11 bookPipeline.
. *Statut* : Suspendu — viendra avec DOC-11 bookPipeline côté document-gradle.

=== PUB-3.09 — codex-plugin
**Priorite** : P0
**Estimation** : 1 pt
**Dependances** : PUB-0 et PUB-1 complets (DNS TXT + GPG)

. `cd foundry/public/codex-gradle && ./gradlew publish closeAndRelease`
. Artefact : `education.cccp:codex-plugin`
. Verifier sur https://central.sonatype.com/artifact/education.cccp/codex-plugin

=== PUB-2.2 — Mise a jour commentaire relocation dans build.gradle.kts
**Priorite** : P1
**Estimation** : 1 pt
**Dependances** : Aucune

Dans le `build.gradle.kts` de codex-plugin, remplacer le commentaire :

```kotlin
// RELOCATION : prepare la migration du groupId education.cccp →
// <futur-domaine>. Activer avec -Prem relocationGroup="io.github.cccp-education"
```

Par :

```kotlin
// RELOCATION : garde comme echappatoire future.
// Activable avec -Prem relocationGroup="<futur-namespace>"
```

'''
== EPIC PUB-5 — Verification

=== PUB-5.1 — Verifier Maven Central

| Artefact | URL |
|---|---|
| codex-plugin | https://central.sonatype.com/artifact/education.cccp/codex-plugin |

'''
== 🔴 EPIC CDX-CR3 — Revue CR-GLOBALE-003 : Création de Contenu Solide

_Source_ : `GLOBAL_CODE_REVIEW.adoc` (workspace root) — session CR-GLOBALE-003 (2026-08-04).
_Focus_ : codex alimente document DOC-11 (bookPipeline) ; les bugs critiques bloquent la chaîne amont.

[cols="1,3,2", options="header"]
|===
| EPIC | Description | Priorité

| CDX-CR3-1
| **SQL injection `CodexIngestTask.kt:118`** — `$chunkId` interpolé directement dans le SQL
(`UPDATE codex_chunks SET embedding = '[$vec]'::vector WHERE id = $chunkId`). Parameterizer comme
les autres requêtes (`$1`, `$2`). _(Rappel de CR-GLOBALE-002, toujours non fixé.)_
| 🔴 P0

| CDX-CR3-2
| **`CodexCompositeContextTask` hardcode `localhost:5432`** — `CodexCompositeContextTask.kt:39`
instancie `CodexVectorStore()` sans pgHost/pgPort lus de l'extension `codex`. Inutilisable hors localhost.
Ajouter les propriétés de tâche + wiring depuis l'extension.
| 🔴 P0

| CDX-CR3-3
| **`chunk_index` incorrect pour contenu dupliqué** — `CodexIngestTask.kt:110` : `chunks.indexOf(chunk)`
retourne la première occurrence → deux chunks identiques reçoivent le même index. Utiliser un compteur
parcouru, pas `indexOf`.
| 🟠 P1
|===