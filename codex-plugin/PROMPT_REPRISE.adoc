= PROMPT_REPRISE — Codex — Session 080
:session: 080
:date: 2026-08-19
:status: PRET
:max-lines: 45

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
../gradlew build -x test   # compile OK, validatePlugins OK
../gradlew test            # 268/268 PASS (baseline préservée S-079)
```

== Session 079 terminée — CDX-3-1 Domaine codex.ontology + OntologyDeriver

. *CDX-3-1 TERMINÉ* — baby-step TDD strict RED → GREEN → REFACTOR.
. *Domaine `codex.ontology`* (12e) — 2 fichiers prod purs :
  `OntologyMapping.kt` (4 data classes immutables) +
  `OntologyDeriver.kt` (object pur 169 lignes, parsing SQL DDL par regex).
. *3 requêtes archétypales* : bounded contexts (BFS connectivité FK),
  aggregates (CASCADE edges par racine, self-ref exclue), value objects
  (tables sans FK sortantes). LDD title nomme le bounded context.
. *13 unit tests* (OntologyDeriverTest) — empty, extraction, FK-connected,
  disconnected, CASCADE roots, dependents, no-CASCADE exclusion, VO
  sans FK, VO exclut FK, columns, 3 vues, pure/déterministe, LDD naming.
. *268 tests* 0 FAIL (baseline 255 → +13). Kover VERT, validatePlugins VERT.
. *Zéro bump BOM, zéro secret, zéro régression, backward compat total.*

== Prochaine session — 080

*Mission principale* : *CDX-4-1* (domaine `codex.enrichment` +
`JsonLddEnricher` + `GraphifyResolver` port + `GraphifyFileResolver`
adapter lit `graph.json`, ~15 unit) — 2e domaine pur du P1.

. Baby-step TDD strict : RED (`JsonLddEnricher` inexistant) → GREEN
  (objet pur créé) → REFACTOR.
. Types : `EnrichedLddNode` étend `LddNode` avec `ragChunks`/
  `graphifyNodes`/`semanticDensity`/`entities`. `GraphifyFileResolver`
  lit `graph.json` (pas de dépendance compile graphify).
. Domaine cible : `codex.enrichment` (13e domaine codex, voir cadrage S-077).
. Gate : aucune — nouveau domaine pur, pas de bump BOM.

*Alternatives* :
. *CDX-5-1* (`codex.licence` + `PdfLicenseDetector` + `LicenseRouter`
  ~10 unit) — si priorité routing licence.
. *CDX-3-2* (tâche `deriveOntology` + wiring `CodexPlugin` ~4 functional)
  — si priorité exposition Gradle.

*Enchaînement* (ordre d'exécution du cadrage S-077) :
. CDX-7-1 ✅ (S-078) → CDX-3-1 ✅ (S-079) → CDX-4-1/5-1 (domaines purs) →
. tâches Gradle (CDX-3-2/4-2/5-2) → câblage (CDX-4-3) → DSL (CDX-6-1/2) →
. tests chaînés (CDX-7-2/3/4) → Cucumber BDD (CDX-3-3/4-4/5-3/6-3).

*Actions reportées* :
. Démarrer EPIC RUNTIME-CONTRACTS côté MEMPHIS (US RC-1..7) pour débloquer
  US CDX-RC-04 (gate publication `runtime-contracts:0.0.1`).

== References

- `.agents/sessions/079-cdx-3-1-ontology-deriver.adoc`
- `CADRAGE_EPIC_P1_CORPUS_ENRICHMENT.adoc` — doc cadrage 5 EPICs P1
- `BACKLOG.adoc` § EPIC CDX-3/4/5/6/7 (US détaillées)
- `build.gradle.kts:30` — BOM 0.0.23
- Central : codex-plugin 0.0.4 (bundle `8556eea3`), BOM 0.0.23 (bundle `8dc8e513`)