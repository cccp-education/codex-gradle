= PROMPT_REPRISE — Codex — Session 100
:session: 100
:date: 2026-08-22
:status: PRET
:max-lines: 50

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew test             # 667/667 PASS (baseline S-098 inchangée)
```

== Session 099 terminée — CDX-OCR-2 ✅ (cross-borough Queens/codebase)

. `VisionOcrEngineAdapter` créé côté codebase (`codebase.koog.llm.adapter`)
  — implémente le port codex `codex.ocr.OcrEngine`, wrappe `VisionProvider`
  (IA), bridge suspend → runBlocking. 4 unit RED→GREEN.
. `CodexOcrEngineAdapter` supprimé (sens inverse de la boundary).
. `OcrTask` codebase IA-uniquement : plus d'import `TesseractOcrEngine`,
  provider "tesseract" → GradleException pointant vers codex `collectOcr`,
  double échec gemini+ollama → erreur sans fallback software.
. Tests migrés : `OcrTaskAiOnlyBoundaryTest` (2 unit) + `ocr.feature`
  2 scénarios boundary PASSED. Gate : check VERT 41m26s, cucumberTestEpicOcr
  29 PASSED ; 3 échecs préexistants au baseline (stash vérifié).

== Prochaine session — 100 : CDX-OCR-3 (purge pile IA codex, gate levée)

*Baby-step TDD strict* :
. Suppression `LlmOcrEngine`, `HttpOllamaChatClient`, `OllamaChatClient`
  + leurs tests (`LlmOcrEngineIntegrationTest` → migration fake/injected).
. `OllamaProbe` CONSERVÉ (utilitaire assumeTrue, plage 11437-11465).
. `CollectOcrTask` : props ollama inertes supprimées (host/port/modelName/config).
. `check` VERT codex (661+ tests intacts) + cucumber dédiés.

*Ensuite* (ordre) : CDX-OCR-4 (Cucumber fake engine) → RAG-1 → RAG-2 →
gate levée → RAG-3 → RAG-4 → RAG-5.

== References

- `.agents/sessions/099-cdx-ocr-2-vision-ocr-engine-adapter.adoc`
- `CADRAGE_EPIC_CDX_OCR_BOUNDARY.adoc` (US-3 : décisions 3, 9, 10)
- `CADRAGE_EPIC_CDX_RAG_SOCLE.adoc` (gate OCR-1/2 avant RAG-3/4)
- `BACKLOG.adoc` § EPIC CDX-OCR-BOUNDARY
