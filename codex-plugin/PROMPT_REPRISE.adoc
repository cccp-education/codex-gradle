= PROMPT_REPRISE — Codex — Session 220
:session: 220
:date: 2026-09-17
:status: PRET

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 987 tests 0 FAIL (S-219)
----

== Session 219 terminée — EPIC CDX-DOUBT-BRIDGE TERMINÉ 3/3 US

*CDX-DOUBT-BRIDGE-0 ✅* — cadrage (5 constats, 7 décisions). Doc
`.agents/CADRAGE_EPIC_CDX_DOUBT_BRIDGE.adoc`.

*CDX-DOUBT-BRIDGE-1 ✅* — `CodexIngestTask` doubt-aware : seam
`internal ingestInto` délègue à `DoubtPolicy.mark` +
`RagVectorStore.ingestWithDoubt` ; boucle R2DBC dupliquée supprimée
(thin wrapper, 135→111 lignes). `CodexIngestDoubtTest` 4 UT +
`CodexIngestRetrieveIT` testcontainers PASSED (chemin R2DBC réel).

*CDX-DOUBT-BRIDGE-2 ✅* — `CodexCompositeContextTask` doubt-aware :
`searchDoubtAware` → `searchWithDoubt` ; `buildDocsContent` →
`DoubtExposure` (annote/exclut) avec provenance préservée ; entrées JSON
+`confidence`/`doubtful` (additif) ; `CodexExtension.excludeDoubtfulDocs`
(défaut false) + wiring. `CodexCompositeContextDoubtTest` 4 UT +
`CodexExtensionDoubtTest` 3 UT. BDD `codex_doubt_bridge.feature` 5
scénarios `@doubt-bridge` + 9e runner dédié.

*GBL-003.4 codex complété* : la surface tâche n'est plus un contournement
du doute (le run réel S-214 passait par `CorpusIngestMain` codebase).

Gate : check VERT 987 tests 0 FAIL (baseline 872 → +115). 9 suites
cucumber dédiées VERTES. Zéro bump BOM, zéro secret, backward compat.

Rapport : `.agents/sessions/219-cdx-doubt-bridge.adoc`.

== Prochaine session — 220 : objectifs

=== Option A — Publication codex-plugin 0.0.8 (si accord pilote) 🟡

* HEAD (S-219) non publié. Version locale 0.0.7, Central 0.0.7 (S-217).
* Bump 0.0.8 requis + MEMPHIS catalog/BOM.
* Contenu depuis 0.0.7 : OCR-QUALITY (S-218) + CDX-DOUBT-BRIDGE (S-219).

=== Option B — Dogfooding run corpus FPA `collectIngest` doubt-aware

* Le corpus FPA (387 chunks, 18 doubtful) a été ingéré via
  `CorpusIngestMain` (codebase) S-214. Vérifier que la tâche codex
  `collectIngest` produit désormais le même résultat doubt-aware.
* Mesure du gain vs découverte manuelle (rapport `OcrQualityReport`).

=== Option C — Maintenance / cross-borough

* À la demande pilote uniquement.

== Hors scope (documenté)

* *Page-provenance* (D6 cadrage) : join chunk→page du `OcrQualityReport`
  vers le doute par chunk — nécessite d'ajouter une provenance de page
  aux chunks (le `SemanticChunker` produit par section, pas par page).
  EPIC futur distinct.

== Méthode

Baby-step TDD strict RED → GREEN → REFACTOR, 3 niveaux (unit → fonctionnel
→ Cucumber), English only.

== Règles

- COMMITS/PUSH — autorisés (décision pilote 2026-09-17).
- PAS DE publishToMavenLocal automatique sans permission.
- SECRETS via configuration/ — jamais en dur dans build.gradle.kts.

== References

- `.agents/INDEX.adoc` (à jour S-219)
- `.agents/BACKLOG.adoc` (EPIC CDX-DOUBT-BRIDGE ✅ 3/3 US)
- `.agents/CADRAGE_EPIC_CDX_DOUBT_BRIDGE.adoc`
