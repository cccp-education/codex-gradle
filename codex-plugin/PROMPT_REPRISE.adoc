= PROMPT_REPRISE — Codex — Session 229
:session: 229
:date: 2026-09-17
:status: PRET
:mission: C-2 — split-brain `DocumentChunk` (prérequis structurel de A-1)

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
git status --porcelain -- src/    # vide ? → règle EAGER
./gradlew compileKotlin compileTestKotlin   # environnement
----

== Contexte — bloc C (maintenance), séquence C → A → B

Le pilote a tranché S-227 : *C (maintenance) → A (EPIC CB-PAGE-PROVENANCE)
→ B (publication)*, 11 sessions S-228 → S-238. Plan :
`.agents/CADRAGE_ROADMAP_S227_C_A_B.adoc`.

- *C-1 (S-228)* ✅ TERMINÉ — drift MEMPHIS pins `0.0.49` → `0.0.50`, garde
  `CodexPluginPublicationTest` 3/3 VERTE, `check` VERT 1393 tests 0 FAIL.
  Audit pins N1 : BOM contraint `codebase-plugin` à 0.0.16, pas de drift.
- *C-2 (S-229)* — **mission ci-dessous**.
- *C-3 (S-230)* : dead code N1 (`StoreStatements.insertChunk()` 5 binds +
  `IngestIndexing.assignLocalIndices`, zéro appelant prod) + 8 désync
  gouvernance (`SESSION_PROCEDURE.adoc` chemins pré-S-105 + commande
  `:codex-plugin` erronée, `SESSION_CHECKLIST.adoc:8`, `INDEX.adoc:18` règle
  « INTERDICTION FORMELLE » contredisant la décision 2026-09-17, `AGENT.adoc`
  § Fichiers, `README.md` badges 0.0.1).
- *C-4 (S-231)* : pollution TOC chunking (99/387 au run FPA S-216), BDD
  `@toc-noise`, clôture C + go/no-go A.

== Mission S-229 — C-2 (baby-step DDD/TDD/BDD strict)

*Constat* : deux data classes `DocumentChunk` identiques coexistent —
`codex.tasks.DocumentChunk` (`codex/tasks/ChunkDocumentTask.kt:34`,
producteur) et `codebase.store.DocumentChunk` (`codebase/store/DocumentChunk.kt:23`,
type N1, migration verbatim RAG-1). `CodexIngestTask` décode **déjà** dans
le type N1 (`ignoreUnknownKeys = true`). Les autres tasks codex
(`SemanticChunker`, `ChunkDocumentTask`, `ExportKnowledgeBaseTask`,
`CollectPageProvenanceTask`, `EnrichJsonLddTask`, `JsonLddEnricher`,
`PageProvenanceResolver`) utilisent le type producteur.

*Travail* :

. Décision (déjà actée S-227) : le type de store vit en N1
   (`codebase.store`), le producteur codex **consomme** le type N1 —
   pattern RAG-3 (codex dépend déjà de `libs.codebase.plugin`).
   Alternative écartée : garder le doublon (double maintenance du futur
   `pages` de A-1).
. Characterization tests **avant** bascule : contrat JSON `chunks.json`
   byte-compatible (`@Serializable` identique) — RED → GREEN.
3. Suppression du type `codex.tasks.DocumentChunk` + bascule des 7 fichiers
   prod (imports) vers `codebase.store.DocumentChunk`.
. Non-régression : `@provenance` (8 scénarios), corpus enrichment,
   `CodexIngestDoubtTest`, `CodexIngestRetrieveIT`.
. Gate : `check` VERT, baseline 1393 tests 0 FAIL, zéro régression,
   backward compat JSON totale.

*Valeur* : **débloque A-1** (un seul type porte `pages`), supprime un drift
structurel documenté depuis S-223.

*Risque* : `codex.tasks.DocumentChunk` est référencé par 8 fichiers de test
— migration mécanique, gate par suite complète.

== Pièges connus

- *mavenLocal partagé entre boroughs* : une session parallèle peut réécrire
  un jar pendant `check` (course S-226 `CodexIngestRetrieveIT`,
  `NoClassDefFoundError: DocumentChunk$$serializer`). Vérifier par stash
  avant d'accuser le diff.
- *Le projet racine **est** `codex-plugin`* — utiliser `./gradlew check`
  (PAS `:codex-plugin:check`, qui échoue). C-3 corrige ce chemin dans
  `SESSION_PROCEDURE.adoc`.
- *Garde de publication* : `CodexPluginPublicationTest` lit le toml du repo
  voisin `../workspace-bom/gradle/libs.versions.toml` (source de vérité).
  Pins alignés 0.0.50 (C-1).

== Méthode / Règles
Baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR, 3 niveaux (unit →
fonctionnel TestKit/ProjectBuilder/testcontainers → Cucumber), English only.
COMMITS/PUSH autorisés (décision pilote 2026-09-17). PAS de publication sans
permission. SECRETS via configuration/. Ne JAMAIS lancer la suite globale
sans nécessité (règle EAGER cycle build/test piloté par le delta source).

== References
- `.agents/CADRAGE_ROADMAP_S227_C_A_B.adoc` (plan C → A → B)
- `.agents/sessions/228-c1-drift-memphis.adoc` (rapport C-1)
- `.agents/CADRAGE_EPIC_CB_PAGE_PROVENANCE.adoc` (EPIC A, S-225)
- `codebase.store.DocumentChunk` · `codex/tasks/ChunkDocumentTask.kt`
