= PROMPT_REPRISE — Codex — Session 076
:session: 076
:date: 2026-08-19
:status: PRET
:max-lines: 40

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
../gradlew build -x test   # compile OK, validatePlugins OK
../gradlew test            # 397/397 PASS (baseline préservée S-075)
```

== Session 075 terminée — MEMPHIS rattrapage + CDX-13-3 N2↔N2 bridge

Deux livrables :

1. **MEMPHIS rattrapage** (gouvernance pure) — INDEX workspace-bom rattrapé
   (4 versions 0.0.19→0.0.22 publiées hors-session). Bug désynchronisation
   catalogue `libs.versions.toml` corrigé (`workspace-bom = "0.0.20"` → `"0.0.22"`).
   INDEX/SESSIONS_HISTORY/BACKLOG synchronisés. Zéro code prod.

2. **CDX-13-3 réactivé** (baby-step TDD/BDD) — split-brain de contrat N2↔N2
   résolu. `CollectOcrTask` produit désormais des pages individuelles
   `NNN-<pageId>.adoc` dans `outputDir` (sortie primaire), alignées sur le
   contrat `PageOrder.fromFileName` de document-gradle `BookAssembler`.
   `outputFile` (legacy concaténé) préservé optionnel backward compat.
   4 unit `CollectOcrPagesContractTest` + 3 scénarios Cucumber `@n2-bridge`.
   211 unit + 186 cucumber = 397 tests, 0 FAIL. `check` VERT.

Bug split-brain découvert : codex produisait un seul fichier concaténé,
document attendait un répertoire de pages ordonnées. Pont N2↔N2 filesystem
spécifié (KDoc `BookPage`) mais non respecté → désormais aligné.

== Prochaine session — 076

1. *Borough DORMANT côté contenu* — CDX-13-3 est désormais ✅ TERMINÉ.
   Tous les EPICs P0/P1 codex sont terminés.
2. *DOGFOODING E2E* — possibilité de tester le pipeline complet
   `collectOcr → bookPipeline` cross-borough (codex→document) sur un vrai
   livre de `office/books-collection/`. À valider avec l'utilisateur.
3. *Publication 0.0.4* — le contrat `outputDir` est un breaking change
   mineur (nouvelle sortie primaire). Bump 0.0.3→0.0.4 + publication Maven
   Central + sync BOM MEMPHIS. À valider avec l'utilisateur.
4. *Code review opportuniste* — si une nouvelle CR-GLOBALE est émise,
   traiter les nouveaux bugs selon baby-step TDD.

== References

- `.agents/sessions/075-memphis-rattrapage-cdx-13-3-n2-bridge.adoc`
- `src/main/kotlin/codex/tasks/CollectOcrTask.kt:27-60` — outputDir + outputFile
- `src/test/kotlin/codex/tasks/CollectOcrPagesContractTest.kt` — contrat N2↔N2
- `src/test/resources/features/ocr-pipeline.feature` — 3 scénarios @n2-bridge
- workspace-bom : INDEX rattrapé 0.0.19→0.0.22, catalogue corrigé