= PROMPT_REPRISE — Codex — Session 101
:session: 101
:date: 2026-08-24
:status: PRET
:max-lines: 50

== Quick Start

----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew test             # 639/639 PASS (baseline S-100 inchangée)
----

== Session 100 terminée — CDX-OCR-3 ✅ (purge pile IA codex)

. Suppression production : `LlmOcrEngine` (83 lignes),
  `HttpOllamaChatClient` (99), `OllamaChatClient` (25), `OcrConfig`
  (77) — 284 lignes. `OllamaProbe` CONSERVÉ (tests intégration).
. `CollectOcrTask` : props `ollamaHost`/`ollamaPort`/`model`
  supprimées. `CodexExtension` : `ollamaHost`/`ollamaPort`/
  `ollamaModel` supprimées (3 properties). `CodexPlugin` : wiring
  ollama retiré, description tâche "AI engine → Tesseract fallback".
. Tests : 4 fichiers supprimés (21 tests pile IA),
  `CollectOcrTaskTest`/`CollectOcrPagesContractTest` mis à jour,
  `OcrPipelineSteps` nettoyé (imports LlmOcrEngine/OcrConfig/
  OllamaChatClient retirés, step `the LlmOcrEngine processes the
  image` supprimé, step `an LLM engine that returns empty text` →
  `a primary engine that returns empty text`). Feature
  `ocr-pipeline.feature` : scénario LlmOcrEngine supprimé, 2
  scénarios renommés.
. `check` VERT 2m20s — 639 tests 0 FAIL (baseline 667 → -28).
  Cucumber dédiés VERTS. Zéro bump BOM, zéro secret.

== Prochaine session — 101 : CDX-OCR-4 (Cucumber BDD fake engine)

*Baby-step BDD strict* :
. Scénarios `ocr-pipeline.feature` étendus : fake AI engine injecté
  dans `OcrPipeline` → succès direct ; degraded Tesseract-only sans
  injection ; cache hit skip engine (Économie d'Encre — le moteur
  n'est pas appelé si cache hit).
. Runner dédié `CodexOcrCucumberRunner` (pattern S-082, 6e runner
  codex) + tâche `cucumberTestOcr` câblée `build.gradle.kts`.
. Steps `OcrPipelineSteps` étendus (fake AI engine + cache hit
  assertion via counting engine).
. `check` VERT codex. EPIC CDX-OCR-BOUNDARY TERMINÉ 4/4 US.

*Ensuite* (gate levée) : EPIC CDX-RAG-SOCLE — RAG-1 (domaine
`codebase.store` + `RagVectorStore` migré verbatim) → RAG-2 (bascule
interne 6 call sites) → RAG-3 (codex délègue + suppression
`CodexVectorStore`/`IngestStatements`/`IngestIndexing`) → RAG-4
(codebase retrait `libs.codex.plugin`) → RAG-5 (Cucumber BDD).

== References

- `.agents/sessions/100-cdx-ocr-3-purge-pile-ia-codex.adoc`
- `CADRAGE_EPIC_CDX_OCR_BOUNDARY.adoc` (US-4)
- `CADRAGE_EPIC_CDX_RAG_SOCLE.adoc` (gate OCR-1/2 levée, RAG-1→5)
- `BACKLOG.adoc` § EPIC CDX-OCR-BOUNDARY + § EPIC CDX-RAG-SOCLE