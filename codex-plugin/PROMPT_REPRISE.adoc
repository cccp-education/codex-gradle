= PROMPT_REPRISE — Codex — Session 107
:session: 107
:date: 2026-08-25
:status: PRET
:max-lines: 50

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline preserved (679 tests 0 FAIL)
----

== Session 106 terminée — CB-STORE-UNIFY ✅ CADUQUE

. CB-STORE-UNIFY était cadré comme "suppression du store legacy après
  migration vers `RagVectorStore`".
. Audit pré-coding côté Queens (codebase-gradle S-206) révèle cadrage trop
  simpliste : APIs très différentes, schémas distincts, usages
  sémantiquement distincts (corpus documentaire codex vs code source
  workspace).
. Discussion pilote confirme architecture N2→N1 (codex ingère, codebase
  stocke + RAG, flèche unique, zéro cycle) — les deux stores légitimes
  servent des domaines distincts.
. ÉPIC clos comme CADUQUE. Hardening ciblé `VectorStore.initSchema()`
  non-destructeur + `clean()` test-only appliqué côté Queens.
. Côté Brooklyn/codex-gradle : zéro consommateur résiduel (déjà vérifié
  S-105). ÉPIC terminé pour Brooklyn.

== Prochaine session — 107 : objectifs

Borough Brooklyn DORMANT — tous EPICs P0/P1 terminés. Maintenance uniquement.

*Options* :
. *Maintenance* : publication, code review, dette technique ciblée.
. *Cadrer un nouvel EPIC* si besoin identifié.
. *Cross-borough* : assister un autre borough (Queens, Manhattan, etc.).

*Methode* : baby-step TDD strict RED → GREEN → REFACTOR, 3 niveaux
  (unit → fonctionnel → Cucumber), English only.

== References

- `.agents/sessions/105-governance-move.adoc`
- `.agents/CADRAGE_EPIC_CB_STORE_UNIFY.adoc` (✅ CADUQUE S-206)
- `.agents/BACKLOG.adoc` § EPIC CB-STORE-UNIFY (✅ CADUQUE S-206)