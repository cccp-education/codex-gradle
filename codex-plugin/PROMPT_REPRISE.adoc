= PROMPT_REPRISE — Codex — Session 095
:session: 095
:date: 2026-08-22
:status: PRET
:max-lines: 45

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew test             # 661/661 PASS (baseline préservée S-094)
./gradlew cucumberTestSessionMemory  # 10/10 scénarios @session-memory PASSED
```

== Session 094 terminée — EPIC CDX-RC-04 TERMINÉ 4/4 baby-steps

. *CDX-RC-04-1* — Domaine `codex.profile` (12e) + objets purs
  unit-testables. `ProfileConfig` (config pgvector),
  `ProfileStatements` (templates SQL — DDL `codex_learner_profiles`
  clé composite + UPSERT ON CONFLICT + cast `$3::jsonb`), `ProfileEmbedding`
  (texte à embedder — weakPoints + annotations values, skip IDs/numériques).
  27 unit. Bump `runtime-contracts:0.0.1` + BOM 0.0.24.
. *CDX-RC-04-2* — `PgVectorSessionMemory` adapter implémente
  `SessionMemoryContract` via R2DBC + ONNX. 3 bugs (bindNull nullables,
  R2DBC lazy Result consume, JSONB cast). 5 functional testcontainers.
. *CDX-RC-04-3* — `PersistLearnerProfileTask` thin wrapper (Jackson
  `LearnerProfile` N0 non-`@Serializable`) + wiring CodexPlugin
  groupe `codex-memory` (14e tâche). 2 functional.
. *CDX-RC-04-4* — Cucumber BDD `codex_session_memory.feature` 10
  scénarios `@session-memory` + runner dédié (6e) + `FakeSessionMemory`
  in-memory. Pure BDD. 10/10 PASSED.
. *661 tests 0 FAIL* (baseline S-093 535 → +126). `check` VERT 1m36s,
  Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro
  régression, backward compat total. Domaine `codex.profile` (12e) clos.

== Prochaine session — 095

*Priorité 1 — Publication codex-plugin 0.0.5* (justifiée — CDX-RC-04 =
nouvelle fonctionnalité pont RAG session memory) :
. Vérifier ordre DAG : `runtime-contracts:0.0.1` déjà Central (MEMPHIS
  S-022), BOM 0.0.24 déjà Central. Codex n'a pas de dépendance N0
  non-publiée.
. Bump `codex-plugin/gradle/libs.versions.toml` ligne `codex-plugin = "0.0.4"` → `"0.0.5"`
  (`build.gradle.kts:26` lit `libs.versions.codex.plugin.get()`).
. Bump BOM constraint codex 0.0.4 → 0.0.5 côté MEMPHIS
  (`workspace-bom/build.gradle.kts`).
. `./gradlew publishAggregationToCentralPortal --no-daemon`
  (bundle AUTOMATIC).
. MàJ BACKLOG § EPIC PUB + INDEX + SESSIONS_HISTORY.

*Alternatives* :
. *Cleanup KDoc* `SemanticChunker.kt` — retirer mention
  "unification différée" (constat #7 résolu S-093, caduque).
. *EPICs P2* non cadrés (futur).

== References

- `.agents/sessions/094-cdx-rc04-session-memory-pont-rag.adoc`
- `BACKLOG.adoc` § EPIC CDX-RC-04 (TERMINÉ) + § EPIC PUB
- Central : codex-plugin 0.0.4 (bundle `8556eea3`), BOM 0.0.24 (bundle `d3b82434`)
- MEMPHIS S-022 : `runtime-contracts:0.0.1` + BOM 0.0.24 publiés Central