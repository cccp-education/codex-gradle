= PROMPT_REPRISE — Codex — Session 228
:session: 228
:date: 2026-09-17
:status: PRET
:mission: C-1 — drift MEMPHIS (pins 0.0.49 → 0.0.50) + baseline verte (ouverture du bloc C : C → A → B)

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew compileKotlin compileTestKotlin   # environnement (sources fraîches → compile seulement)
./gradlew test --tests 'codex.CodexPluginPublicationTest'   # RED attendu (drift C-1)
----

== Contexte — ordonnancement pilote acté S-227

Le pilote a tranché : *C (maintenance) puis A (EPIC CB-PAGE-PROVENANCE)
puis B (publication)*. Chacun = 3 à 4 sessions distinctes. Total 11 sessions
(S-228 → S-238). Plan complet : `.agents/CADRAGE_ROADMAP_S227_C_A_B.adoc`.

- *C* — 4 sessions : C-1 drift/baseline (S-228) · C-2 split-brain
  `DocumentChunk` (S-229) · C-3 dead code + gouvernance (S-230) · C-4
  pollution TOC + clôture (S-231).
- *A* — 4 sessions : US-1 N1 transport (S-232) · US-2 N2 transport (S-233) ·
  US-3 N1 exposition (S-234) · US-4 BDD (S-235).
- *B* — 3 sessions : B-1 codebase-first (S-236) · B-2 codex 0.0.12 (S-237) ·
  B-3 propagation + dogfooding (S-238).

Pourquoi C avant A : (1) la baseline n'est PAS verte à HEAD (drift C-1) ;
(2) C-2 est un prérequis structurel de A-1 (le champ `pages` ne doit pas
être maintenu dans deux types `DocumentChunk`).

== Mission S-228 — C-1 (baby-step TDD strict)

*Constat prouvé S-227* : une session MEMPHIS **parallèle** (S-227, commit
`c756b33`) a publié **BOM/catalog 0.0.50** + `codebase-plugin 0.0.16` après
la clôture codex S-226 (pin 0.0.49). La garde RED est le test.

. *RED* — `./gradlew test --tests 'codex.CodexPluginPublicationTest'` →
   `1 failed` (`AssertionError at CodexPluginPublicationTest.kt:54`) :
   le pin platform `workspace-bom:0.0.49` ≠ catalog ws 0.0.50.
. *GREEN* — bump pins `0.0.49` → `0.0.50` :
   - `settings.gradle.kts` : `from("education.cccp:workspace-catalog:0.0.49")`
     → `0.0.50` ;
   - `build.gradle.kts` : `platform("education.cccp:workspace-bom:0.0.49")`
     → `0.0.50`.
   Le toml local `codex-plugin = "0.0.11"` **ne bouge pas** (pas de release).
3. *Garde 3/3 VERTE* (`--rerun-tasks`).
. *Audit secondaire* : toml local `codebase = "0.0.12"` vs catalog `0.0.16`
   — résolu par la contrainte BOM, aucune garde ne l'exige. Vérifier ce
   n'est pas un drift latent (S-226 l'a classé « pas de drift réel »).
. *Gate* : `./gradlew check` VERT, baseline **1393 tests**
   (1263 unit + 130 cucumber) 0 FAIL, Kover VERT, validatePlugins VERT.

== Pièges connus

- *mavenLocal partagé entre boroughs* : une session parallèle peut réécrire
  un jar pendant `check` (course S-226 `CodexIngestRetrieveIT`,
  `NoClassDefFoundError: DocumentChunk$$serializer`). Vérifier par stash
  avant d'accuser le diff.
- *Le chemin de projet* : le projet racine **est** `codex-plugin` — utiliser
  `./gradlew check` (PAS `:codex-plugin:check`, qui échoue). Voir C-3
  (SESSION_PROCEDURE à corriger S-230).
- *Garde de publication* : `CodexPluginPublicationTest` lit le toml du repo
  voisin `../workspace-bom/gradle/libs.versions.toml` (source de vérité).

== Bloc C — suite (rappel, ne pas anticiper)

- *S-229 / C-2* : unifier `DocumentChunk` (supprimer `codex.tasks.DocumentChunk`,
  basculer 7 fichiers prod vers `codebase.store.DocumentChunk`) — **bloque A-1**.
- *S-230 / C-3* : dead code N1 (`insertChunk()` 5 binds + `IngestIndexing`) +
  8 désynchronisations gouvernance (`SESSION_PROCEDURE.adoc` chemins pré-S-105
  + commande `:codex-plugin` erronée, `INDEX.adoc:18` règle « INTERDICTION
  FORMELLE » contredisant la décision 2026-09-17, `AGENT.adoc` § Fichiers,
  `README.md` badges 0.0.1).
- *S-231 / C-4* : pollution TOC chunking (99/387 au run FPA S-216), BDD
  `@toc-noise`, clôture C + go/no-go A.

== Méthode / Règles
Baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR, 3 niveaux (unit →
fonctionnel TestKit/ProjectBuilder/testcontainers → Cucumber), English only.
COMMITS/PUSH autorisés (décision pilote 2026-09-17). PAS de publication sans
permission. SECRETS via configuration/. Ne JAMAIS lancer la suite globale
sans nécessité (règle EAGER cycle build/test piloté par le delta source).

== References
- `.agents/CADRAGE_ROADMAP_S227_C_A_B.adoc` (plan de séquence C → A → B)
- `.agents/sessions/227-ordonnancement-c-a-b.adoc` (rapport S-227)
- `.agents/CADRAGE_EPIC_CB_PAGE_PROVENANCE.adoc` (EPIC A, S-225)
- `.agents/sessions/226-maintenance-publication-0011.adoc` (point de départ C)
- `workspace-bom` commit `c756b33` (déclencheur du drift C-1)
