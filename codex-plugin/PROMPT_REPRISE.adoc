= PROMPT_REPRISE — Codex — Session 224
:session: 224
:date: 2026-09-17
:status: PRET
:mission: implémenter CDX-PAGE-PROVENANCE-1 (domaine `codex.provenance`)

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 1164 tests (1050 unit + 114 cucumber), 0 FAIL
----

== Session 223 terminée — Cadrage EPIC CDX-PAGE-PROVENANCE

Traçabilité du doute OCR jusqu'à la page source (décision D6 du cadrage
CDX-DOUBT-BRIDGE S-219). Gouvernance pure, zéro code.

*Problème* : le chunk sait qu'il est douteux mais pas *de quelle page* il
vient ; `OcrQualityReport` (S-218) sait quelle page est douteuse mais est
*orphelin* (zéro consommateur). La jointure manque.

*Décision structurante* : provenance = **jointure dérivée non stockée**
(TOC × chunks × rapport), join par titre normalisé (`SectionTitleNormalizer`,
canon prouvé 115/115 S-217) — zéro DDL, zéro re-vectorisation (Économie
d'Encre). Nouveau domaine `codex.provenance` (object pur + tâche thin
wrapper), champ N3 additif `pages`, TOC input explicite. 3 options écartées
(ancre in-band 3 boroughs, re-chunk par page contredit RD1 S-216, colonne
pgvector reportée).

Doc : `.agents/CADRAGE_EPIC_CDX_PAGE_PROVENANCE.adoc` (10 constats, 9
décisions, 5 US ~10 pts). Rapport : `.agents/sessions/223-cadrage-cdx-page-provenance.adoc`.

== Enjeux gardés pour la reprise (à trancher par le pilote)

. *Arbitrage de périmètre codex-only* — confirmer que la persistance pgvector
   (colonne page `codex_chunks` + `RetrieveResult.page`, N1 Queens) reste
   **hors scope** (D7) ; sinon EPIC cross-borough `CB-PAGE-PROVENANCE` à cadrer.
. *Décision D1 — provenance dérivée vs stockée* — valider le pari
   architectural (jointure dérivée, zéro DDL) avant US-1 : il conditionne tout
   l'EPIC.
. *Risque de jointure titre TOC ↔ chunk* — taux **non mesuré** sur le corpus
   FPA réel. À mesurer en pré-US-2 (mitigation : canon `SectionTitleNormalizer`).
   Dégradé silencieux (page `null`), jamais d'échec de build.
. *Limite d'admission du rapport d'acquisition* — `OcrQualityReport.pageId`
   (nom d'image) ≠ page TOC (numéro) sur les 201 scans hétérogènes. Rapport =
   enrichissement **optionnel** (`@Optional`, clé = page TOC), non bloquant.
. *Grain du `sectionPath`* — la jointure normalise le **dernier segment**.
   Confirmer ce grain (vs path complet) : impact direct sur le taux de jointure.
. *Validation du cadrage* — US-1 ne démarre qu'après validation pilote du
   `CADRAGE_EPIC_CDX_PAGE_PROVENANCE.adoc`. Désaccord → traité au cadrage.

== Prochaine session — 224 : CDX-PAGE-PROVENANCE-1

. `TocPageIndex` — parse TOC AsciiDoc table (`Référence | Titre | Page |
  Fichier`, format `BookTocParser`) → sections typées. Pur.
. `ChunkPageProvenance` / `PageDoubt` — data classes `@Serializable`.
. `PageProvenanceResolver` — `resolve(chunks, tocSections, qualityReport)` :
  dernier segment `sectionPath` normalisé → titre TOC → page(s) → doute
  acquisition. Multi-pages (`pages: List<Int>`), dégradé silencieux.
. ~15-20 unit. Baby-step TDD strict RED → GREEN → REFACTOR.

*Gate* : `check` VERT (baseline 1164 + tests), Kover + validatePlugins VERTS.
Zéro bump BOM, zéro secret.

== Ordre global EPIC

US-1 (résolveur pur) → US-2 (tâche `collectPageProvenance`) → US-3 (champ N3
additif `pages`) → US-4 (Cucumber `@provenance`, 10e runner).

== Hors scope (documenté)

Pgvector N1 (D7) · ancre de page in-band (A1) · re-chunking par page (A2) ·
pollution TOC corpus FPA (cause racine amont).

== Méthode / Règles

Baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR, 3 niveaux (unit →
fonctionnel TestKit/ProjectBuilder → Cucumber), English only.
COMMITS/PUSH autorisés (décision pilote 2026-09-17). PAS de publication sans
permission. SECRETS via configuration/.

== References

- `.agents/INDEX.adoc` (S-223) · `.agents/BACKLOG.adoc` (EPIC 🟡 CADRÉ S-223)
- `.agents/CADRAGE_EPIC_CDX_PAGE_PROVENANCE.adoc` · `.agents/CADRAGE_EPIC_CDX_DOUBT_BRIDGE.adoc` (D6)
- `src/main/kotlin/codex/enrichment/SectionTitleNormalizer.kt` (canon de titre)
