= PROMPT_REPRISE — Codex — Session 222
:session: 222
:date: 2026-09-17
:status: PRET
:mission: PUBLICATION codex-plugin 0.0.10 (décision pilote)

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 1050 unit + 114 cucumber, 0 FAIL (S-221)
----

== Session 221 terminée — EPIC CDX-CONTEXT-HARDENING TERMINÉ 2/2 US

*US-1* — fallback `enrichedJsonFile` standalone. Le wiring plugin posait
l'`@InputFile @Optional` inconditionnellement → `generateCompositeContext`
seule échouait en validation Gradle (`Input file does not exist`), fallback
`""` inatteignable. Correctif : input **tolérant** `@InputFiles`
(`ConfigurableFileCollection`) — artefact ciblé par défaut, absence →
`""` (Économie d'Encre, jamais forcer `enrichJsonLdd`). Preuve par TestKit
réel (`CodexCompositeContextGradleValidationTest`, sentinel `doFirst`).

*US-2* — test d'exécution `execute()` (comble le trou P0 S-220). Seam
`@Internal storeOverride` + 4 tests pilotés par fake in-memory
`RecordingRagStore` (JSON N3 + vibecoding + metadata, standalone, dégradé,
canal Graphify). Audit des 16 `@TaskAction` : toutes couvertes.

*BDD* — 3 scénarios `hardened` ajoutés à `codex_doubt_bridge.feature`
(runner dédié réutilisé). 10/10 PASSED.

Rapport : `.agents/sessions/221-cdx-context-hardening.adoc`.

== Prochaine session — 222 : objectifs

=== Option A (RETENUE — décision pilote) — Publication codex-plugin 0.0.10

*Objectif prioritaire acté par le pilote : publier la release 0.0.10.*
Contenu depuis 0.0.9 : EPIC CDX-CONTEXT-HARDENING (S-221, fix input
tolérant `enrichedJsonFile` standalone + test d'exécution `execute()`).
La release rend le correctif US-1 disponible aux consommateurs.

Ordre DAG strict (pattern S-220) :
. *MEMPHIS d'abord* : bump `workspace-bom/gradle/libs.versions.toml`
   `codex-plugin = "0.0.9"` → `"0.0.10"` + version BOM/catalog `0.0.46`
   → `0.0.47` ; gardes `contracts.catalog.*` + `PlatformCatalogAlignmentTest` ;
   `publishToMavenLocal` VERT → publication Central (bundle AUTOMATIC).
. *codex ensuite* : bump `codex-plugin/gradle/libs.versions.toml`
   `codex-plugin = "0.0.9"` → `"0.0.10"` + pin platform `workspace-bom:0.0.47`
   + settings `workspace-catalog:0.0.47` ; garde `CodexPluginPublicationTest`
   3/3 (D5 — toml local == toml MEMPHIS, platform pin == catalog BOM) ;
   `build -x test` VERT → `publishAggregationToCentralPortal --no-daemon`.
. *Propagation* : vérifier repo1 HTTP 200 (codex-plugin 0.0.10 + BOM/catalog
   0.0.47) ; aligner consommateurs (le cas échéant).
. Note PUB-3.14 au BACKLOG (état 🟡 À FAIRE → ✅ TERMINÉ avec bundles).

*Aucun publish ne se fait sans permission explicite* — mais la décision
pilote acte que la 222 EST la session de publication.

=== Option B — Page-provenance (D6 cadrage S-219)

* Join chunk→page du `OcrQualityReport` — nécessite une provenance de page
  dans les chunks. EPIC futur distinct, hors scope DOUBT-BRIDGE.

=== Option C — Dogfooding enrichi (canal Graphify réel)

* Rejouer `enrichJsonLdd` (S-217, 115/115 sections) → `generateCompositeContext`
  avec le canal Graphify peuplé, mesurer le `composite-context.json`
  complet (EAGER/RAG/GRAPHIFY/DOCS).
* Consommateur transitoire `/tmp` (pattern Règle 2), base pgvector jetable.

=== Option D — Pollution TOC corpus FPA (99 chunks dot-leaders)

* Cause racine amont OCR/scan, pas le chunking codex (documenté S-216).

== Hors scope (documenté)

* Page-provenance (D6 cadrage S-219) : EPIC futur distinct.
* Pollution TOC du corpus FPA : cause racine amont OCR/scan.

== Méthode

Baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR, 3 niveaux (unit →
fonctionnel TestKit → Cucumber), English only.

== Règles

- COMMITS/PUSH — autorisés (décision pilote 2026-09-17).
- PAS DE publishToMavenLocal automatique sans permission — mais la décision
  pilote acte explicitement que la S-222 EST la session de publication 0.0.10.
- SECRETS via configuration/ — jamais en dur dans build.gradle.kts.

== References

- `.agents/INDEX.adoc` (à jour S-221)
- `.agents/BACKLOG.adoc` (PUB-3.14 🟡 À FAIRE)
- `.agents/sessions/221-cdx-context-hardening.adoc`
- `workspace-bom/` (bump 0.0.46 → 0.0.47, ordre DAG MEMPHIS-first)
