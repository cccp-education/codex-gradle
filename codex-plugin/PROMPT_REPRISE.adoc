= PROMPT_REPRISE — Codex — Session 106
:session: 106
:date: 2026-08-25
:status: PRET
:max-lines: 50

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline preserved (679 tests 0 FAIL)
----

== Session 105 terminée — Gouvernance : déplacement .adoc vers codex-plugin/.agents/

. Déplacement de 8 fichiers .adoc de la racine `codex-gradle/` vers
  `codex-plugin/.agents/` (AGENT, BACKLOG, 6 CADRAGE_EPIC_*).
. Vérification `check` VERT — 679 tests 0 FAIL, baseline préservée.
. Audit CB-STORE-UNIFY : aucune consommation résiduelle de
  `codebase.rag.VectorStore` dans codex-gradle. Le travail de migration
  (CB-STORE-UNIFY-1/2/3) relève de Queens (codebase-gradle), pas Brooklyn.

== Prochaine session — 106 : objectifs

*Objectif principal* : CB-STORE-UNIFY est un EPIC cross-borough dont le
code cible est dans codebase-gradle (Queens), pas codex-gradle (Brooklyn).
Pour codex-gradle, l'EPIC est terminé (zéro consommateur résiduel). Options :

. *Si Queens est disponible* : poursuivre CB-STORE-UNIFY côté codebase-gradle
  (migration consommateurs résiduels → suppression store JDBC legacy).
. *Si Queens indisponible* : identifier un autre EPIC dans le backlog codex
  ou faire de la maintenance (publication, code review, dette technique).

*Methode* : baby-step TDD strict RED → GREEN → REFACTOR, 3 niveaux
  (unit → fonctionnel → Cucumber), English only.

== References

- `.agents/sessions/105-governance-move.adoc`
- `.agents/CADRAGE_EPIC_CB_STORE_UNIFY.adoc`
- `.agents/BACKLOG.adoc` § EPIC CB-STORE-UNIFY