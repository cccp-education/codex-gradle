= PROMPT_REPRISE — Codex — Session 087
:session: 087
:date: 2026-08-20
:status: PRET
:max-lines: 45

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew test             # 365/365 PASS (baseline préservée S-086)
./gradlew cucumberTestOntology  # 3/3 scénarios CDX-3-3 PASSED
```

== Session 086 terminée — CDX-4-2 Tâche enrichJsonLdd + wiring

. *CDX-4-2 TERMINÉ* — `EnrichJsonLddTask` (80 lignes, thin wrapper
  Gradle pattern `DeriveOntologyTask`) consomme `jsonFile` (LDD) +
  `chunksFile` (chunks) + `graphifyFile` (graph.json) → appelle
  `JsonLddEnricher.enrich` → écrit `EnrichedLddNode` JSON.
  `@DisableCachingByDefault`, 4 properties (3 `@InputFile` + 1
  `@OutputFile`). `EnrichedLddNode` annoté `@Serializable` (additif,
  backward compat). Wiring `CodexPlugin` : tâche `enrichJsonLdd` groupe
  `transform`, `outputFile` default `build/codex/enriched-ldd.json`.
  KDoc 12 → 13 tâches. Baby-step TDD strict RED → GREEN → REFACTOR.
. *377 tests 0 FAIL* (365 unit + 6 cucumber licence + 6 cucumber
  ontology). `check` VERT, Kover VERT, validatePlugins VERT. Zéro
  bump BOM, zéro secret, zéro régression, backward compat total.

== Prochaine session — 087

*Mission principale* : *CDX-4-3* câblage canal Graphify dans
`CodexCompositeContextTask` (`graphifySection` peuplé depuis
`enrichedJsonFile` au lieu de `""`). ~3 unit. Non-regression : fallback
`""` si enrichissement absent. Domaine `codex.enrichment` consommé par
le composite context.

*Alternatives* :
. *CDX-4-4* (Cucumber BDD `codex_enrich_json_ldd.feature` 4 scénarios
  `@enrichment` + runner dédié `CodexEnrichmentCucumberRunner`).
. *CDX-6-1/2* (DSL `licenceRouting` + `CodexPipelineTask` routing).
. *CDX-7-2* (`ConvertToMarkdownTaskTest` chaîné ~5 unit).

*Enchaînement* (ordre d'exécution du cadrage S-077) :
. Domaines purs P1 TERMINÉS (CDX-3-1/4-1/5-1 ✅).
. Phase tâches Gradle : CDX-5-2 ✅ → CDX-3-2 ✅ → CDX-4-2 ✅ → câblage
  (CDX-4-3) → DSL (CDX-6-1/2).
. Cucumber BDD : CDX-3-3 ✅ → CDX-5-3 ✅ → CDX-4-4 → CDX-6-3.

*Actions reportées* :
. Démarrer EPIC RUNTIME-CONTRACTS côté MEMPHIS (US RC-1..7) pour
  débloquer US CDX-RC-04 (gate publication `runtime-contracts:0.0.1`).

== References

- `.agents/sessions/086-cdx-4-2-enrich-json-ldd-task.adoc`
- `CADRAGE_EPIC_P1_CORPUS_ENRICHMENT.adoc` — doc cadrage 5 EPICs P1
- `BACKLOG.adoc` § EPIC CDX-4/6/7 (US détaillées)
- `build.gradle.kts:175` — tâche `cucumberTestOntology` (pattern S-082)
- Central : codex-plugin 0.0.4 (bundle `8556eea3`), BOM 0.0.23 (bundle `8dc8e513`)