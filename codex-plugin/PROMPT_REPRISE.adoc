= PROMPT_REPRISE — Codex — Session 214
:session: 214
:date: 2026-09-07
:status: PRET

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline preserved (754 tests 0 FAIL)
----

== Session 213 terminée — US-ROLLOUT-6 readme-gradle (cross-borough Chiraq pilotée depuis Brooklyn)

Tri candidats ROLLOUT-6 : codex déjà ROLLOUT-2 ✅ (S-211) ; readme retenu
(entrée catalog Central 0.0.31 présente + split-brain latent) ; dashboard
reporté (zéro entrée catalog, bump MEMPHIS requis). Exécution côté readme
(Chiraq) : garde `ReadmePluginPublicationTest` 3/3 (RED → GREEN, mirror
`CodexPluginPublicationTest` S-211) + pin ws 0.0.31 (D4) + version dérivée
ws (D3) + id inline. Gate : `check` VERT 83 tests 0 FAIL (baseline 80 → +3)
+ `publishToMavenLocal` VERT (POM 0.0.1 version dérivée ws).
Rapport : `.agents/sessions/213-us-rollout-6-readme.adoc`.

== Prochaine session — 214 : objectifs

[IMPORTANT]
Borough Brooklyn DORMANT — tous EPICs P0/P1 terminés (OCR-BOUNDARY 4/4,
RAG-SOCLE 6/6, OCR-CONTRACTS 5/5, CROSS-DOC-BOOK 3/3, CB-STORE-UNIFY
CADUQUE). Seul MEM-CAT-ROLLOUT reste ouvert (ordre 6 partiel : readme ✅,
dashboard/graphify ⏳ À PLANIFIER).

=== Option A — Cross-borough MEM-CAT-ROLLOUT (suite)

* ROLLOUT-6 suite : graphify-gradle (entrée catalog présente, baseline à
  auditer) — référentiel `workspace-bom/BACKLOG.adoc`.
* ROLLOUT-6 dashboard-gradle : GATE MEMPHIS — bump catalog requis (zéro
  entrée `dashboard` dans workspace-bom toml) à la demande du pilote.
* ROLLOUT-3 plantuml / ROLLOUT-4 hyperframes / ROLLOUT-5 runner déjà livrés
  par MEMPHIS S-032 (hors périmètre codex).

=== Option B — Maintenance

* Publication, code review, dette technique ciblée.

=== Option C — Nouvel EPIC

* Cadrer si besoin identifié.

== Méthode

Baby-step TDD strict RED → GREEN → REFACTOR, 3 niveaux (unit → fonctionnel
→ Cucumber), English only.

== Règles

- COMMITS/PUSH — INTERDICTION FORMELLE sans permission pilote.
- PAS DE publishToMavenLocal automatique sans permission.
- SECRETS via configuration/ — jamais en dur dans build.gradle.kts.

== References

- `.agents/INDEX.adoc` (à jour S-213)
- `.agents/BACKLOG.adoc` (en-têtes EPICs ✅ resynchronisés S-212 gouvernance)
- `.agents/sessions/213-us-rollout-6-readme.adoc`
- `.agents/CADRAGE_EPIC_CROSS_DOC_BOOK.adoc` (✅ TERMINÉ S-210)