= PROMPT_REPRISE — Codex — Session 103
:session: 103
:date: 2026-08-24
:status: PRET
:max-lines: 50

== Quick Start

----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline preserved (0 FAIL)
----

== Session 102 terminee — EPIC CDX-RAG-SOCLE 5/6 US

. RAG-2 : 6 call sites codebase `CodexVectorStore` → `RagVectorStore`
  (API identique, 6 fichiers). Gate non-regression codebase VERT.
. RAG-3 : codex delegue au socle `codebase.store` — dep
  `libs.codebase.plugin` ajoutee, 3 taches thin wrappers
  (CodexCompositeContextTask/CodexIngestTask/CodexRetrieveTask),
  suppression `codex.store.*` (3 prod + 3 tests mirrores),
  `CodexRetrieveTask` refondu. Fix `pluginUnderTestMetadata`.
. RAG-4 : PARTIEL — `codex.store.*` imports elimines (RAG inversion
  morte), mais `libs.codex.plugin` conserve pour le port OCR
  (`codex.ocr.OcrEngine`/`OcrRequest`/`OcrResult` consommes par
  `VisionOcrEngineAdapter` S-099). Gate : extraction `codex.ocr` en
  artefact N0 (EPIC futur CDX-OCR-CONTRACTS).
. RAG-5 : Cucumber BDD `@rag-socle` 6 scenarios cote codebase +
  `RagSocleCucumberRunner` + `StubRagStore` fake + `RagVectorStore`
  `open` pour testabilite. 6/6 PASSED.
. `check` VERT codex 679 tests 0 FAIL + codebase check VERT. Zero
  bump BOM, zero secret, zero regression.

== Prochaine session — 103 : RAG-4 final OU CB-STORE-UNIFY

*Option A* : RAG-4 final — extraire `codex.ocr` port types
(`OcrEngine`, `OcrRequest`, `OcrResult`) en artefact N0
`codex-contracts` (MEMPHIS ou standalone). Retirer `libs.codex.plugin`
de codebase apres `rg "import codex."` = zero. L'inversion N1->N2
est totalement morte.

*Option B* : EPIC CB-STORE-UNIFY — consolidation
`codebase.rag.VectorStore` JDBC legacy (DROP CASCADE destructif) apres
migration RAG-SOCLE. Gate : RAG-1→4 (RAG-4 partiel OK — le store
RAG est migre, le JDBC legacy est independant).

== References

- `.agents/sessions/102-cdx-rag-socle-rag-2-3-5.adoc`
- `CADRAGE_EPIC_CDX_RAG_SOCLE.adoc` (RAG-4 gate OCR port)
- `BACKLOG.adoc` § EPIC CDX-RAG-SOCLE + § EPIC CB-STORE-UNIFY