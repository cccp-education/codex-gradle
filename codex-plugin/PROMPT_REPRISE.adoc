= PROMPT_REPRISE — Codex — Session 099
:session: 099
:date: 2026-08-22
:status: PRET
:max-lines: 50

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew test             # 667/667 PASS (baseline 661 + 6 CDX-OCR-1)
```

== Session 098 terminée — CDX-OCR-1 ✅ (port AI OCR injecté)

. Characterization : `OcrPipelineTest` (S-072) verrouillait déjà l'ordre
  fallback + sortie AsciiDoc — complété par task-level
  `CollectOcrAiEngineInjectionTest` (6 unit RED→GREEN).
. `CollectOcrTask` : propriété `@Internal aiEngine: Property<OcrEngine>`
  ajoutée ; chaîne = AI injecté → Tesseract, défaut sans injection =
  Tesseract seul (dégradé fonctionnel acté). Props ollama inertes mais
  conservées (purge en OCR-3).
. `OcrEngine` converti `fun interface` + KDoc boundary à jour.
. `check` VERT : 667 tests 0 FAIL + 52 cucumber dédiés PASSED.

== Prochaine session — 099 : CDX-OCR-2 (cross-borough Queens/codebase)

*Baby-step TDD strict RED → GREEN → REFACTOR (~4 unit)* :
. Adapter `VisionOcrEngineAdapter : codex.ocr.OcrEngine` côté codebase —
  wrappe `VisionProvider` (IA), mapping bytes/mimeType → `OcrRequest`.
. Suppression `CodexOcrEngineAdapter` (sens inverse de la boundary).
. Retrait import `TesseractOcrEngine` de `OcrTask` codebase
  (IA-uniquement).
. Gate non-régression codebase (666+ tests Queens intacts).

*Ensuite* (ordre) : CDX-OCR-3 (purge pile IA codex, gated par 1+2) →
CDX-OCR-4 (Cucumber fake engine) → RAG-1 → RAG-2 → gate levée →
RAG-3 → RAG-4 → RAG-5.

== References

- `.agents/sessions/098-cdx-ocr-1-ai-engine-injection.adoc`
- `CADRAGE_EPIC_CDX_OCR_BOUNDARY.adoc` (US-2 : décisions 5, 7, 8)
- `CADRAGE_EPIC_CDX_RAG_SOCLE.adoc` (gate OCR-1/2 avant RAG-3/4)
- `BACKLOG.adoc` § EPIC CDX-OCR-BOUNDARY
