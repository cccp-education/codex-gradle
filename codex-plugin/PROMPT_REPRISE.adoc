= PROMPT_REPRISE — Codex — Session 083
:session: 083
:date: 2026-08-19
:status: PRET
:max-lines: 45

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
../gradlew build -x test   # compile OK, validatePlugins OK
../gradlew test            # 306/306 PASS (baseline préservée S-082)
```

== Session 082 terminée — CDX-5-2 Tâche deployKnowledgeBaseRouted + wiring

. *CDX-5-2 TERMINÉ* — baby-step TDD strict RED → GREEN → REFACTOR.
. *`DeployKnowledgeBaseRoutedTask`* (nouveau, 69 lignes) étend
  `ExportKnowledgeBaseTask` : détecte licence PDF via
  `PdfLicenseDetector` → route via `LicenseRouter` → rebind
  `outputDir` → délègue à `super.export()` (multi-format inchangé).
  3 props Gradle : `pdfFile` (`@InputFile`), `baseDir`
  (`@OutputDirectory`), `fallbackZone` (`@Input @Optional`).
  `@DisableCachingByDefault` (validatePlugins).
. *`ExportKnowledgeBaseTask.export()` rendu `open`* — backward
  compat total, 1 mot-clé.
. *Wiring `CodexPlugin`* — task `deployKnowledgeBaseRouted` groupe
  `deploy`, `fallbackZone.convention(zone)` (apply-time).
. *6 tests* (5 functional + 1 wiring) — Apache→OSS, Copyright→office,
  Unknown+OSS fallback, Unknown+CSS fallback, CC-BY→OSS, plugin
  registers task. 306 tests 0 FAIL (baseline 300 → +6). `check` VERT,
  Kover VERT, validatePlugins VERT.
. *Zéro bump BOM, zéro secret, zéro régression, backward compat total.*

== Prochaine session — 083

*Mission principale* : *CDX-5-3* (Cucumber BDD `codex_export_routed.feature`
3 scénarios `@licence @routing` + runner dédié `CodexLicenceCucumberRunner`
pattern S-082) — clôture EPIC CDX-5.

. Baby-step BDD strict : RED (3 `UndefinedStepException`) → GREEN
  (3/3 scénarios PASSED) → REFACTOR.
. Scénarios : PDF Apache → OSS/, PDF copyright → office/, PDF unknown
  → fallback zone projet.
. Runner dédié `CodexLicenceCucumberRunner` + `CucumberTaskSpec`
  (pattern S-082 capsule/slider).
. Domaine `codex.licence` déjà créé (S-081), tâche déjà câblée (S-082)
  — pure BDD, zéro production modifié.

*Alternatives* :
. *CDX-3-2* (tâche `deriveOntology` + wiring ~4 functional) — 1re
  tâche Gradle ontology.
. *CDX-4-2* (tâche `enrichJsonLdd` + wiring ~5 functional) — 1re
  tâche Gradle enrichment.
. *CDX-6-1/2* (DSL `licenceRouting` + `CodexPipelineTask` routing).

*Enchaînement* (ordre d'exécution du cadrage S-077) :
. Domaines purs P1 TERMINÉS (CDX-3-1/4-1/5-1 ✅).
. Phase tâches Gradle : CDX-5-2 ✅ (S-082) → CDX-3-2 / CDX-4-2 (~4-5
  functional chacune) → câblage (CDX-4-3) → DSL (CDX-6-1/2) →
. Cucumber BDD (CDX-3-3/4-4/5-3/6-3).

*Actions reportées* :
. Démarrer EPIC RUNTIME-CONTRACTS côté MEMPHIS (US RC-1..7) pour
  débloquer US CDX-RC-04 (gate publication `runtime-contracts:0.0.1`).

== References

- `.agents/sessions/082-cdx-5-2-deploy-routed.adoc`
- `CADRAGE_EPIC_P1_CORPUS_ENRICHMENT.adoc` — doc cadrage 5 EPICs P1
- `BACKLOG.adoc` § EPIC CDX-3/4/5/6/7 (US détaillées)
- `build.gradle.kts:30` — BOM 0.0.23
- Central : codex-plugin 0.0.4 (bundle `8556eea3`), BOM 0.0.23 (bundle `8dc8e513`)