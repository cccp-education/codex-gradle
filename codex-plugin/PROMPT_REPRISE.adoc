= PROMPT_REPRISE — Codex — Session 217
:session: 217
:date: 2026-09-08
:status: PRET

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 682 tests 0 FAIL (pin BOM 0.0.36 réaligné S-216)
----

== Session 216 terminée — CDX-CORPUS run acquisition corpus FPA (GBL-003.3)

*CDX-CORPUS-0/1/3 ✅ TERMINÉS* — corpus FPA (livre.adoc 7787 lignes) acquis
en 387 chunks sémantiques + knowledge base 3 formats, dans
`office/metiers/FPA/Devenir_.../codex-out/` (œuvre dérivée PROPRIETARY).
Pipeline codex dogfoodé *sans modification du build script codex* :
`transformToMarkdown` → `transformChunk` → `deployKnowledgeBase` +
`corpusPublish` (move office/ + wipe /tmp, pattern FPA-BOOK-4 Règle 2).
Cadrage : `.agents/CADRAGE_CDX_CORPUS_RUN.adoc`. Rapport run + validation
qualité : `codex-out/RAPPORT_CDX_CORPUS_RUN.adoc`.

+ fix drift BOM cross-borough : pin platform 0.0.33 → 0.0.36 + pin
settings workspace-catalog 0.0.36 (garde `CodexPluginPublicationTest`
avait attrapé le bump MEMPHIS post-S-214). Gates : check VERT 682/0.

Rapport : `.agents/sessions/216-cdx-corpus-run.adoc`.

== Prochaine session — 217 : objectifs

=== Option A — OCR-QUALITY-1/2 (critical path, P0) 🟡 RECOMMANDÉE

* OCR-QUALITY-1 : Tesseract vraie confidence — `meanConfidence()` réel
  dans `TesseractOcrEngine` (TDD, cadrage S-215 `CADRAGE_EPIC_OCR_QUALITY.adoc`).
* OCR-QUALITY-2 : détection images problématiques à l'acquisition
  (image fantôme, page quasi vide, marqueur [ILLISIBLE]).
* Débloque : OCR-QUALITY-3 (document) + OCR-QUALITY-4 (codebase doubt
  metadata GBL-003.4) — le rapport S-216 (`codex-out/RAPPORT_CDX_CORPUS_RUN.adoc`)
  documente 19 chunks `[ILLISIBLE]` candidats douteux.

=== Option B — CDX-CORPUS-2 (enrichissement graphe, optionnel)

* Enrichissement chunks via graphe GF-FPA (domaine `codex.enrichment`
  existant) — vérifier la fraîcheur de `office/metiers/FPA/graph-content.json`
  (GBL-003.2) avant consumption.

=== Option C — Maintenance / ROLLOUT-6 dashboard

* À la demande pilote (gate bump MEMPHIS requis).

== Méthode

Baby-step TDD strict RED → GREEN → REFACTOR, 3 niveaux (unit → fonctionnel
→ Cucumber), English only.

== Règles

- COMMITS/PUSH — INTERDICTION FORMELLE sans permission pilote.
- PAS DE publishToMavenLocal automatique sans permission.
- SECRETS via configuration/ — jamais en dur dans build.gradle.kts.

== References

- `.agents/INDEX.adoc` (à jour S-216)
- `.agents/BACKLOG.adoc` (EPICs CDX-CORPUS ✅ US-0/1/3, OCR-QUALITY 🟡)
- `.agents/CADRAGE_EPIC_OCR_QUALITY.adoc` (cadrage prêt)
- `.agents/CADRAGE_CDX_CORPUS_RUN.adoc` (run S-216)
- `codex-out/RAPPORT_CDX_CORPUS_RUN.adoc` (validation qualité + chunks douteux)