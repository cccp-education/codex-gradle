= PROMPT_REPRISE — Codex — Session 221
:session: 221
:date: 2026-09-17
:status: PRET

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 1010 unit + 108 cucumber, 0 FAIL (S-220)
----

== Session 220 terminée — maintenance + publication 0.0.9 + dogfooding

*C — Maintenance* : drift MEMPHIS réaligné (pin 0.0.43→0.0.44, garde
`CodexPluginPublicationTest` était RED à HEAD). Marqueur périmé CADRAGE
DOUBT-BRIDGE corrigé.

*A — Publication* : deux releases Central. 0.0.8 (bundle `b573cf61`,
MEMPHIS 0.0.45 `d199bf00`) puis 0.0.9 corrective (bundle `7849b4b1`,
MEMPHIS 0.0.46 `d1c09b5e`). Propagation repo1 vérifiée HTTP 200.

*B — Dogfooding* : run `collectIngest` doubt-aware → **387 chunks /
18 doubtful / avg 0.9535** (identique S-214). Le run a découvert un
**bug P0 préexistant** : `generateCompositeContext` levait
`Serializer for class 'Any' is not found` (`Map<String, Any>` non
sérialisable kotlinx). Corrigé TDD strict (3 UT `CodexCompositeContextJsonTest`
+ 2 scénarios BDD) via seams `buildCompositeJson`/`buildVibecodingJson`
(pattern `JsonObject` de `ExportKnowledgeBaseTask`). Re-run vert : 12
entries, 1 doubtful exposé, canal Docs peuplé.

Rapport : `.agents/sessions/220-maintenance-pub-009-dogfooding.adoc`.

== Prochaine session — 221 : objectifs

=== Option A — Second constat B : fallback `enrichedJsonFile` standalone

* Le wiring plugin pose inconditionnellement `enrichedJsonFile`
  (`CodexPlugin.kt:219`, `build/codex/enriched-ldd.json`) alors que la
  propriété est `@Optional` et que `buildGraphifySection()` gère
  l'absence (fallback `""`).
* Conséquence : `generateCompositeContext` seule échoue en validation
  Gradle (`Input file does not exist`) sans `enrichJsonLdd` préalable —
  le fallback documenté est inatteignable.
* Correctif candidat : soit retirer le wiring par défaut (laisser la
  propriété non configurée → fallback `""`), soit `dependsOn("enrichJsonLdd")`.
  Décider (économie d'encre vs canal Graphify par défaut).
* TDD strict : test d'exécution `execute()` avec fake store (comble le
  trou qui a laissé passer le P0).

=== Option B — Test d'exécution `execute()` (comble le trou P0)

* Aucun test n'exécute `CodexCompositeContextTask.execute()` de bout en
  bout. Un test avec `RagVectorStore` fake (sous-classe open) + fichier
  temporaire couvrirait la vraie chaîne (résultats → JSON → écriture →
  metadata).
* Généraliser aux autres tâches à `execute()` non couvert (audit).

=== Option C — Dogfooding enrichi (canal Graphify réel)

* Rejouer `enrichJsonLdd` (S-217, 115/115 sections) → `generateCompositeContext`
  avec le canal Graphify peuplé, mesurer le `composite-context.json`
  complet (EAGER/RAG/GRAPHIFY/DOCS).

=== Option D — Maintenance / cross-borough

* À la demande pilote uniquement.

== Hors scope (documenté)

* *Page-provenance* (D6 cadrage S-219) : join chunk→page du
  `OcrQualityReport` — EPIC futur distinct.
* *Pollution TOC* du corpus FPA (99 chunks dot-leaders) : cause racine
  amont OCR/scan, pas le chunking codex.

== Méthode

Baby-step TDD strict RED → GREEN → REFACTOR, 3 niveaux (unit → fonctionnel
→ Cucumber), English only.

== Règles

- COMMITS/PUSH — autorisés (décision pilote 2026-09-17).
- PAS DE publishToMavenLocal automatique sans permission.
- SECRETS via configuration/ — jamais en dur dans build.gradle.kts.

== References

- `.agents/INDEX.adoc` (à jour S-220)
- `.agents/BACKLOG.adoc`
- `.agents/sessions/220-maintenance-pub-009-dogfooding.adoc`
