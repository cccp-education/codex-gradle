= PROMPT_REPRISE — Codex — Session 219
:session: 219
:date: 2026-09-17
:status: PRET

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 872 tests 0 FAIL (S-218)
----

== Session 218 terminée — OCR-QUALITY US-1/2 (codex) TERMINÉES

*OCR-QUALITY-1 ✅* — vraie confidence Tesseract : `TesseractTsvParser`
objet pur (moyenne TSV word-level normalisée [0,1], mots blancs/confiance
négative ignorés) + `TesseractOcrEngine` invoqué avec config `tsv` +
`metadata["confidenceSource"]` = `"tsv"` ou `"degraded"`. `OcrPipeline`
ne gate plus sur la confiance (signal de doute aval, pas critère de
fallback — sinon le texte dégradé serait jeté).

*OCR-QUALITY-2 ✅* — `OcrQualityAnalyzer` objet pur (ILLISIBLE, TOO_SHORT,
LOW_CONFIDENCE, IMAGE_MISSING, macro `image::?` bloc+inline) +
`CollectOcrTask.qualityReportFile` optionnel (rapport JSON) +
`CodexExtension.ocrLowConfidenceThreshold` (défaut 0.5).

*EPIC OCR-QUALITY complété* : US-1/2 codex (S-218) + US-3 document
(S-251 ✅) + US-4 codebase (S-213 ✅).

Gate : check VERT 872 tests 0 FAIL (baseline 704 → +168). 9 suites
cucumber dédiées VERTES. + Fix drift BOM 0.0.39→0.0.43 cross-borough
MEMPHIS (piège #13).

Rapport : `.agents/sessions/218-ocr-quality-confidence-detection.adoc`.

== Prochaine session — 219 : objectifs

=== Option A — GBL-003.4 RAG doubt metadata (critical path, P0) 🟡 RECOMMANDÉE

* Les rapports de doute sont désormais produits des deux côtés (codex
  acquisition `OcrQualityReport`, document assembly `BookOcrIssueReport`).
* Coordination codebase (US-4 ✅ S-213 `DoubtMetadata`/`ingestWithDoubt`
  déjà en place) : brancher le rapport d'acquisition codex sur l'ingestion
  RAG pour que le contexte augmenté pèse/exclut les pages douteuses.
* Option B : dogfooding run corpus FPA réel avec le nouveau rapport
  qualité (mesure du gain vs découverte manuelle).

=== Option B — Publication codex-plugin (si accord pilote)

* HEAD (S-218) non publié. Version locale 0.0.7, Central 0.0.7 (S-217).
* Bump 0.0.8 requis + MEMPHIS catalog/BOM.

=== Option C — Maintenance / cross-borough

* À la demande pilote uniquement.

== Méthode

Baby-step TDD strict RED → GREEN → REFACTOR, 3 niveaux (unit → fonctionnel
→ Cucumber), English only.

== Règles

- COMMITS/PUSH — autorisés (décision pilote 2026-09-17).
- PAS DE publishToMavenLocal automatique sans permission.
- SECRETS via configuration/ — jamais en dur dans build.gradle.kts.

== References

- `.agents/INDEX.adoc` (à jour S-218)
- `.agents/BACKLOG.adoc` (EPIC OCR-QUALITY ✅ 4/4 US)
- `.agents/CADRAGE_EPIC_OCR_QUALITY.adoc` (EPIC complet)
- `codex-out/RAPPORT_CDX_CORPUS_RUN.adoc` (chunks douteux GBL-003.4)
