= PROMPT_REPRISE — Codex — Session 225
:session: 225
:date: 2026-09-17
:status: PRET
:mission: mission à trancher par le pilote (EPIC CDX-PAGE-PROVENANCE TERMINÉ S-224)

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 1393 tests (1263 + 130 cucumber), 0 FAIL
----

== Session 224 terminée — EPIC CDX-PAGE-PROVENANCE TERMINÉ 5/5 US

Traçabilité du doute OCR jusqu'à la page source, par **jointure dérivée non
stockée** (zéro DDL, zéro re-vectorisation). Domaine `codex.provenance` créé
(6 fichiers prod) + tâche `collectPageProvenance` → `page-provenance.json`
+ champ N3 additif `pages` + Cucumber `@provenance` (10e runner).

*Découverte structurante* : le grain du cadrage D2 (dernier segment) joint
seulement **24,8 %** du corpus FPA réel. La **marche d'ancêtre
deepest-first** + `ProvenanceTitleNormalizer` (référence numérique en tête,
ponctuation en queue) atteint **70,8 %** (274/387 chunks), pinné par test
d'intégration. Déviation D6 documentée : jointure retrieval → page par
`sectionPath` (le `chunkId` de `RetrieveResult` est le BIGSERIAL pgvector,
pas l'id SHA-256).

Rapport : `.agents/sessions/224-cdx-page-provenance.adoc`.

== Enjeux gardés pour la reprise (à trancher par le pilote)

. *`CB-PAGE-PROVENANCE` (N1 Queens)* — persister la page dans `codex_chunks`
   + exposer `RetrieveResult.page` pour localiser le doute **au retrieval
   sans sidecar**. Cross-borough, hors scope codex (D7). À cadrer seulement
   si un consommateur N3/N4 exige la page sans le sidecar.
. *Pollution TOC/OCR du corpus FPA* — 99 chunks dot-leaders, titres OCR
   divergents (cause racine amont). Améliorerait le taux de jointure au-delà
   de 70,8 % mais touche `office/` (œuvre dérivée privée).
. *EPIC PUB* — publier une version codex intégrant la provenance (Central) ;
   nécessite l'accord pilote + bump MEMPHIS catalogue.
. *Seuil du plancher de jointure* — le test réel pinne `>= 0.5` ; mesuré
   0,708. Ajuster si le corpus évolue.

== Candidats de mission (aucun engagé)

. Option A — cadrage `CB-PAGE-PROVENANCE` (cross-borough Queens, gouvernance
   pure pattern S-057).
. Option B — EPIC PUB : publication Central (0.0.11) intégrant la provenance.
. Option C — attente / maintenance (le borough est sain, tous EPICs cadrés
   terminés).

== Ordre global EPIC (TERMINÉ)

US-0 cadrage (S-223) → US-1 résolveur pur (S-224) → US-2 tâche (S-224) →
US-3 N3 (S-224) → US-4 Cucumber (S-224) → US-5 mesure réelle (S-224).

== Hors scope (documenté)

Pgvector N1 (D7) · ancre de page in-band (A1) · re-chunking par page (A2) ·
pollution TOC corpus FPA (cause racine amont).

== Méthode / Règles

Baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR, 3 niveaux (unit →
fonctionnel TestKit/ProjectBuilder → Cucumber), English only.
COMMITS/PUSH autorisés (décision pilote 2026-09-17). PAS de publication sans
permission. SECRETS via configuration/.

== References

- `.agents/INDEX.adoc` (S-224) · `.agents/BACKLOG.adoc` (EPIC ✅ TERMINÉ S-224)
- `.agents/CADRAGE_EPIC_CDX_PAGE_PROVENANCE.adoc` (cadrage S-223)
- `src/main/kotlin/codex/provenance/` (domaine)
- `src/main/kotlin/codex/tasks/CollectPageProvenanceTask.kt` (tâche)
- `.agents/sessions/224-cdx-page-provenance.adoc` (rapport)
