= PROMPT_REPRISE — Codex — Session 105
:session: 105
:date: 2026-08-24
:status: PRET
:max-lines: 50

== Quick Start

----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline preserved (0 FAIL)
----

== Session 104 terminee — CDX-OCR-CONTRACTS US-2

. US-1 MEMPHIS : artefact N0 `ocr-contracts:0.0.1` (3 types purs
  `contracts.ocr`, 14/14 unit PASSED, publié mavenLocal).
. US-2 codex : typealiases `codex.ocr.{OcrEngine,OcrRequest,OcrResult}`
  → `contracts.ocr.*`. Jackson helpers extraits en top-level functions
  (`OcrRequestJson.kt`/`OcrResultJson.kt`) — typealias ne propage pas le
  companion object. Tests migrés. Gate VERT.
. Bump codex-plugin 0.0.5 → 0.0.6.
. Cross-borough : US-3 codebase livrée en parallèle S-201
  (`libs.codex.plugin` retiré, imports basculés `contracts.ocr.*`)
  — **RAG-4 FINAL : l'inversion N1→N2 est morte**.

== Prochaine session — 105 : EPIC CDX-OCR-CONTRACTS US-4

*US-4 Cucumber BDD* — scénarios boundary OCR sur port N0 (fake AI engine +
degraded Tesseract + cache hit). Validation end-to-end du contrat N0.
Pure BDD, zéro prod modifié. Clôture EPIC + RAG-4 final.

Ensuite : publication codex-plugin 0.0.6 Maven Central (PUB-3.12) +
publication MEMPHIS ocr-contracts:0.0.1 Central + BOM 0.0.26 (accord pilote requis).

*Baby-step TDD strict* : RED → GREEN → REFACTOR, 3 niveaux
(unit → functional → Cucumber).

== References

- `.agents/sessions/104-cdx-ocr-contracts-us2-typealiases.adoc`
- `CADRAGE_EPIC_CDX_OCR_CONTRACTS.adoc` (10 décisions archi)
- BACKLOG § EPIC CDX-OCR-CONTRACTS