= PROMPT_REPRISE — Codex — Session 077
:session: 077
:date: 2026-08-19
:status: PRET
:max-lines: 40

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
../gradlew build -x test   # compile OK, validatePlugins OK
../gradlew test            # 211/211 PASS (baseline préservée S-076)
```

== Session 076 terminée — Cadrage CDX-RC-04 + koog cleanup + publication 0.0.4

Six éléments actionnables traités (ordre 6,1,2,3,5,4) :

1. **Cadrage CDX-RC-04** (gouvernance pure) — US `CDX-RC-04` cadrée dans
   BACKLOG.adoc documentant le rôle de codex comme implémenteur privé du
   contrat `SessionMemoryContract` (mémoire incrémentale RAG) de l'EPIC
   RUNTIME-CONTRACTS-3 MEMPHIS. Table dédiée `codex_learner_profiles`,
   domaine `codex.profile` (12e), gate publication MEMPHIS
   `runtime-contracts:0.0.1`.
2. **koog cleanup** — dépendance morte `koog-agents:1.0.0` supprimée
   (zéro usage dans src). build.gradle.kts + libs.versions.toml nettoyés.
3. **BOM bump** — workspace-bom 0.0.21 → 0.0.22 dans build.gradle.kts.
4. **Session 031 🟡** clôturée ✅ CADUQUE (koog supprimé, migration sans objet).
5. **CDX-15** (EPIC fantôme) ✅ CADUQUE — jamais cadré, résidu AGENTS racine.
6. **Publication 0.0.4** — codex-plugin 0.0.4 sur Central (bundle
   `8556eea3`) + BOM 0.0.23 sur Central (bundle `8dc8e513`, constraint
   codex 0.0.4). 211 tests 0 FAIL, `check` VERT, zéro régression, zéro secret.

== Prochaine session — 077

1. *Bump consommateur codex vers BOM 0.0.23* — propagation Central 0.0.23
   effective, changer `build.gradle.kts` ligne 30 `0.0.22` → `0.0.23`.
2. *Démarrer EPIC RUNTIME-CONTRACTS côté MEMPHIS* (US RC-1..7) pour débloquer
   l'US CDX-RC-04 côté codex (gate publication `runtime-contracts:0.0.1`).
3. *Sync AGENTS.adoc racine* — corriger mention "CDX-15 TDD rétroactif
   suspendu" ligne 480 (remplacer par "CDX-15 ✅ CADUQUE").
4. *DOGFOODING E2E* collectOcr→bookPipeline cross-borough (codex→document)
   sur un vrai livre de `office/books-collection/`. À valider.

== References

- `.agents/sessions/076-cdx-rc04-koog-cleanup-pub-0.0.4.adoc`
- `BACKLOG.adoc` § EPIC CDX-RC-04 (cadrage pont RAG SessionMemoryContract)
- `BACKLOG.adoc` § EPIC CDX-15 (✅ CADUQUE)
- `build.gradle.kts:30` — BOM 0.0.22 (bump 0.0.23 reporté propagation)
- `gradle/libs.versions.toml` — koog supprimé, codex-plugin 0.0.4
- Central : codex-plugin 0.0.4 (bundle `8556eea3`), BOM 0.0.23 (bundle `8dc8e513`)