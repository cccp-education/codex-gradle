= PROMPT_REPRISE — Codex — Session 104
:session: 104
:date: 2026-08-24
:status: PRET
:max-lines: 50

== Quick Start

----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline preserved (0 FAIL)
----

== Session 103 terminee — Cadrage EPIC CDX-OCR-CONTRACTS

. RAG-4 partiel S-102 : `libs.codex.plugin` conservé car
  `VisionOcrEngineAdapter` (codebase) consomme
  `codex.ocr.{OcrEngine,OcrRequest,OcrResult}`.
. Cadrage EPIC CDX-OCR-CONTRACTS (gouvernance pure, zéro code) :
  extraction 3 types en artefact N0 `ocr-contracts` (9e MEMPHIS).
. 6 constats, 10 décisions archi (typealiases backward compat,
  Jackson helpers restent codex, package `contracts.ocr`).
. 5 US ~7 pts (US-0 ✅, US-1→4 À FAIRE).
. Doc `CADRAGE_EPIC_CDX_OCR_CONTRACTS.adoc`.
. Baseline vérifiée : build VERT, 679 tests 0 FAIL.

== Prochaine session — 104 : EPIC CDX-OCR-CONTRACTS US-1

*US-1 MEMPHIS* : artefact `ocr-contracts` module standalone pattern
`runtime-contracts` (settings.gradle.kts + build.gradle.kts + gradlew
+ libs.versions.toml). 3 types purs package `contracts.ocr` :
`OcrEngine` fun interface, `OcrRequest` data class (4 champs, sans
Jackson), `OcrResult` data class (7 champs + `of` companion factory).
~10 unit (mirror OcrRequestTest/OcrResultTest sans Jackson).
Publication `ocr-contracts:0.0.1` Central (nmcp). Bump BOM constraint.

Enchaîner US-2 (codex typealiases + extension functions + bump 0.0.6)
si temps restant.

*Baby-step TDD strict* : RED → GREEN → REFACTOR, 3 niveaux
(unit → functional → Cucumber).

== References

- `.agents/sessions/103-cdx-ocr-contracts-cadrage.adoc`
- `CADRAGE_EPIC_CDX_OCR_CONTRACTS.adoc` (10 décisions archi)
- `BACKLOG.adoc` § EPIC CDX-OCR-CONTRACTS