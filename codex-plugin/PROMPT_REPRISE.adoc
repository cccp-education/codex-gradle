= PROMPT_REPRISE — Codex — Session 223
:session: 223
:date: 2026-09-17
:status: PRET
:mission: à la demande du pilote

== Quick Start
----
cd /home/cheroliv/workspace/foundry/public/codex-gradle/codex-plugin
./gradlew build -x test   # compile OK, validatePlugins OK
./gradlew check           # baseline 1050 unit + 114 cucumber, 0 FAIL (S-221/S-222)
----

== Session 222 terminée — PUBLICATION codex-plugin 0.0.10 TERMINÉE

*Mission pilote (Option A)* : publier la 0.0.10 pour rendre le fix
CDX-CONTEXT-HARDENING (S-221) disponible aux consommateurs.

*Ordre DAG MEMPHIS-first* exécuté en baby-step TDD (garde RED → bump GREEN) :
. MEMPHIS : `PlatformCatalogAlignmentTest` épinglé 0.0.47 (RED) →
   toml `workspace-bom = "0.0.47"` + `codex-plugin = "0.0.10"` (GREEN) →
   mavenLocal VERT → Central bundle `48b061ce-20c5-4fd0-92d3-8a7508b21c88`.
. codex : `CodexPluginPublicationTest` 2/3 RED → toml local 0.0.10 + pin
   settings `workspace-catalog:0.0.47` + pin platform `workspace-bom:0.0.47`
   (GREEN 3/3) → `properties` version 0.0.10 → `build -x test` VERT →
   Central bundle `3a1b95e9-e4a4-48a0-9dc2-ea1413e7ff55`.
. Non-régression : `check` VERT 1164 tests 0 FAIL (baseline préservée).
. Propagation : BOM + catalog 0.0.47 repo1 HTTP 200 ; codex 0.0.10 en
   polling (7-27 min).

Rapport : `.agents/sessions/222-publication-codex-0010.adoc`.

== Prochaine session — 223 : objectifs (à trancher par le pilote)

=== Option A — Page-provenance (D6 cadrage S-219)

* Join chunk→page du `OcrQualityReport` — nécessite une provenance de page
  dans les chunks. EPIC futur distinct, hors scope DOUBT-BRIDGE.
* Valeur : traçabilité du doute OCR jusqu'à la page source d'origine.

=== Option B — Dogfooding enrichi (canal Graphify réel)

* Rejouer `enrichJsonLdd` (S-217, 115/115 sections) → `generateCompositeContext`
  avec le canal Graphify peuplé, mesurer le `composite-context.json` complet
  (EAGER/RAG/GRAPHIFY/DOCS). Consommateur transitoire `/tmp` (Règle 2),
  pgvector jetable (docker).

=== Option C — Pollution TOC corpus FPA (99 chunks dot-leaders)

* Cause racine amont OCR/scan, pas le chunking codex (documenté S-216).
* Nécessite probablement une action côté acquisition (codex) ou amont.

=== Option D — Vérifier propagation repo1 0.0.10

* Confirmer `https://repo1.maven.org/maven2/education/cccp/codex-plugin/0.0.10/`
  HTTP 200 + aligner d'éventuels consommateurs si besoin.

== Hors scope (documenté)

* Page-provenance (D6 cadrage S-219) : EPIC futur distinct.
* Pollution TOC du corpus FPA : cause racine amont OCR/scan.

== Méthode

Baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR, 3 niveaux (unit →
fonctionnel TestKit → Cucumber), English only.

== Règles

- COMMITS/PUSH — autorisés (décision pilote 2026-09-17).
- PAS DE publishToMavenLocal / publication Central sans permission explicite.
- SECRETS via configuration/ — jamais en dur dans build.gradle.kts.

== References

- `.agents/INDEX.adoc` (à jour S-222)
- `.agents/BACKLOG.adoc` (PUB-3.14 ✅ TERMINÉ)
- `.agents/sessions/222-publication-codex-0010.adoc`
- `workspace-bom/` (0.0.47 publié, ordre DAG MEMPHIS-first)
