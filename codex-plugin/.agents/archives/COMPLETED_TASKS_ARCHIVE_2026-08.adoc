= COMPLETED_TASKS_ARCHIVE — Août 2026 — codex-gradle

== Session 085 (2026-08-20) — CDX-3-3 Cucumber BDD ontology derivation + clôture EPIC CDX-3

=== US CDX-3-3 — Cucumber BDD codex_derive_ontology.feature + runner dédié (TERMINÉ)

* *Valeur* : Clôture EPIC CDX-3 (4/4 US). Validation BDD end-to-end des
  3 vues archétypales de l'ontologie dérivée (bounded contexts, aggregates,
  value objects) via `DeriveOntologyTask` + `ProjectBuilder`.
* *Implémentation* :
  - `codex_derive_ontology.feature` (nouveau) — 3 scénarios
    `@ontology @derivation` (bounded context nommé depuis LDD title,
    aggregates depuis ON DELETE CASCADE, value objects = tables sans FK
    sortantes). Background partagé SQL DDL + LDD title.
  - `CodexOntologyCucumberRunner.kt` (nouveau) — pattern S-082
    (`@Suite` + `@SelectClasspathResource` + glue `codex.bdd` + filter
    `@ontology and @derivation`). Mirror `CodexLicenceCucumberRunner` (S-083).
  - `OntologySteps.kt` (nouveau) — 8 step defs (3 Given, 1 When, 4 Then)
    + helper `parseMapping`. Drive `DeriveOntologyTask` via `ProjectBuilder`
    (in-process, zéro I/O réseau). SQL DDL synthétique identique à
    `DeriveOntologyTaskTest` (3 tables book/documents/paragraphs, 2 CASCADE FK).
  - `build.gradle.kts` (modifié) — tâche `cucumberTestOntology` (pattern
    S-082 inline, +25 lignes). `excludeEngines("junit-jupiter")`,
    `includeTestsMatching("codex.bdd.CodexOntologyCucumberRunner")`,
    `systemProperty("cucumber.features", ...)` + `systemProperty("cucumber.filter.tags", ...)`.
* *Tests* : 337 → 370 (+6 cucumber licence + +6 cucumber ontology + variations
  découverte JUnit, 0 FAIL). `check` VERT, Kover VERT, validatePlugins VERT.
* *Zéro bump BOM, zéro secret, zéro régression, backward compat total.*
* *Pure BDD* — zéro production modifié.
* *3 fichiers test créés + 1 fichier build modifié.*
* *EPIC CDX-3 TERMINÉ 4/4 US* (CDX-3-0 ✅ S-077, CDX-3-1 ✅ S-079,
  CDX-3-2 ✅ S-084, CDX-3-3 ✅ S-085). Domaine `codex.ontology` (12e) clos.
* *Baby-step BDD strict RED (6 PendingException) → GREEN (3/3 PASSED) →
  REFACTOR (`check` VERT).*

'''

== Session 083 (2026-08-19) — CDX-5-3 Cucumber BDD licence routing + clôture EPIC CDX-5

=== US CDX-5-3 — Cucumber BDD codex_export_routed.feature + runner dédié (TERMINÉ)

* `codex_export_routed.feature` (nouveau, 3 scénarios `@licence @routing`)
  — Background chunks + baseDir, 3 PDFs routing end-to-end (Apache→OSS,
  Copyright→office, Unknown→OSS fallback).
* `CodexLicenceCucumberRunner.kt` (nouveau, runner dédié pattern S-082,
  1re application codex) — `@Suite` + `@SelectClasspathResource` +
  glue `codex.bdd` + filter `@licence and @routing and not @wip and not
  @integration`.
* `LicenceRoutingSteps.kt` (nouveau, 6 step defs) — Given chunks file +
  baseDir, Given PDF text, When task runs + fallback, Then exported
  under, And contains file. Réutilise PDFBox + ProjectBuilder (mirror
  `DeployKnowledgeBaseRoutedTaskTest`).
* Tâche Gradle `cucumberTestLicence` (nouvelle, Test dédiée) —
  excludeEngines junit-jupiter, filter runnerClass, override
  `cucumber.features` via systemProperty (contourne le
  `junit-platform.properties` global qui override les
  `@SelectClasspathResource`).
* Catalogue `libs.versions.toml` — +1 version `junit-platform =
  "1.14.3"` + +1 library `junit-platform-suite`.
* Build `build.gradle.kts` — +1 dependency
  `testImplementation(libs.junit.platform.suite)` +
  cucumber-junit-platform-engine `testRuntimeOnly` → `testImplementation`
  (accès `Constants.*` depuis le runner) + +1 tâche `cucumberTestLicence`.
* 333 tests 0 FAIL (baseline 306 → +27 : 6 cucumber dédiés × 2
  découvertes runner). `check` VERT, Kover VERT, validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total.
  Pure BDD — zéro production modifié. Baby-step BDD strict RED → GREEN
  → REFACTOR.
* EPIC CDX-5 TERMINÉ 4/4 US (CDX-5-0 ✅ S-077, CDX-5-1 ✅ S-081,
  CDX-5-2 ✅ S-082, CDX-5-3 ✅ S-083). Domaine `codex.licence` (14e) clos.

== Session 082 (2026-08-19) — CDX-5-2 Tâche deployKnowledgeBaseRouted + wiring

=== US CDX-5-2 — DeployKnowledgeBaseRoutedTask + wiring CodexPlugin (TERMINÉ)

* `DeployKnowledgeBaseRoutedTask` (nouveau, 69 lignes, codex.tasks) étend
  `ExportKnowledgeBaseTask` : détecte licence PDF via `PdfLicenseDetector`
  → route via `LicenseRouter` → rebind `outputDir` → délègue à
  `super.export()` (multi-format JSON-LD/Markdown/AsciiDoc inchangé).
  3 props Gradle : `pdfFile` (`@InputFile`), `baseDir`
  (`@OutputDirectory`), `fallbackZone` (`@Input @Optional`).
  `@DisableCachingByDefault` (validatePlugins).
* `ExportKnowledgeBaseTask.export()` rendu `open` (1 mot-clé) —
  backward compat total (comportement inchangé pour consommateurs).
* Wiring `CodexPlugin` : task `deployKnowledgeBaseRouted` groupe
  `deploy`, `fallbackZone.convention(zone)` (apply-time, cohérent avec
  `extension.zone.convention`).
* `DeployKnowledgeBaseRoutedTaskTest.kt` (nouveau, 5 functional) —
  Apache→OSS, Copyright→office, Unknown+OSS fallback, Unknown+CSS
  fallback, CC-BY→OSS (PDFs synthétiques PDFBox, ProjectBuilder).
* `CodexPluginTest.kt` (extension, +1 wiring) — task registered, type,
  group, description contains "licence".
* Tests : 300 → 306 (+5 functional + 1 wiring, 0 FAIL). `check` VERT,
  Kover VERT, validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total
  (`ExportKnowledgeBaseTask`/`LicenseZoneDetector`/`CodexExtension`
  inchangés sauf 1 mot-clé `open`).
* 1 fichier prod créé + 1 fichier prod modifié (1 mot-clé) + 1 wiring
  plugin + 1 fichier test créé + 1 test étendu.
* EPIC CDX-5 3/4 US (CDX-5-0 ✅, CDX-5-1 ✅, CDX-5-2 ✅, CDX-5-3 🟡 À FAIRE).
* Baby-step TDD strict RED → GREEN → REFACTOR.

'''
== Session 081 (2026-08-19) — CDX-5-1 Domaine codex.licence + PdfLicenseDetector + LicenseRouter

=== US CDX-5-1 — Domaine codex.licence (14e) + PdfLicenseDetector + LicenseRouter (TERMINÉ)

* `PdfLicenseDetector` (object pur, codex.licence) — scanne contenu PDF :
  5 patterns OSS (`apache license 2.0`/`apache-2.0`/`creative commons`/
  `cc-by`/`cc0`) + 3 patterns CSS (`©`/`copyright`/`all rights reserved`),
  precedence OSS > CSS > UNKNOWN, case-insensitive. `detect(text)` pure +
  `detect(pdfFile)` via `Loader.loadPDF` + `PDFTextStripper`. Dégradé
  silencieux UNKNOWN sur erreur. Additif à `LicenseZoneDetector` path-based.
* `LicenseRouter` (object pur, codex.licence) — `route(zone, baseDir): File`
  (OSS→`OSS/`, CSS/UNKNOWN→`office/` closed by default, ne crée pas de dirs).
* `PdfLicenseDetectorTest.kt` (nouveau, 12 unit) — apache 2.0 full
  phrase, SPDX apache-2.0, creative commons, CC-BY, CC0, © sign,
  copyright word, all rights reserved, no mention UNKNOWN, blank UNKNOWN,
  apache precedence over copyright, case-insensitive.
* `LicenseRouterTest.kt` (nouveau, 4 unit) — OSS→OSS/, CSS→office/,
  UNKNOWN→office/ precaution, no dir creation.
* Tests : 284 → 300 (+16 unit, 0 FAIL). `check` VERT, Kover VERT,
  validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total
  (`LicenseZoneDetector`/`CodexPlugin`/`CodexPipelineTask`/
  `CodexExtension` inchangés).
* 2 fichiers prod créés + 2 fichiers test créés (zéro fichier existant modifié).
* EPIC CDX-5 2/4 US (CDX-5-0 ✅, CDX-5-1 ✅, CDX-5-2/3 🟡 À FAIRE).
* Domaines purs P1 TERMINÉS (CDX-3-1/4-1/5-1 tous ✅).
* Baby-step TDD strict RED → GREEN → REFACTOR.

'''
== Session 073 (2026-08-19) — EPIC CDX-CR3 + US-CDX-13-1/2

=== EPIC CDX-CR3 — Revue CR-GLOBALE-003 (3/3 US TERMINÉ)

* CDX-CR3-1 (🔴 P0) — SQL injection CodexIngestTask.kt:118
  - IngestStatements objet pur (codex.store), templates SQL paramétrés
  - updateEmbedding : vecteur inliné safe (ONNX) + chunkId inliné (Long RETURNING id)
  - R2DBC/pgvector limitation binding $1::vector contexte SET documentée
  - 10 unit tests (IngestStatementsTest)

* CDX-CR3-2 (🔴 P0) — CodexCompositeContextTask hardcode localhost:5432
  - 5 propriétés pgHost/pgPort/pgDatabase/pgUser/pgPassword (@Input)
  - Conventions backward compat (localhost:5432/codex/codex/codex)
  - CodexVectorStore instancié avec valeurs extension
  - Plugin wiring depuis CodexExtension
  - 7 unit tests (CodexCompositeContextTaskTest)

* CDX-CR3-3 (🟠 P1) — chunk_index incorrect pour contenu dupliqué
  - Compteur local par document (docChunks.withIndex()) au lieu de chunks.indexOf(chunk)
  - IngestIndexing objet pur (codex.store)
  - 6 unit tests (ChunkIndexAssignmentTest)

=== US-CDX-13-1 — Persistance OcrResult par page (cache disque hash image)

* Loi de l'Économie d'Encre (Regle 5 AGENT.adoc)
* OcrResultCache (codex.ocr) — objet pur, cache par hash SHA-256
* DiskCacheStorage — implémentation disque (JSON)
* CollectOcrTask — propriété cacheDir optionnelle, cache hit → skip LLM
* Dégradé silencieux : erreur stockage → miss
* 13 unit tests (OcrResultCacheTest)

=== US-CDX-13-2 — Tests intégration réelle Ollama (assumeTrue)

* OllamaProbe (codex.ocr) — sonde /api/tags, plage 11437-11465
* LlmOcrEngineIntegrationTest — 5 tests @Tag("integration")
* assumeTrue(ollamaReady()) pour skip propre hors CI/local
* Résultat local : 5/5 PASSED (port 11437, gpt-oss:120b-cloud)

=== Métriques

- Tests : 167 → 204 (+37)
- Failures : 0
- check : BUILD SUCCESSFUL
- BOM : zéro bump
- Secrets : zéro
- Régression : zéro

== Session 074 (2026-08-19) — PUB-3.09 Publication Maven Central 0.0.3

=== PUB-3.09 — codex-plugin:0.0.3 sur Maven Central

* Bundle `3b4d1915-bf5a-456b-8b6b-a34134ac408b` (AUTOMATIC, propagé ~60s)
* Bump version catalogue `codex-plugin = "0.0.2"` → `"0.0.3"`
* Commande : `./gradlew publishAggregationToCentralPortal --no-daemon`
* Vérifié : https://central.sonatype.com/artifact/education.cccp/codex-plugin/0.0.3
* Contenu depuis 0.0.2 : S-072 OCR Pipeline + S-073 EPIC CDX-CR3 + cache OCR +
  tests intégration Ollama

=== PUB-2.2 — ✅ CADUQUE

* Bloc relocation (commentaire + DSL `findProperty("relocationGroup")`)
  supprimé en S-071 (CNV-6 migration conventions plugins). Plus de cible.
* Marqué CADUQUE plutôt que TERMINÉ (obsolescence de spécification).

=== Backlog cleanup

* Section EPIC CDX-CR3 dupliquée en fin de BACKLOG supprimée
  (la section d'en-tête est la source de vérité unique)

=== Métriques

- Tests : 204 (baseline préservée S-073)
- Failures : 0
- Code production modifié : zéro (session gouvernance + publication)
- BOM : zéro bump (workspace-bom 0.0.21 inchangé)
- Secrets : zéro
- Régression : zéro
'''
== Session 075 (2026-08-19) — MEMPHIS rattrapage + CDX-13-3 N2↔N2 bridge

=== A. MEMPHIS rattrapage gouvernance (workspace-bom)

* 4 versions BOM rattrapées (0.0.19→0.0.22) dans INDEX/SESSIONS_HISTORY
* Bug désynchronisation catalogue `libs.versions.toml` corrigé
  (`workspace-bom = "0.0.20"` → `"0.0.22"`)
* BACKLOG.adoc PUB-3.01 mis à jour (dernière version 0.0.22)
* Zéro code de production modifié (catalogue = metadata)

=== B. CDX-13-3 — Contrat N2↔N2 pages individuelles (TERMINÉ)

* Split-brain de contrat découvert et résolu : `CollectOcrTask` produisait
  un seul fichier concaténé, mais `BookAssembler` (document-gradle) attend
  un répertoire de fichiers `NNN-<pageId>.adoc` (contrat PageOrder)
* `outputDir` (DirectoryProperty, sortie primaire) — une page par fichier
  `NNN-<pageId>.adoc` (zero-padded 3-digit prefix, aligné PageOrder.fromFileName)
* `outputFile` (legacy optionnel, concaténé) — backward compat préservée
* Plugin wiring default `outputDir = build/codex/ocr-pages`
* 4 unit tests `CollectOcrPagesContractTest` + 3 scénarios Cucumber `@n2-bridge`

=== Métriques

- Tests : 211 unit + 186 cucumber = 397 (baseline 204 unit → +7 unit, +18 total)
- Failures : 0
- Code production modifié : 2 fichiers (CollectOcrTask.kt, CodexPlugin.kt)
- BOM : zéro bump (workspace-bom 0.0.22 inchangé)
- Secrets : zéro
- Régression : zéro (legacy outputFile backward compat)

'''
== Session 077 (2026-08-19) — Cadrage 5 EPICs P1 + BOM bump 0.0.23 + sync AGENTS racine

=== A. Actions reportées S-076

* Bump BOM 0.0.22 → 0.0.23 dans codex-plugin/build.gradle.kts:30 (propagation Central effective). Build + tests verts (211/211 PASS).
* Sync AGENTS.adoc racine ligne 480 : "CDX-15 ✅ CADUQUE (jamais cadré, résidu résolu S-076)".
* Entrée Brooklyn désynchronisée (s'arrêtait S-072) alignée sur état réel (S-073→S-076 : CDX-CR3, PUB-3.09, CDX-13-3, CDX-RC-04, pub 0.0.4).

=== B. Cadrage 5 EPICs P1 « Enrichissement Croisé du Corpus » (gouvernance pure, zéro code, pattern S-057/S-063)

* Document `CADRAGE_EPIC_P1_CORPUS_ENRICHMENT.adoc` créé (12 constats, 13 décisions).
* CDX-3 `deriveOntology` (P1-19) — domaine `codex.ontology` 12e. 3 US (CDX-3-0 ✅, CDX-3-1/2/3 🟡).
* CDX-4 `enrichJsonLdd` (P1-20) — domaine `codex.enrichment` 13e. 4 US (CDX-4-0 ✅, CDX-4-1/2/3/4 🟡).
* CDX-5 `exportKnowledgeBase` routing (P1-21) — domaine `codex.licence` 14e. 3 US (CDX-5-0 ✅, CDX-5-1/2/3 🟡).
* CDX-6 OSS/CSS routing (P1-22) — DSL `licenceRouting`. 3 US (CDX-6-0 ✅, CDX-6-1/2/3 🟡).
* CDX-7 tests (P1-23) — FontStyleDetector/TypographicAnalyzer/SemanticChunker/E2E PDF. 4 US (CDX-7-0 ✅, CDX-7-1/2/3/4 🟡).
* PLAN_GLOBAL.adoc 5 EPICs P1 ✅ Cadrés S-077.
* BACKLOG.adoc 5 EPICs P1 ajoutés avec US détaillées.

=== Métriques

- Tests : 211 (baseline préservée — zéro code prod modifié)
- Failures : 0
- Code production modifié : 0 (gouvernance pure)
- BOM : 0.0.22 → 0.0.23 (bump consommateur, propagation Central)
- Secrets : zéro
- Régression : zéro

=== CDX-3-1 — Domaine codex.ontology + OntologyDeriver (S-079)

* Domaine `codex.ontology` (12e) créé — 2 fichiers prod purs :
  `OntologyMapping.kt` (4 data classes immutables) + `OntologyDeriver.kt`
  (object pur 169 lignes, parsing SQL DDL par regex).
* 3 requêtes archétypales : bounded contexts (BFS connectivité FK),
  aggregates (CASCADE edges par racine, self-ref exclue), value objects
  (tables sans FK sortantes). LDD title nomme le bounded context.
* `OntologyDeriverTest.kt` (nouveau, 13 unit) — couverture exhaustive.
* Tests : 255 → 268 (+13 unit, 0 FAIL). `check` VERT, Kover VERT,
  validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total.
* EPIC CDX-3 2/4 US (CDX-3-0 ✅, CDX-3-1 ✅, CDX-3-2/3 🟡 À FAIRE).
* Baby-step TDD strict RED → GREEN → REFACTOR.

== Session 080 (2026-08-19) — CDX-4-1 Domaine codex.enrichment + JsonLddEnricher

=== CDX-4-1 — Domaine codex.enrichment + JsonLddEnricher + GraphifyFileResolver

* Domaine `codex.enrichment` (13e) créé — 2 fichiers prod purs :
  `JsonLddEnricher.kt` (90 lignes) + `GraphifyFileResolver.kt` (70 lignes).
* `GraphifyResolver` `fun interface` port — `resolve(sectionTitle):
  List<String>`. Aucune dépendance compile graphify (contrat N0 par fichier).
* `EnrichedLddNode` data class composition de `LddNode` (ldd + ragChunks +
  graphifyNodes + semanticDensity + entities). Composition (pas héritage) —
  `LddNode` est `data class` non `open`, contrat stable non modifié.
* `JsonLddEnricher` object pur déterministe `enrich(lddNodes, ragChunks,
  graphifyResolver): List<EnrichedLddNode>`. Jointure par dernier segment
  `sectionPath` (case-insensitive). Densité = joined/total. Entités regex
  capitalized words (peuple `DocumentChunk.entities` placeholder constat #3).
* `GraphifyFileResolver` adapter lit `graph.json` via kotlinx.serialization
  (modèle privé mirror `GraphModel`). Dégradé silencieux (fichier
  manquant/JSON invalide/champ `nodes` absent → empty).
* `JsonLddEnricherTest.kt` (nouveau, 10 unit) — empty, chunk matching,
  other section excluded, graphify resolved, density 0.5, zero density,
  entities extracted, ldd preserved, pure/deterministic, paragraph handled.
* `GraphifyFileResolverTest.kt` (nouveau, 6 unit `@TempDir`) — empty,
  exact match, case-insensitive, no match, missing nodes field, multiple
  matches.
* Tests : 268 → 284 (+16 unit, 0 FAIL). `check` VERT, Kover VERT,
  validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total.
* 2 fichiers prod créés + 2 fichiers test créés (zéro fichier existant modifié).
* EPIC CDX-4 1/4 US (CDX-4-0 ✅, CDX-4-1 ✅, CDX-4-2/3/4 🟡 À FAIRE).
* Baby-step TDD strict RED → GREEN → REFACTOR.

=== Session 081 — CDX-5-1 Domaine codex.licence + PdfLicenseDetector + LicenseRouter (2026-08-19)

*EPIC* : CDX-5 (exportKnowledgeBase routing licence P1-21)
*US* : CDX-5-1 — Domaine `codex.licence` (14e) + `PdfLicenseDetector` +
`LicenseRouter`
*Méthode* : baby-step TDD strict (RED → GREEN → REFACTOR)

*Livrables* :
* Domaine `codex.licence` (14e domaine codex) créé — 2 fichiers prod purs
  (~110 lignes) + 2 fichiers test (16 unit).
* `PdfLicenseDetector.kt` (object pur) — `detect(text: String):
  LicenseZone` (5 patterns OSS regex case-insensitive `apache license
  2.0`/`apache-2.0`/`creative commons`/`cc-by`/`cc0` + 3 patterns CSS
  `©`/`copyright`/`all rights reserved` + precedence OSS > CSS > UNKNOWN
  + blank → UNKNOWN) + `detect(pdfFile: File): LicenseZone` (lit PDF via
  `Loader.loadPDF` + `PDFTextStripper`, délègue à `detect(text)`, dégradé
  silencieux UNKNOWN sur erreur). Additif à `LicenseZoneDetector`
  path-based (content d'abord, path en fallback).
* `LicenseRouter.kt` (object pur) — `route(zone, baseDir): File` :
  OSS→`baseDir/OSS/`, CSS/UNKNOWN→`baseDir/office/` (closed by default
  precaution). Ne crée PAS de dirs (calcul pur).
* `PdfLicenseDetectorTest.kt` (nouveau, 12 unit) — apache 2.0 full
  phrase, SPDX apache-2.0, creative commons, CC-BY, CC0, © sign,
  copyright word, all rights reserved, no mention UNKNOWN, blank UNKNOWN,
  apache precedence over copyright, case-insensitive.
* `LicenseRouterTest.kt` (nouveau, 4 unit) — OSS→OSS/, CSS→office/,
  UNKNOWN→office/ precaution, no dir creation.
* Tests : 284 → 300 (+16 unit, 0 FAIL). `check` VERT, Kover VERT,
  validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total
  (`LicenseZoneDetector`/`CodexPlugin`/`CodexPipelineTask`/
  `CodexExtension` inchangés).
* 2 fichiers prod créés + 2 fichiers test créés (zéro fichier existant modifié).
* EPIC CDX-5 2/4 US (CDX-5-0 ✅, CDX-5-1 ✅, CDX-5-2/3 🟡 À FAIRE).
* Domaines purs P1 TERMINÉS (CDX-3-1/4-1/5-1 tous ✅).
* Baby-step TDD strict RED → GREEN → REFACTOR.

=== CDX-3-2 — Tâche deriveOntology + wiring CodexPlugin (session 084, 2026-08-20)

*EPIC* : CDX-3 (deriveOntology P1-19)
*US* : CDX-3-2
*Méthode* : baby-step TDD strict (RED → GREEN → REFACTOR)

* `DeriveOntologyTask.kt` (nouveau, 68 lignes) — thin wrapper Gradle
  autour de `OntologyDeriver` (objet pur S-079). `@DisableCachingByDefault`,
  3 properties : `sqlFile` + `jsonFile` (`@InputFile` PathSensitivity.RELATIVE)
  + `outputFile` (`@OutputFile`). `@TaskAction derive()` : lit SQL LMD +
  LDD JSON, appelle `OntologyDeriver.derive(lddNodes, sqlScript)`, écrit
  `OntologyMapping` JSON via kotlinx.serialization.
* `OntologyMapping.kt` (modifié) — sérialisation rétroactive : `OntologyMapping`
  + 3 sous-types (`BoundedContext`, `Aggregate`, `ValueObject`) annotés
  `@Serializable` + `import kotlinx.serialization.Serializable`. Modification
  additive (pas de nouveau champ, backward compat total).
* `CodexPlugin.kt` (modifié) — wiring : `import DeriveOntologyTask` + tâche
  `deriveOntology` groupe `transform`, `outputFile` default
  `build/codex/ontology-mapping.json`. Compteur KDoc 11 → 12 tâches.
* `DeriveOntologyTaskTest.kt` (nouveau, 143 lignes, 4 functional) —
  ProjectBuilder + SQL DDL synthétique + assertions JSON (tâche registered
  transform group, writes mapping from SQL DDL, uses LDD title for context
  name, empty SQL → empty mapping).
* Tests : 333 → 337 (+4 functional, 0 FAIL). `check` VERT, Kover VERT,
  validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total
  (`OntologyDeriver`/`ImportBookSqlTask`/`AsciiDocToJsonLddTask` inchangés).
* 2 fichiers prod créés + 1 fichier prod modifié + 1 fichier test créé.
* EPIC CDX-3 progression 2/3 US (CDX-3-0 ✅ S-077, CDX-3-1 ✅ S-079,
  CDX-3-2 ✅ S-084, CDX-3-3 🟡 À FAIRE Cucumber).
* Baby-step TDD strict RED → GREEN → REFACTOR.

== Session 086 — CDX-4-2 tâche enrichJsonLdd + wiring CodexPlugin

*Date* : 2026-08-20
*Type* : dev
*Score* : 9/10
*EPIC* : CDX-4 (enrichJsonLdd P1-20)
*US* : CDX-4-2
*Méthode* : baby-step TDD strict (RED → GREEN → REFACTOR)

* `EnrichJsonLddTask.kt` (nouveau, 80 lignes) — thin wrapper Gradle
  autour de `JsonLddEnricher` (objet pur S-080). `@DisableCachingByDefault`,
  4 properties : `jsonFile` + `chunksFile` + `graphifyFile` (`@InputFile`
  PathSensitivity.RELATIVE) + `outputFile` (`@OutputFile`). `@TaskAction
  enrich()` : lit LDD JSON (`ListSerializer(LddNode.serializer())`) +
  chunks JSON (`ListSerializer(DocumentChunk.serializer())`), instancie
  `GraphifyFileResolver(graphInput)`, appelle
  `JsonLddEnricher.enrich(lddNodes, ragChunks, resolver)`, écrit
  `List<EnrichedLddNode>` JSON via kotlinx.serialization.
* `JsonLddEnricher.kt` (modifié) — sérialisation rétroactive :
  `EnrichedLddNode` annoté `@Serializable` (+1 import). Modification
  additive (pas de nouveau champ, backward compat total).
* `CodexPlugin.kt` (modifié) — wiring : `import EnrichJsonLddTask` + tâche
  `enrichJsonLdd` groupe `transform`, `outputFile` default
  `build/codex/enriched-ldd.json`. Compteur KDoc 12 → 13 tâches.
* `EnrichJsonLddTaskTest.kt` (nouveau, 6 functional) — ProjectBuilder +
  JSON LDD synthétique (2 sections) + chunks RAG synthétiques (2 chunks)
  + `graph.json` synthétique (2 nodes). Tests : registered transform
  group, writes enriched JSON, rag chunks attached by section title,
  graphify nodes resolved, degraded silent missing graph, density 0.5.
* `CodexPluginTest.kt` (modifié) — +1 test wiring (enrichJsonLdd task
  type + group + description).
* Tests : 370 → 377 (+7 : 6 functional + 1 wiring, 0 FAIL). `check` VERT,
  Kover VERT, validatePlugins VERT.
* Zéro bump BOM, zéro secret, zéro régression, backward compat total
  (`JsonLddEnricher`/`GraphifyFileResolver` inchangés).
* 1 fichier prod créé + 1 fichier prod modifié (2 lignes) + 1 fichier
  test créé + 1 fichier test modifié.
* EPIC CDX-4 progression 2/4 US (CDX-4-0 ✅ S-077, CDX-4-1 ✅ S-080,
  CDX-4-2 ✅ S-086, CDX-4-3/4 🟡 À FAIRE).
* Baby-step TDD strict RED → GREEN → REFACTOR.

'''

== CDX-4-3 — Câblage canal Graphify dans CodexCompositeContextTask (S-087)

*Date* : 2026-08-20
*EPIC* : CDX-4 (enrichJsonLdd P1-20)
*US* : CDX-4-3
*Statut* : ✅ TERMINÉ (S-087)
*Méthode* : baby-step TDD strict (RED → GREEN → REFACTOR)
*Score* : 9/10

*Résumé* :
`GraphifySectionBuilder` object pur (domaine `codex.enrichment`, 38
lignes, pattern `JsonLddEnricher`) construit le texte du canal Graphify
du `CompositeContext` depuis `List<EnrichedLddNode>` : filtre sections
sans nœuds Graphify (économie d'encre), une entrée par section (titre +
ids résolus), déterministe, empty list → "" (fallback backward compat).
`CodexCompositeContextTask` étendu : propriété optionnelle
`enrichedJsonFile` (`@InputFile @Optional @PathSensitive RELATIVE`) +
méthode `internal buildGraphifySection(): String` (dégradé silencieux
fichier absent/vide/JSON invalide → "", log `warn`). `execute()` consomme
`buildGraphifySection()` au lieu de `""`. Wiring `CodexPlugin` :
`enrichedJsonFile` lié à `build/codex/enriched-ldd.json` (sortie
`enrichJsonLdd`).

*Fichiers* :
- `src/main/kotlin/codex/enrichment/GraphifySectionBuilder.kt` (créé).
- `src/test/kotlin/codex/enrichment/GraphifySectionBuilderTest.kt` (créé, 7 unit).
- `src/main/kotlin/codex/tasks/CodexCompositeContextTask.kt` (modifié).
- `src/test/kotlin/codex/tasks/CodexCompositeContextGraphifyTest.kt`
  (créé, 7 functional).
- `src/test/kotlin/codex/tasks/CodexCompositeContextTaskTest.kt`
  (modifié, +2 tests).
- `src/main/kotlin/codex/CodexPlugin.kt` (modifié, +2 lignes wiring).

*Résultats* :
392 tests 0 FAIL (baseline 377 → +15). `check` VERT (BUILD SUCCESSFUL
2m 3s), Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret,
zéro régression, backward compat total.

*EPIC CDX-4 progression* : 3/4 US (CDX-4-0 ✅ S-077, CDX-4-1 ✅ S-080,
CDX-4-2 ✅ S-086, CDX-4-3 ✅ S-087, CDX-4-4 🟡 À FAIRE — Cucumber BDD).
*Baby-step TDD strict RED → GREEN → REFACTOR.

== Session 088 — CDX-4-4 Cucumber BDD JSON LDD enrichment (S-088)

*EPIC* : CDX-4 (enrichJsonLdd P1-20). *US* : CDX-4-4. *Méthode* :
baby-step BDD strict (RED → GREEN → REFACTOR). *Score* : 9/10.

*Objectif* — Cucumber BDD `codex_enrich_json_ldd.feature` 4 scénarios
`@enrichment` + runner dédié `CodexEnrichmentCucumberRunner` (pattern
S-082, 3e runner dédié codex).

*Artefacts* :
- `src/test/resources/features/codex_enrich_json_ldd.feature` (créé,
  26 lignes — 4 scénarios `@enrichment`).
- `src/test/kotlin/codex/bdd/CodexEnrichmentCucumberRunner.kt` (créé,
  37 lignes — pattern S-082 `@Suite` + `@SelectClasspathResource` +
  glue `codex.bdd` + filter `@enrichment`).
- `src/test/kotlin/codex/bdd/EnrichmentSteps.kt` (créé, 180 lignes —
  13 step defs, Background LDD + chunks + graph json, scénarios 2/3
  surchargent `graphFile` pour dégradé silencieux, scénario 4
  idempotence par exécution double).
- `build.gradle.kts` (modifié, +24 lignes — tâche
  `cucumberTestEnrichment` pattern S-082 inline).

*Résultats* :
437 tests 0 FAIL (baseline S-087 392 → +45 : +8 cucumber dédiés +37
variations découverte par le `test` task qui scanne
`src/test/resources/features/` via `junit-platform.properties`). `check`
VERT (BUILD SUCCESSFUL 2m14s), Kover VERT, validatePlugins VERT. Zéro
bump BOM, zéro secret, zéro régression, backward compat total. Pure BDD
— zéro production modifié.

*EPIC CDX-4 TERMINÉ* 4/4 US (CDX-4-0 ✅ S-077, CDX-4-1 ✅ S-080,
CDX-4-2 ✅ S-086, CDX-4-3 ✅ S-087, CDX-4-4 ✅ S-088). Domaine
`codex.enrichment` (13e) clos.
*Baby-step BDD strict RED → GREEN → REFACTOR.
