= COMPLETED_TASKS_ARCHIVE_2026-09 — codex-gradle

== S-224 (2026-09-17) — EPIC CDX-PAGE-PROVENANCE TERMINÉ 5/5 US ✅

- CDX-PAGE-PROVENANCE-1 ✅ — domaine `codex.provenance` : `TocSection` +
  `TocPageIndex` (parse TOC AsciiDoc table + index canonique titre → pages)
  + `ProvenanceTitleNormalizer` (canon S-217 + 2 raffinements OCR : référence
  numérique en tête, ponctuation/leaders en queue) + `ChunkPageProvenance`/
  `PageDoubt` + `PageProvenanceResolver` (`object` pur, `resolve` +
  `joinRate`, marche d'ancêtre deepest-first, dégradé silencieux) +
  `PageProvenanceReport` (sidecar auto-descriptif). 46 unit.
- CDX-PAGE-PROVENANCE-2 ✅ — tâche `collectPageProvenance` (thin wrapper,
  groupe `collect`) → `page-provenance.json` + wiring `CodexPlugin`. 8 FT
  `ProjectBuilder`.
- CDX-PAGE-PROVENANCE-3 ✅ — `CodexCompositeContextTask.pageProvenanceFile`
  (`@InputFiles` tolérante, pattern S-221) + `buildPageIndex()` + champ JSON
  additif `pages` par entrée (omis quand non résolu). 7 tests.
- CDX-PAGE-PROVENANCE-4 ✅ — BDD `codex_page_provenance.feature`
  `@provenance` 8 scénarios (30 steps) + runner dédié
  `CodexPageProvenanceCucumberRunner` (10e runner) +
  `cucumberTestPageProvenance`.
- CDX-PAGE-PROVENANCE-5 (mesure) ✅ — `FpaPageProvenanceJoinRateTest` :
  jointure réelle **70,8 %** (274/387 chunks, 116 sections TOC), pinnée au
  plancher 0,5.
- **Découverte majeure** : le grain D2 « dernier segment » joignait
  seulement 24,8 % du corpus FPA réel → marche d'ancêtre deepest-first +
  raffinements OCR (70,8 %). Décision tranchée par la mesure (enjeu « grain
  du sectionPath » du cadrage).
- **Déviation D6 documentée** : jointure retrieval → page par `sectionPath`
  (le `chunkId` de `RetrieveResult` est le `BIGSERIAL` pgvector `Long`, pas
  l'id SHA-256).
- Gates : `check` VERT 11m17s — **1393 tests 0 FAIL** (1263 + 130 cucumber,
  +75 vs S-223). Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro
  secret, backward compat totale.

== S-223 (2026-09-17) — Cadrage EPIC CDX-PAGE-PROVENANCE ✅

- CDX-PAGE-PROVENANCE-0 ✅ — cadrage (gouvernance pure, zéro code, pattern
  S-057/S-063). Traçabilité du doute OCR jusqu'à la page source (décision
  D6 du cadrage CDX-DOUBT-BRIDGE S-219). Doc
  `CADRAGE_EPIC_CDX_PAGE_PROVENANCE.adoc` : audit 10 constats, 9 décisions,
  3 options écartées, 5 US ~10 pts (US-0 ✅ cadrage, US-1→4 🟡 À FAIRE).
- Décision structurante : provenance = **jointure dérivée non stockée**
  (TOC × chunks × rapport d'acquisition), join par titre normalisé
  (`SectionTitleNormalizer`, canon prouvé 115/115 S-217) — zéro DDL, zéro
  re-vectorisation (Loi de l'Économie d'Encre).
- Nouveau domaine cible `codex.provenance` (US-1) + tâche
  `collectPageProvenance` (US-2) + champ N3 additif `pages` (US-3) +
  Cucumber `@provenance` (US-4, 10e runner codex).
- Option D S-223 vérifiée : propagation repo1 `codex-plugin 0.0.10`
  **confirmée** (HTTP 200).
- Baseline préservée : `check` VERT 1164 tests 0 FAIL. Zéro bump BOM,
  zéro secret.

== S-222 (2026-09-17) — Publication codex-plugin 0.0.10 + MEMPHIS 0.0.47 ✅

- PUB-3.14 ✅ — `codex-plugin:0.0.10` publié Central (bundle
  `3a1b95e9-e4a4-48a0-9dc2-ea1413e7ff55`, AUTOMATIC). Rend disponible le fix
  CDX-CONTEXT-HARDENING (S-221) : input `enrichedJsonFile` tolérant
  (`@InputFiles`) → `generateCompositeContext` standalone, fallback `""`.
- MEMPHIS-first ✅ — `PlatformCatalogAlignmentTest` 0.0.47 (RED) → toml
  `workspace-bom = "0.0.47"` + `codex-plugin = "0.0.10"` (GREEN) →
  Central bundle `48b061ce-20c5-4fd0-92d3-8a7508b21c88`.
- codex alignement ✅ — toml local 0.0.10 + pin `workspace-catalog:0.0.47`
  (settings) + pin `workspace-bom:0.0.47` (platform), garde
  `CodexPluginPublicationTest` 3/3 (2/3 RED avant bump), version dérivée
  0.0.10.
- Non-régression ✅ — `check` VERT 1164 tests (1050 unit + 114 cucumber),
  0 FAIL, Kover VERT, validatePlugins VERT.
- Propagation : BOM/catalog 0.0.47 repo1 200 ; codex 0.0.10 polling.
- Zéro secret, zéro régression. Next : S-223 à la demande du pilote.

== S-218 (2026-09-17) — OCR-QUALITY US-1/2 (codex) vraie confidence + détection acquisition ✅

- OCR-QUALITY-1 ✅ — `TesseractTsvParser` (nouveau, objet pur, 7 UT) :
  moyenne TSV word-level normalisée [0,1] ; mots blancs, confiance négative
  et lignes malformées ignorés (le doute ne doit pas être masqué).
  `TesseractOcrEngine` invoqué avec config `tsv` + `metadata["confidenceSource"]`
  (`"tsv"` mesure réelle / `"degraded"` vieux build sans TSV) — plus de
  placeholder 0.7. 3 tests (image AWT réelle `confidence > 0.5`, textless
  0.0, stub sans TSV dégradé).
- Fix régression potentielle : `OcrPipeline` ne gate plus sur
  `confidence > 0.0` — la confiance est un signal de doute aval, jamais un
  critère de fallback (sinon le texte dégradé serait jeté). 1 UT verrouillage.
- OCR-QUALITY-2 ✅ — `OcrQualityAnalyzer` (nouveau, objet pur, 11 UT) :
  ILLISIBLE / TOO_SHORT / LOW_CONFIDENCE / IMAGE_MISSING, macro `image::?`
  bloc+inline, ordre exclusive-first puis cumulatif. `OcrQualityReport`
  `@Serializable`. `CollectOcrTask.qualityReportFile` optionnel (rapport
  JSON, 4 FT) + `lowConfidenceThreshold` + `CodexExtension.ocrLowConfidenceThreshold`
  (défaut 0.5).
- BDD : `codex_ocr_quality.feature` 11 scénarios `@ocr-quality`,
  `OcrQualitySteps`, `CodexOcrQualityCucumberRunner` (8e runner codex,
  pattern S-082), tâche `cucumberTestOcrQuality`.
- Fix drift BOM MEMPHIS : pin workspace-catalog/workspace-bom 0.0.39 → 0.0.43
  (garde `CodexPluginPublicationTest` attrapée, piège #13).
- EPIC OCR-QUALITY transverse complété : US-1/2 codex S-218 + US-3 document
  S-251 + US-4 codebase S-213.
- Gates : check VERT 872 tests 0 FAIL (baseline 704 → +168), 9 suites
  cucumber dédiées VERTES, Kover VERT, validatePlugins VERT.
- Next : GBL-003.4 RAG doubt metadata (brancher le rapport d'acquisition
  sur l'ingestion codebase) OU publication 0.0.8.

== S-217 (2026-09-10) — CDX-CORPUS-2 enrichment JSON-L via graphify GBL-003.3 ✅

- CDX-CORPUS-2 ✅ — EPIC CDX-CORPUS TERMINÉ 4/4 US, GBL-003.3 DÉBLOQUÉ.
- `SectionTitleNormalizer` (nouveau, objet pur, 19 UT) — normalise titres
  LDD/chunks (entities HTML, footnote macros, HTML tags, apostrophes typo,
  dot leaders 2+, footnote markers `[n]`) → join de titres 30/111 → 110/111.
- `JsonLddEnricher` rendu récursif (champ `children` + `enrichNode`) —
  join par titre appliqué à chaque nœud (régression corrigée : run réel
  passait de 4 sections enrichies → 115/115).
- Run réel : 115/115 sections titrées enrichies (184 chunks + 521 graphify
  nodes), 468 nœuds totaux, 353 paragraphs exclus (titre vide).
  Enriched-ldd.json → `office/.../codex-out/` (PROPRIETARY, Règle 2).
- Fix drift BOM : pin workspace-catalog/workspace-bom 0.0.36/0.0.37 → 0.0.38
  (pénalité MEMPHIS S-218 — garde publication attrapée).
- Gates : check VERT 704 tests 0 FAIL (+22 : 19 normalizer + 3 récursifs),
  Kover VERT, validatePlugins VERT, publishToMavenLocal 0.0.6 frais.
- Next : OCR-QUALITY-1/2 codex (critical path P0, cadrage S-215 prêt,
  18-19 chunks `[ILLISIBLE]` candidats doubt GBL-003.4).

== S-216 (2026-09-08) — CDX-CORPUS run acquisition corpus FPA (GBL-003.3) + fix drift BOM ✅

- CDX-CORPUS-0 ✅ — cadrage run (doc `CADRAGE_CDX_CORPUS_RUN.adoc`).
- CDX-CORPUS-1 ✅ — run : livre.adoc 7787 lignes → 387 chunks sémantiques + KB 3 formats
  dans `office/.../codex-out/` (PROPRIETARY, Règle 2 : buildDir /tmp + wipe).
  Pipeline consommé *sans modification du build script codex* (buildscript
  classpath 0.0.6 mavenLocal).
- CDX-CORPUS-3 ✅ — validation : invariants verts (ids SHA-256 387/387, 0 content vide,
  coverage headings) + échantillonnage 10 chunks (rapport `codex-out/RAPPORT_CDX_CORPUS_RUN.adoc`).
- Fix drift BOM — pin platform + settings workspace-catalog 0.0.33 → 0.0.36
  (bump MEMPHIS post-S-214 non réaligné — garde publication attrapée).
- Gates : check VERT 682 tests 0 FAIL, zéro code prod (2 lignes pin), zéro secret.
- CDX-CORPUS-2 ⏳ reporté (graphe GF-FPA pas encore produit).
- Next : OCR-QUALITY-1/2 codex (critical path P0, cadrage S-215 prêt).
== S-217 (2026-09-10) — CDX-CORPUS US-2 enrichment récursif via graphify (GBL-003.3) ✅

- `SectionTitleNormalizer` (nouveau, objet pur, 19 UT) — normalise les titres
  LDD/chunks (entities HTML, footnote macros, tags HTML, apostrophes typo,
  dot leaders 2+, footnote markers) → join titres 30/111 → 110/111.
- `JsonLddEnricher.enrich` rendu récursif (champ `children` + `enrichNode`)
  — join par titre appliqué à chaque nœud.
- Run réel : 115/115 sections titrées enrichies (184 chunks + 521 graphify
  nodes), 468 nœuds totaux. Output PROPRIETARY dans office/ (Règle 2).
- Fix drift BOM 0.0.36/0.0.37 → 0.0.38 (MEMPHIS S-218, garde attrapée).
- Gates : check VERT 704 tests 0 FAIL (+22), publishToMavenLocal 0.0.6.

== S-218 (2026-09-17) — OCR-QUALITY US-1/2 codex ✅

- US-1 vraie confidence Tesseract : `TesseractTsvParser` (7 UT, moyenne TSV
  word-level normalisée, mots blancs/confiance négative/malformés ignorés) +
  config `tsv` + `confidenceSource` tsv/degraded + fix gate `OcrPipeline`
  (la confiance est un signal aval, pas un critère de fallback).
- US-2 détection images problématiques : `OcrQualityAnalyzer` (11 UT,
  ILLISIBLE/TOO_SHORT/LOW_CONFIDENCE/IMAGE_MISSING, macro `image::?` bloc +
  inline) + `CollectOcrTask.qualityReportFile` JSON + `ocrLowConfidenceThreshold`.
- BDD `codex_ocr_quality.feature` 11 scénarios + 8e runner dédié.
- EPIC transverse OCR-QUALITY complété 4/4 US (codex S-218 + document S-251
  + codebase S-213).
- Fix drift BOM 0.0.39 → 0.0.43 cross-borough MEMPHIS (piège #13).
- Gates : check VERT 872 tests 0 FAIL (baseline 704 → +168), 9 suites Cucumber.

== S-219 (2026-09-17) — CDX-DOUBT-BRIDGE ✅ TERMINÉ 3/3 US

- US-0 cadrage (5 constats, 7 décisions, doc `CADRAGE_EPIC_CDX_DOUBT_BRIDGE.adoc`).
- US-1 `CodexIngestTask` doubt-aware : `ingestInto` délègue à `DoubtPolicy.mark`
  + `RagVectorStore.ingestWithDoubt` ; boucle R2DBC dupliquée supprimée
  (thin wrapper 135→111 lignes). 4 UT + IT testcontainers PASSED.
- US-2 `CodexCompositeContextTask` doubt-aware : `searchWithDoubt` +
  `DoubtExposure` canal Docs + `excludeDoubtfulDocs` (défaut false) + entrées
  JSON `confidence`/`doubtful` additifs. 4 UT + 3 FT DSL.
- BDD `codex_doubt_bridge.feature` 5 scénarios + 9e runner dédié.
- GBL-003.4 codex complété — la tâche n'est plus un contournement.
- Gates : check VERT 987 tests 0 FAIL (baseline 872 → +115), 9 suites Cucumber.
- Zéro bump BOM, zéro secret, backward compat totale.

== S-220 (2026-09-17) — Maintenance + publication 0.0.9 + dogfooding

- C — Drift MEMPHIS réaligné : garde `CodexPluginPublicationTest` RED à HEAD
  (pin codex 0.0.43 vs MEMPHIS 0.0.44 S-216) → pin settings/platform 0.0.43
  → 0.0.44, garde 3/3 VERTE. Marqueur CADRAGE DOUBT-BRIDGE US-1/2 corrigé
  (`🟡 À FAIRE` → `✅ TERMINÉ (S-219)`).
- A — Deux releases Central : 0.0.8 (MEMPHIS 0.0.45 `d199bf00`, codex
  `b573cf61`) puis 0.0.9 corrective (MEMPHIS 0.0.46 `d1c09b5e`, codex
  `7849b4b1`). Propagation repo1 HTTP 200.
- B — Dogfooding `collectIngest` : 387 chunks / 18 doubtful / 387 embeddings
  / avg 0.9535 (identique S-214). Bug P0 préexistant découvert
  `generateCompositeContext` (`Serializer for class 'Any' is not found`,
  `Map<String, Any>` non sérialisable kotlinx, depuis rename S-058) —
  corrigé TDD strict 3 UT (`CodexCompositeContextJsonTest`) + 2 BDD
  (`codex_doubt_bridge.feature`), seams `buildCompositeJson`/
  `buildVibecodingJson` via `JsonObject`.
- Gates : check VERT 1010 unit + 108 cucumber 0 FAIL, Kover VERT,
  validatePlugins VERT. Zéro secret.
- Constat non corrigé (documenté) : wiring `enrichedJsonFile`
  inconditionnel vs `@Optional` (US future).

== S-221 (2026-09-17) — EPIC CDX-CONTEXT-HARDENING TERMINÉ 2/2 US ✅

- CDX-CONTEXT-HARDENING-1 ✅ — fallback `enrichedJsonFile` standalone.
  L'`@InputFile @Optional` était wired inconditionnellement
  (`CodexPlugin.kt:219`) → `generateCompositeContext` seule échouait en
  validation Gradle (`Input file does not exist`), le fallback `""`
  documenté restait inatteignable. Correctif : input **tolérant**
  `@InputFiles` (`ConfigurableFileCollection`) — artefact ciblé par défaut,
  absence → `""` (Économie d'Encre, jamais forcer `enrichJsonLdd`).
  RED 8 UT `CodexCompositeContextHardeningTest` (14× Unresolved) → GREEN
  (tâche + plugin `setFrom`) → REFACTOR (migration des 2 suites
  historiques). Preuve par **TestKit réel** `CodexCompositeContextGradleValidationTest`
  (2 tests — sentinel `doFirst` atteint, `enrichJsonLdd` non planifié ;
  `ProjectBuilder` ne joue pas la validation d'input Gradle).
- CDX-CONTEXT-HARDENING-2 ✅ — test d'exécution `execute()` (comble le trou
  P0 S-220). Seam `@Internal storeOverride: Property<RagVectorStore>` ;
  4 tests pilotés par fake in-memory `RecordingRagStore` (JSON N3 +
  vibecoding + metadata, standalone sans enriched JSON, dégradé silencieux
  JSON invalide, canal Graphify peuplé). Audit des 16 `@TaskAction` :
  toutes couvertes (unit ou IT).
- BDD ✅ — 3 scénarios `hardened` ajoutés à `codex_doubt_bridge.feature`
  (runner dédié réutilisé — Économie d'Encre, pas de 10e runner), steps
  préfixés anti-collision S-088. 10/10 PASSED.
- Gates : `check` VERT 6m02s — 1050 unit (+40 vs baseline 1010) + 114
  cucumber dédiés (+6), 0 FAIL. Kover VERT, validatePlugins VERT. Zéro
  bump BOM, zéro secret, zéro régression, backward compat totale (canal
  Graphify par défaut préservé).
