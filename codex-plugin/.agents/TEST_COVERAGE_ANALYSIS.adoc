= TEST_COVERAGE_ANALYSIS — codex-gradle
:date: 2026-08-19
:session: 083

== Session 083 — CDX-5-3 Cucumber BDD licence routing (+6 cucumber dédiés, 333/333 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/kotlin/codex/bdd/LicenceRoutingSteps.kt` | 6 step defs (3 scénarios × 2 découvertes) | ✅ PASS (Apache→OSS, Copyright→office, Unknown→OSS fallback — PDFs synthétiques PDFBox, ProjectBuilder, délégation `super.export()`)
| `test/resources/features/codex_export_routed.feature` | 3 scénarios `@licence @routing` | ✅ PASS (Background chunks + baseDir, 3 PDFs routing end-to-end)
| `test/kotlin/codex/bdd/CodexLicenceCucumberRunner.kt` | runner dédié | ✅ PASS (pattern S-082, `@Suite` + `@SelectClasspathResource`, glue `codex.bdd`)
|===

*Nouveau testé* :
- `CodexLicenceCucumberRunner` — runner Cucumber dédié (pattern S-082,
  1re application codex). Isole `codex_export_routed.feature` du suite
  global via `@SelectClasspathResource` + filter tags.
- `LicenceRoutingSteps` — 6 step defs BDD (Given chunks file + baseDir,
  Given PDF text, When task runs + fallback, Then exported under, And
  contains file). Réutilise PDFBox + ProjectBuilder (mirror
  `DeployKnowledgeBaseRoutedTaskTest`).
- Tâche Gradle `cucumberTestLicence` — Test dédiée (excludeEngines
  junit-jupiter, filter runnerClass, override `cucumber.features`).

*Couverture* : pure BDD — zéro production modifié. Le domaine
`codex.licence` (S-081) et la tâche `DeployKnowledgeBaseRoutedTask`
(S-082) sont exercés via `ProjectBuilder`. Aucune nouvelle logique
métier.

*Non-régression* : 306 → 333 tests (+27 : 6 cucumber dédiés × 2
découvertes runner). 0 failures, 15 skipped (@integration préservés).
`check` VERT, Kover VERT, validatePlugins VERT.

== Session 082 — CDX-5-2 Tâche deployKnowledgeBaseRouted + wiring CodexPlugin (+6 tests, 306/306 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/tasks/DeployKnowledgeBaseRoutedTaskTest.kt` | 5 functional | ✅ PASS (Apache→OSS, Copyright→office, Unknown+OSS fallback, Unknown+CSS fallback, CC-BY→OSS — PDFs synthétiques PDFBox, ProjectBuilder, délégation `super.export()`)
| `test/codex/CodexPluginTest.kt` (extension) | +1 wiring | ✅ PASS (task `deployKnowledgeBaseRouted` registered, type `DeployKnowledgeBaseRoutedTask`, group `deploy`, description contains "licence")
|===

*Nouveau testé* :
- `DeployKnowledgeBaseRoutedTask` — tâche Gradle additive (étend `ExportKnowledgeBaseTask`) :
  detection `PdfLicenseDetector` → routing `LicenseRouter` → rebind `outputDir` →
  delegation `super.export()`. 3 props Gradle (`pdfFile`, `baseDir`, `fallbackZone`).
  `@DisableCachingByDefault` (validatePlugins).
- Backward compat : `ExportKnowledgeBaseTask.export()` rendu `open` — 8 tests existants préservés.

*Niveaux de tests baby-step TDD* :
- RED : 1 fichier test créé (5 functional) → compile fail (`Unresolved reference 'fallbackZone'`).
- GREEN : 2 fichiers prod créés (1 nouvelle tâche + 1 modification `open`) → 5/5 functional + 1/1 wiring PASSED.
- REFACTOR : bug `validatePlugins` (`@DisableCachingByDefault` manquante) → corrigée. `check` VERT.

*Non-régression* :
- `ExportKnowledgeBaseTaskTest` 8/8 PASS (backward compat `open`).
- `CodexPluginTest` 7/7 PASS (incl. +1 wiring, 6 existants préservés).
- `check` BUILD SUCCESSFUL in 44s (Kover VERT, validatePlugins VERT).
- 306 tests 0 FAIL (baseline 300 → +6 : 5 functional + 1 wiring).

*Zéro bump BOM, zéro secret, zéro régression, backward compat total.*

'''
== Session 081 — CDX-5-1 Domaine codex.licence + PdfLicenseDetector + LicenseRouter (+16 unit, 300/300 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/licence/PdfLicenseDetectorTest.kt` | 12 JUnit5 | ✅ PASS (object pur — apache 2.0 full, SPDX apache-2.0, creative commons, CC-BY, CC0, © sign, copyright word, all rights reserved, no mention UNKNOWN, blank UNKNOWN, apache precedence over copyright, case-insensitive)
| `test/codex/licence/LicenseRouterTest.kt` | 4 JUnit5 | ✅ PASS (object pur — OSS→OSS/, CSS→office/, UNKNOWN→office/ precaution, no dir creation)
|===

*Baseline* : 284 tests (S-080) → *300 tests* (S-081), +16 unit, 0 FAIL.

== Session 080 — CDX-4-1 Domaine codex.enrichment + JsonLddEnricher + GraphifyFileResolver (+16 unit, 284/284 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/enrichment/JsonLddEnricherTest.kt` | 10 JUnit5 | ✅ PASS (object pur — empty, chunk matching, other section excluded, graphify resolved, density 0.5, zero density, entities extracted, ldd preserved, pure/deterministic, paragraph handled)
| `test/codex/enrichment/GraphifyFileResolverTest.kt` | 6 JUnit5 | ✅ PASS (adapter — empty, exact match, case-insensitive, no match, missing nodes field, multiple matches, `@TempDir`)
|===

*Baseline* : 268 tests (S-079) → *284 tests* (S-080), +16 unit, 0 FAIL.

== Session 079 — CDX-7-1 tests FontStyleDetector + TypographicAnalyzer (+44 unit, 255/255 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/tasks/FontStyleDetectorTest.kt` | 32 JUnit5 | ✅ PASS (characterization exhaustive objet existant)
| `test/codex/tasks/TypographicAnalyzerTest.kt` | 12 JUnit5 | ✅ PASS (objet extrait de ExtractBookStructureTask)
|===

*Baseline* : 211 tests (S-077) → *255 tests* (S-078), +44 unit, 0 FAIL.

*FontStyleDetectorTest* (32) — characterization de l'objet existant `FontStyleDetector` :
. 12 mots-clés monospace (courier, mono, consolas, typewriter, menlo, monaco,
  source code, fira code, jetbrains, droid sans mono, dejavu sans mono, liberation mono)
. Priorité monospace > bold (CourierNew-Bold → MONOSPACE)
. 5 variantes bold (bold, heavy, black, "bd " avec espace, BoldMT regex)
. 5 variantes italic (italic, oblique, slanted, "it " avec espace, ItalicMT regex)
. Combinaison BOLD_ITALIC (BoldItalic, Bold Italic, BoldOblique)
. Fallback NORMAL (Helvetica, Arial, Times-Roman, empty string)
. Case-insensitive (HELVETICA-BOLD, helvetica-italic, COURIER)
. Helpers enum `isMonospace`/`isBold`/`isItalic` pour les 5 valeurs

*TypographicAnalyzerTest* (12) — objet extrait de `ExtractBookStructureTask` :
. 4 thresholds dynamiques (large range > 4.0) : h1 = max - range×0.05, etc.
. 4 fallbacks (range < 1.0 → max×0.95, < 2.0 → max×0.75, < 3.0 → max×0.50, < 4.0 → max×0.35)
. Zero range → tous fallbacks
. Ordre h1 > h2 > h3 > h4 pour large range
. Plafond max (thresholds ≤ max)
. Range négative défensive → tous fallbacks

*Gate Kover* : VERT (préservé S-077). Kover 0.9.8 ne supporte pas offline
instrumentation — couverture plugin tests non comptée (limitation connue).

*Prochaines US de test (CDX-7)* :
. CDX-7-2 `ConvertToMarkdownTask` chaîné ~5 unit
. CDX-7-3 `SemanticChunker` extrait ~7 unit
. CDX-7-4 `CodexPipelineIntegrationTest` E2E PDF ~3 integration

'''
== Session 072 — CDX-13 OCR Pipeline (+32 tests, 167/167 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/ocr/LlmOcrEngineTest.kt` | 6 JUnit5 | ✅ PASS (fake OllamaChatClient)
| `test/codex/ocr/OcrPipelineTest.kt` | 6 JUnit5 | ✅ PASS (fallback chain)
| `test/codex/ocr/HttpOllamaChatClientTest.kt` | 5 JUnit5 | ✅ PASS (mock server JDK)
| `test/codex/tasks/CollectOcrTaskTest.kt` | 5 JUnit5 | ✅ PASS (ProjectBuilder)
| `test/resources/features/ocr-pipeline.feature` | 5 Cucumber | ✅ PASS (18 steps)
|===

Nouveaux tests détaillés :

*LlmOcrEngineTest* (6) :
. `LlmOcrEngine implements OcrEngine`
. `process returns OcrResult with structured AsciiDoc text from LLM response`
. `process returns empty result when LLM response is blank`
. `process returns empty result when client throws`
. `process uses custom prompt when provided in request`
. `process encodes image as base64 in Ollama chat payload`

*OcrPipelineTest* (6) :
. `OcrPipeline returns LLM result when LLM succeeds`
. `OcrPipeline falls back to Tesseract when LLM returns empty text`
. `OcrPipeline falls back to next engine when LLM confidence is zero`
. `OcrPipeline returns empty result when all engines fail`
. `OcrPipeline with single engine returns that engine result`
. `OcrPipeline metadata records which engine won`

*HttpOllamaChatClientTest* (5) :
. `chat sends POST to api chat endpoint and returns message content`
. `chat throws on non 2xx response`
. `chat supports legacy response field when message missing`
. `chat uses config endpoint when provided`
. `chat payload contains model and images fields`

*CollectOcrTaskTest* (5) :
. `plugin registers collectOcr task with correct type`
. `collectOcr task has correct group and description`
. `collectOcr task inherits Ollama conventions from extension`
. `collectOcr task produces empty output when no images found`
. `extension exposes ollama and ocr properties`

*ocr-pipeline.feature* (5 scénarios) :
. `LLM OCR engine produces structured AsciiDoc from image`
. `OCR pipeline falls back to Tesseract when LLM fails`
. `OCR pipeline returns empty result when all engines fail`
. `collectOcr task concatenates pages into a single AsciiDoc document`
. `collectOcr task produces empty output when no images found`

Fix : `OcrConfigTest.kt:12` — nom test contenant `:` illégal Kotlin (renommé).

== Session 067 — TesseractOcrEngineTest (5 tests JUnit5)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/ocr/TesseractOcrEngineTest.kt` | 5 JUnit5 | ✅ PASS
|===

Tests :
. `TesseractOcrEngine implements OcrEngine`
. `processes minimal image without throwing`
. `returns empty result on tesseract failure`
. `maps language codes to tesseract format`
. `returns result with sourceFormat from request`

Note : Tesseract 5.3.4 installé sur le système de test. Tests utilisent
un PNG 1x1 minimal (sans texte) — vérifient le plumbing sans crash, pas
l'extraction de texte réelle.

== Metrics Kover (après session 024 — CDX-14)

Rapport HTML : `build/reports/kover/html/index.html`

|===
| Metrique | Valeur | Objectif | Note
| Line coverage | **77.5%** (979/1263) | ≥ 85% | Stable — Kover ne capture pas les tests via ProjectBuilder (Gradle TestKit)
| Branch coverage | **60.8%** (397/653) | ≥ 75% | Stable
| Method coverage | **75.5%** (108/143) | ≥ 85% | +1% via Metadata.forBrooklyn()
| Class coverage | **68.6%** (35/51) | ≥ 85% | Stable
|===

[NOTE]
====
Les nouveaux tests JUnit5 pour CodexPlugin (8 tests), CodexExtension (4 tests), Metadata (7 tests)
et les extensions CodexVectorStore (+5 tests) utilisent ProjectBuilder ou la reflexion.
Kover ne peut pas instrumenter le bytecode charge dans un classloader Gradle TestKit separe,
ce qui explique que les metrics ne remontent pas. 105/105 tests PASS.
====

== Trous de couverture identifies

|===
| Package | Line cov | Classes critiques | Priorite | Tests ecrits
| `codex` | 26.6% | `CodexPlugin.kt`, `Metadata.kt`, `CodexExtension.kt` (0% Kover, mais testes) | 🟢 COUVERT | 8+4+7 = 19 nouveaux tests ✅
| `codex.store` | 14.3% | `CodexVectorStore.kt` | 🟡 MOYENNE | +5 tests (computeEmbedding, buildConnectionFactory, params) ✅
| `codex.tasks` | ~85% | ImportBookSqlTask, ExtractEpubStructureTask, etc. | 🟢 BASSE | Deja couverts
|===

== Couverture par Classe (JUnit5 — 105/105 PASS)

|===
| Classe | Tests | Statut
| `LicenseZoneDetectorTest` | 7 | ✅ OSS/CSS/UNKNOWN detection + Apache-2.0/PROPRIETARY mapping
| `ExtractTextTaskTest` | 3 | ✅ Simple PDF, multi-ligne, empty PDF
| `ExtractBookStructureTaskTest` | 10 | ✅ Bold/Italic/Monospace/Normal/BoldItalic detection, Headers+code block, Empty PDF
| `ExtractEpubStructureTaskTest` | 6 | ✅ EPUB synthetique, hierarchie multi-niveaux, code blocks, inline code, empty EPUB
| `AsciiDocToJsonLddTaskTest` | 4 | ✅ Simple hierarchy, Paragraphs, Empty doc, Nested children
| `ImportBookSqlTaskTest` | 4 | ✅ DDL+INSERT, Correct titles, Paragraph INSERT, Empty array
| `ConvertToMarkdownTaskTest` | 10 | ✅ Headers, Code blocks, Admonitions, Tables, Images, Links, Lists, Inline formatting
| `ChunkDocumentTaskTest` | 11 | ✅ Multi-headings, Code blocks, Deterministic IDs, Section hierarchy, Overlap, Licence
| `ExportKnowledgeBaseTaskTest` | 9 | ✅ 3 formats, JSON-L context/graph, Code blocks, Markdown hierarchy, AsciiDoc, Sorting
| `CodexRetrieveTaskTest` | 2 | ✅ Registration, Configurabilite
| `CodexVectorStoreTest` | 5 | ✅ Instanciation, Default params, RetrieveResult contract, searchBlocking, search
| `CodexIngestRetrieveIT` | 4 | ✅ End-to-end pgvector, Batching batchSize=1, Pipeline auto-detect PDF, Empty chunks
|===

== Cucumber (BDD)

✅ IMPLANTE Session 024 (CDX-14) :

|===
| # | Feature | Scenarios | Statut
| 1 | extract-chunk-pipeline.feature | 1 | ✅ Ecrit + steps implementees
| 2 | chunk-ingest-pipeline.feature | 1 | ✅ Ecrit + steps implementees
| 3 | ingest-retrieve-pipeline.feature | 2 | ✅ Ecrit + steps implementees
| 4 | pipeline-auto-detect.feature | 1 | ✅ Ecrit + steps implementees
| 5 | license-zone-detection.feature | 2 | ✅ Ecrit + steps implementees
| **Total** | **5 features** | **7 scenarios** | ✅
|===

== Infrastructure de test

- JUnit5 + TestContainers 1.21.4 + docker-java 3.7.0
- pgvector/pgvector:pg16 pour tests end-to-end
- Kover pour metrics de couverture (HTML + XML)
- slf4j 2.0.17 + logback 1.5.32

== Dette de couverture — Session 027 (OCR-0)

*Date* : 2026-05-30

|===
| Classe | Tests | Statut
| `codex.ocr.OcrRequest` | 0 | 🔴 Aucun test — session 028
| `codex.ocr.OcrResult` | 0 | 🔴 Aucun test — session 028
| `codex.ocr.OcrConfig` | 0 | 🔴 Aucun test — session 028
|===

3 regressions preexistantes a investiguer :
- `ExtractBookStructureTaskTest` : 1 echec (single line monospace devient inline code)
- `ExtractEpubStructureTaskTest` : 2 echecs (empty EPUB + note header)

== Session 029 — Publication Maven Central (aucun test modifie)

Aucun test modifié. Publication ops uniquement (version, nmcp, POM).

== Session 030 — Fix plugin ID + buildscript Gradle 9.x (aucun test OCR ajoute)

Aucun test OCR ajouté. Infrastructure build uniquement :
- Plugin ID synchronisé : `education.cccp.codex` dans build.gradle.kts et tests
- `plugins {}` déplacé avant `buildscript {}` (Gradle 9.5.1)
- Tests toujours 135/135 PASS

== Prochaine session (031)

- CDX-15 : TDD retroactif OcrRequest/OcrResult/OcrConfig
- Diagnostic des 3 regressions
- publishToMavenLocal (si autorise)
- CDX-13 : Suspension REST maintenue

== Session 073 — EPIC CDX-CR3 + US-CDX-13-1/2 (+37 tests, 204/204 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/codex/store/IngestStatementsTest.kt` | 10 JUnit5 | ✅ PASS (templates SQL paramétrés)
| `test/codex/store/ChunkIndexAssignmentTest.kt` | 6 JUnit5 | ✅ PASS (index locaux par document)
| `test/codex/tasks/CodexCompositeContextTaskTest.kt` | 7 JUnit5 | ✅ PASS (propriétés pg + defaults)
| `test/codex/ocr/OcrResultCacheTest.kt` | 13 JUnit5 | ✅ PASS (cache hit/miss, hash, dégradé)
| `test/codex/ocr/LlmOcrEngineIntegrationTest.kt` | 5 integration | ✅ PASS (vrai Ollama port 11437)
|===

*Nouveaux domaines testés* :
- `codex.store` — IngestStatements (templates SQL), IngestIndexing (index locaux)
- `codex.ocr` — OcrResultCache (cache hash), OllamaProbe (sonde disponibilité)

*Tests d'intégration* : 5 tests @Tag("integration") avec assumeTrue(ollamaReady())
— skip propre hors CI/local sans Ollama. En local (port 11437, gpt-oss:120b-cloud),
5/5 PASSED avec vrai contrat HTTP.

*Non-régression* : CodexIngestRetrieveIT 4/4 PASS (Testcontainers pgvector),
Cucumber OCR pipeline 5/5 PASS, tous tests existants préservés.
