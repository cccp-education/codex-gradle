= TEST_COVERAGE_ANALYSIS — codex-gradle
:date: 2026-08-24
:session: 104

== Session 104 — EPIC CDX-OCR-CONTRACTS (679/679 PASS — baseline préservée, typealiases transparents)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `workspace-bom/ocr-contracts/src/test/kotlin/contracts/ocr/OcrEngineTest.kt` | 3 unit | ✅ PASS (fun interface lambda, process forwarding, throwing engine)
| `workspace-bom/ocr-contracts/src/test/kotlin/contracts/ocr/OcrRequestTest.kt` | 5 unit | ✅ PASS (4 champs, prompt custom, equals contenu imageData, hashCode cohérent, toString sans raw bytes)
| `workspace-bom/ocr-contracts/src/test/kotlin/contracts/ocr/OcrResultTest.kt` | 6 unit | ✅ PASS (7 champs, metadata default, factory `of` defaults + metadata, equals data class, equals diffère sur metadata)
| `codex-plugin/src/test/kotlin/codex/ocr/OcrRequestTest.kt` | tests migrés | ✅ PASS — helpers companion → top-level functions (`writeOcrRequestToJsonFile`/`readOcrRequestFromJsonFile`)
| `codex-plugin/src/test/kotlin/codex/ocr/OcrResultTest.kt` | tests migrés | ✅ PASS — idem (`writeOcrResultToJsonFile`/`readOcrResultFromJsonFile`) + roundtrip preserved
| `codex-plugin/src/test/kotlin/codex/bdd/*` (7 cucumber dédiés) | scénarios inchangés | ✅ PASSED — typealiases transparents, zéro modification des steps/features
|===

*Point clé* : le passage par typealiases est *transparent* pour la suite de
tests existante — zéro test modifié hors les 2 fichiers consommant les
helpers Jackson déplacés. La baseline 679 tests 0 FAIL est préservée à
l'identique (S-103 = S-104). Côté codebase (Queens) : adapter 4/4 +
boundary 2/2 + cucumber @ocr-contracts 5/5 PASSED après bascule imports.

== Session 094 — EPIC CDX-RC-04 Pont RAG SessionMemoryContract (+126 tests, 661/661 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/kotlin/codex/profile/ProfileConfigTest.kt` | 2 unit | ✅ PASS (defaults localhost:5432/codex/codex/codex, custom values)
| `test/kotlin/codex/profile/ProfileStatementsTest.kt` | 9 unit | ✅ PASS (DDL table dédiée + composite PK + embedding 384, upsert ON CONFLICT + cast `$3::jsonb`, select par clé, updateEmbedding inline safe, bind counts, interpolation check)
| `test/kotlin/codex/profile/ProfileEmbeddingTest.kt` | 8 unit | ✅ PASS (empty → empty, weakPoints, annotations values, concat, completedModules excluded, numerics excluded, currentModule excluded, deterministic)
| `test/kotlin/codex/profile/PgVectorSessionMemoryIT.kt` | 5 functional `@Tag("integration")` | ✅ 5/5 PASSED (Testcontainers pgvector/pgvector:pg16 — round-trip complet, upsert overwrite, null clé inconnue, clé composite même learner ≠ formations = profils distincts, empty profile). Valide les 3 fixes R2DBC : bindNull nullables, Result consumption `.rowsUpdated.awaitFirst()`, JSONB cast `$3::jsonb`
| `test/kotlin/codex/profile/PersistLearnerProfileTaskIT.kt` | 2 functional `@Tag("integration")` | ✅ 2/2 PASSED (persistance profil JSON → pgvector + reload via load(), registration + group codex-memory dans CodexPlugin)
| `test/kotlin/codex/bdd/SessionMemorySteps.kt` + `FakeSessionMemory` | 24 step defs | ✅ Pure BDD in-memory map implémente SessionMemoryContract — pas de DB. Préfixes "session memory"/"profile" (bug S-088 — évite DuplicateStepDefinition)
| `test/resources/features/codex_session_memory.feature` | 10 scénarios `@session-memory` | ✅ 10/10 PASSED via `CodexSessionMemoryCucumberRunner` (pattern S-082, 6e runner codex) — 4 embedding + 5 save/load + 1 SQL template
|===

*Pont RAG CDX-RC-04* : contrat N0 `SessionMemoryContract` (MEMPHIS
`runtime-contracts:0.0.1`) implémenté côté codex N2 via pgvector.
Table dédiée `codex_learner_profiles` (clé composite
`(learner_id, formation_id)`), embedding ONNX AllMiniLmL6V2 384-dim
calculé sur weakPoints + annotations uniquement.

*Pièges R2DBC documentés* : (1) `bind(index, Any)` n'accepte pas null →
`bindNull` ; (2) R2DBC lazy — `execute().awaitFirstOrNull()` ne consomme
pas le Result → INSERT non exécuté, utiliser `.awaitFirst().rowsUpdated.awaitFirst()` ;
(3) JSONB nécessite cast SQL explicite `$3::jsonb`.

*Coverage impact* : baseline 535 (S-093) → 661 tests (+126 dont +27
unit profile + +5 functional IT + +2 functional task IT + +20 cucumber
dédiés session-memory doublés par le `test` task + variations).
214 skip (`@Tag("integration")` sans Docker dans le `test` task par
défaut — les IT passent avec Docker dans `check`). `check` VERT 1m36s,
Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro
régression, backward compat total.

'''

== Session 093 — CDX-UNIFY unification constat #7 (+50 tests, 535/535 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/kotlin/codex/tasks/CodexPipelineChunkingTest.kt` | 4 characterization | ✅ PASS (output AsciiDoc non-blank, output contient texte extrait, PDF vide → fallback AsciiDoc, déterminisme cross-runs)
| `test/kotlin/codex/bdd/UnifiedPipelineChunkingSteps.kt` | 3 scénarios BDD | ✅ PASS (routing disabled → outputFile + AsciiDoc, routing enabled Apache → OSS/ + AsciiDoc, déterminisme cross-runs)
| `test/resources/features/codex_pipeline_unified_chunking.feature` | 3 scénarios `@pipeline @unify` | ✅ 3/3 PASSED via `CodexPipelineUnifyCucumberRunner` (pattern S-082, 5e runner codex)
| `main/kotlin/codex/tasks/CodexPipelineTask.kt` | refactor -37 lignes | ✅ `chunkMd` supprimé (33 lignes), délègue à `SemanticChunker.chunk()`, import `MessageDigest` supprimé. Non-régression : `CodexPipelineRoutingTest` 7/7, `CodexPipelineIntegrationTest` 3/3, `SemanticChunkerTest` 7/7, `ChunkDocumentTaskTest` 11/11.
|===

*Refactor CDX-UNIFY-1* : `CodexPipelineTask.chunkMd` (constat #7) unifié
avec `SemanticChunker.chunk()`. L'audit infirme l'hypothèse de risque
S-091 ("pgvector orphelins") : `chunkMd` était éphémère (log-only), hors
circuit pgvector. Le chemin pgvector passe par `ChunkDocumentTask` →
`SemanticChunker.chunk()` (depuis S-091). Refactor sécuritaire.

*Characterization tests* (pattern CDX-7-1) : 4 tests verrouillent le
contrat de sortie de `pipeline()` AVANT le refactor (GREEN pré-refactor)
→ REFACTOR (délégation `SemanticChunker.chunk()`) → GREEN post-refactor
(tests inchangés, behaviour output identique).

*Cucumber BDD CDX-UNIFY-2* : 3 scénarios `@pipeline @unify` via runner
dédié `CodexPipelineUnifyCucumberRunner` (pattern S-082, 5e runner
dédié codex). Step defs préfixés "unified" (bug S-088 — évite
DuplicateStepDefinition avec `LicencePipelineRoutingSteps` CDX-6-3).

*Coverage impact* : baseline 485 (S-092) → 535 tests (+50 dont +4
characterization +46 variations découverte cucumber par le `test` task
qui scanne `src/test/resources/features/`). `check` VERT 2m11s, Kover
VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression,
backward compat total.

'''

== Session 091 — CDX-7-3 SemanticChunker extraction (+7 tests, 508/508 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/kotlin/codex/tasks/SemanticChunkerTest.kt` | 7 unit | ✅ PASS (chunk splitting 3+ sections, no-heading doc → 1 chunk level 0, code blocks JS+Python, deterministic SHA-256 IDs, buildSectionPath hierarchy with " > " separators, extractFirstTwoSentences 2-sentence, extractFirstTwoSentences null for empty/blank/heading-only)
| `main/kotlin/codex/tasks/SemanticChunker.kt` | (object pur extrait) | ✅ Pattern CDX-7-1 `TypographicAnalyzer` — `chunk()` + `buildSectionPath()` + `extractFirstTwoSentences()` publics, logique privée préservée verbatim de `ChunkDocumentTask`
| `main/kotlin/codex/tasks/ChunkDocumentTask.kt` | refactor -153 lignes | ✅ Délègue à `SemanticChunker.chunk()`, `Section`/`buildChunks`/helpers supprimés. Non-régression 11/11 ChunkDocumentTaskTest PASSED.
|===

*Nouveau testé* :
- `SemanticChunker` — object pur (domaine `codex.tasks`, pattern CDX-7-1
  `TypographicAnalyzer`). 7 unit tests couvrent : chunk splitting par
  heading (3+ sections), document sans heading → 1 chunk level 0,
  extraction code blocks (``` fences JS + Python), IDs déterministes
  SHA-256 (`chk-<16hex>` prefix), `buildSectionPath` hiérarchie ancêtres
  avec " > " separators (exclut siblings), `extractFirstTwoSentences`
  2-sentence extraction + null pour empty/blank/heading-only.
- `ChunkDocumentTask` delegation — refactor backward compat total.
  11/11 `ChunkDocumentTaskTest` PASSED après refactor (behaviour
  identique : même chunks, mêmes IDs, mêmes sectionPaths, mêmes
  overlaps, mêmes code blocks).

*Constat #7 documenté* : `CodexPipelineTask.chunkMd` (lignes 364-396)
duplicate simplifié de `SemanticChunker` (pas de hiérarchie, pas
d'overlap, pas de code blocks, IDs positionnels `$sourceDocument:$i`
vs SHA-256 `$source:$sectionPath`). Unification *différée* — changer
`chunkMd` casserait IDs chunks → chunks pgvector orphelins. KDoc
`SemanticChunker` trace le constat pour US future.

*Coverage impact* : baseline 501 → 508 tests (+7). `check` VERT, Kover
VERT, validatePlugins VERT. Zéro régression, zéro bump BOM, zéro secret.

'''

== Session 087 — CDX-4-3 câblage canal Graphify (+15 tests, 392/392 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/kotlin/codex/enrichment/GraphifySectionBuilderTest.kt` | 7 unit | ✅ PASS (empty → "", omits sections without graphify nodes, lists resolved ids per section, pure deterministic, multiple sections, all-empty → "", ignores rag chunks and entities)
| `test/kotlin/codex/tasks/CodexCompositeContextGraphifyTest.kt` | 7 functional | ✅ PASS (buildGraphifySection empty when file absent, empty when empty list, populates from enriched nodes with graphify ids, degrades silently on invalid JSON, deterministic, enrichedJsonFile optional property)
| `test/kotlin/codex/tasks/CodexCompositeContextTaskTest.kt` | +2 extension | ✅ PASS (task exposes optional enrichedJsonFile, unset does not crash configuration)
|===

*Nouveau testé* :
- `GraphifySectionBuilder` — object pur du domaine `codex.enrichment`
  (pattern `JsonLddEnricher`). 7 unit tests couvrent : empty list → "",
  filtre sections sans nœuds Graphify (économie d'encre), rend une entrée
  par section (titre + liste ids résolus), déterministe (pure), multi-
  sections, all-empty → "", ignore rag chunks/entities (seul
  `graphifyNodes` est consommé).
- `CodexCompositeContextTask.buildGraphifySection()` — méthode
  `internal` testable sans pgvector (la dépendance coûteuse reste
  isolée dans `execute()`). 7 functional tests couvrent : fichier absent
  → "" (backward compat), liste vide → "", enrichissement avec ids →
  texte peuplé, JSON invalide → "" (dégradé silencieux), déterminisme,
  propriété optionnelle (pas de default, pas de crash).
- `CodexCompositeContextTask.enrichedJsonFile` — propriété Gradle
  optionnelle (`@InputFile @Optional`), 2 tests extension
  (`CodexCompositeContextTaskTest`) vérifient l'exposition et
  l'optionalité.

*Couverture* :
- Logique domaine `GraphifySectionBuilder.build` → 7 unit tests purs
  (aucune dépendance Gradle, testable plain JUnit5).
- Câblage Gradle `buildGraphifySection()` (IO + dégradé silencieux) →
  7 functional tests ProjectBuilder + fichiers JSON synthétiques
  (enriched-empty, enriched with 2 nodes, enriched-bad).
- Wiring `CodexPlugin` (liaison `enrichedJsonFile` →
  `build/codex/enriched-ldd.json`) → couvert par la non-régression
  `CodexPluginTest` (baseline préservée, le test existant ne casse pas
  car la propriété est `@Optional`).
- Backward compat total : `execute()` non testé (pgvector requise),
  mais `buildGraphifySection()` est `internal` et testé isolément. La
  modification de `execute()` est une seule ligne
  (`graphifySection = buildGraphifySection()` au lieu de `""`).

*Baseline* : 377 (S-086) → 392 (+15 net). 0 FAIL. `check` VERT, Kover
VERT, validatePlugins VERT.

'''

== Session 086 — CDX-4-2 tâche enrichJsonLdd (+7 tests, 377/377 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/kotlin/codex/tasks/EnrichJsonLddTaskTest.kt` | 6 functional | ✅ PASS (registered transform group, writes enriched JSON from LDD+chunks+graph, rag chunks attached by section title, graphify nodes resolved, degraded silent missing graph, semantic density 0.5)
| `test/kotlin/codex/CodexPluginTest.kt` | +1 wiring | ✅ PASS (enrichJsonLdd task type EnrichJsonLddTask, group transform, description "enriched")
|===

*Nouveau testé* :
- `EnrichJsonLddTask` — thin wrapper Gradle autour de `JsonLddEnricher`
  (objet pur créé S-080). 6 functional tests ProjectBuilder + JSON LDD
  synthétique (2 sections Architecture/Testing) + chunks RAG synthétiques
  (2 chunks) + `graph.json` synthétique (2 nodes).
- Sérialisation `@Serializable` sur `EnrichedLddNode` — exercée par
  `parseEnriched()` dans les 6 tests (lecture JSON → data class roundtrip,
  `EnrichedLddNode.serializer()`).
- Dégradé silencieux : `graph.json` manquant → `graphifyNodes` vide, pas
  de crash (contrat `GraphifyFileResolver` S-080).

*Couverture* : la logique métier (`JsonLddEnricher.enrich`) est couverte
par les 10 unit tests `JsonLddEnricherTest` + 6 `GraphifyFileResolverTest`
(S-080). La tâche Gradle ajoute la couverture functional du wiring (IO
contract, ProjectBuilder, plugin registration, output file écriture).

*Non-régression* : 365 unit/functional préservés (baseline S-085),
6 cucumber licence préservés (S-083), 6 cucumber ontology préservés (S-085).
377 tests 0 FAIL. `check` VERT, Kover VERT, validatePlugins VERT.

'''

== Session 085 — CDX-3-3 Cucumber BDD Ontology Derivation (+6 cucumber dédiés, 370/370 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/kotlin/codex/bdd/OntologySteps.kt` (via `CodexOntologyCucumberRunner`) | 3 scénarios `@ontology @derivation` | ✅ PASS (bounded context nommé depuis LDD title, aggregates depuis ON DELETE CASCADE, value objects = tables sans FK sortantes)
|===

*Nouveau runner Cucumber dédié* : `CodexOntologyCucumberRunner` (pattern S-082 —
`@Suite` + `@SelectClasspathResource` + glue `codex.bdd` + filter `@ontology and @derivation`).
Tâche Gradle `cucumberTestOntology` (pattern S-082 inline, `excludeEngines("junit-jupiter")`,
`includeTestsMatching("codex.bdd.CodexOntologyCucumberRunner")`).

*Pure BDD* : zéro production modifié. `OntologySteps` (8 step defs) drive
`DeriveOntologyTask` via `ProjectBuilder` (in-process, zéro I/O réseau).
SQL DDL synthétique identique à `DeriveOntologyTaskTest` (3 tables book/documents/paragraphs,
2 CASCADE FK, 1 self-ref).

*Non-régression* : 337 unit/functional préservés (baseline S-084),
6 cucumber licence préservés (S-083), +6 cucumber ontology (S-085).
370 tests 0 FAIL. `check` VERT, Kover VERT, validatePlugins VERT.

'''

== Session 084 — CDX-3-2 tâche deriveOntology (+4 functional, 337/337 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/kotlin/codex/tasks/DeriveOntologyTaskTest.kt` | 4 functional | ✅ PASS (tâche registered transform group, writes mapping from SQL DDL, uses LDD title for context name, empty SQL → empty mapping)
|===

*Nouveau testé* :
- `DeriveOntologyTask` — thin wrapper Gradle autour de `OntologyDeriver`
  (objet pur créé S-079). 4 functional tests ProjectBuilder + SQL DDL
  synthétique + assertions JSON (parse `OntologyMapping.serializer()`).
- Sérialisation rétroactive `@Serializable` sur `OntologyMapping` + 3
  sous-types — exercée par `parseMapping()` dans les 4 tests (lecture
  JSON → data class roundtrip).

*Couverture* : la logique métier (`OntologyDeriver.derive`) est couverte
par les 13 unit tests `OntologyDeriverTest` (S-079). La tâche Gradle
ajoute la couverture functional du wiring (IO contract, ProjectBuilder,
plugin registration, output file écriture).

== Session 083 — CDX-5-3 Cucumber BDD licence routing (+6 cucumber dédiés, 333/333 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/kotlin/codex/bdd/LicenceRoutingSteps.kt` | 6 step defs (3 scénarios × 2 découvertes) | ✅ PASS (Apache→OSS, Copyright→office, Unknown→OSS fallback — PDFs synthétiques PDFBox, ProjectBuilder, délégation `super.export()`)
| `test/resources/features/codex_export_routed.feature` | 3 scénarios `@licence @routing` | ✅ PASS (Background chunks + baseDir, 3 PDFs routing end-to-end)
| `test/kotlin/codex/bdd/CodexLicenceCucumberRunner.kt` | runner dédié | ✅ PASS (pattern S-082, `@Suite` + `@SelectClasspathResource`, glue `codex.bdd`)
|===

*Nouveau testé* :
- `CodexLicenceCucumberRunner` — runner Cucumber dédié (pattern S-082,
  1re application codex). Isole `codex_export_routed.feature` du suite
  global via `@SelectClasspathResource` + filter tags.
- `LicenceRoutingSteps` — 6 step defs BDD (Given chunks file + baseDir,
  Given PDF text, When task runs + fallback, Then exported under, And
  contains file). Réutilise PDFBox + ProjectBuilder (mirror
  `DeployKnowledgeBaseRoutedTaskTest`).
- Tâche Gradle `cucumberTestLicence` — Test dédiée (excludeEngines
  junit-jupiter, filter runnerClass, override `cucumber.features`).

*Couverture* : pure BDD — zéro production modifié. Le domaine
`codex.licence` (S-081) et la tâche `DeployKnowledgeBaseRoutedTask`
(S-082) sont exercés via `ProjectBuilder`. Aucune nouvelle logique
métier.

*Non-régression* : 306 → 333 tests (+27 : 6 cucumber dédiés × 2
découvertes runner). 0 failures, 15 skipped (@integration préservés).
`check` VERT, Kover VERT, validatePlugins VERT.

== Session 082 — CDX-5-2 Tâche deployKnowledgeBaseRouted + wiring CodexPlugin (+6 tests, 306/306 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/tasks/DeployKnowledgeBaseRoutedTaskTest.kt` | 5 functional | ✅ PASS (Apache→OSS, Copyright→office, Unknown+OSS fallback, Unknown+CSS fallback, CC-BY→OSS — PDFs synthétiques PDFBox, ProjectBuilder, délégation `super.export()`)
| `test/codex/CodexPluginTest.kt` (extension) | +1 wiring | ✅ PASS (task `deployKnowledgeBaseRouted` registered, type `DeployKnowledgeBaseRoutedTask`, group `deploy`, description contains "licence")
|===

*Nouveau testé* :
- `DeployKnowledgeBaseRoutedTask` — tâche Gradle additive (étend `ExportKnowledgeBaseTask`) :
  detection `PdfLicenseDetector` → routing `LicenseRouter` → rebind `outputDir` →
  delegation `super.export()`. 3 props Gradle (`pdfFile`, `baseDir`, `fallbackZone`).
  `@DisableCachingByDefault` (validatePlugins).
- Backward compat : `ExportKnowledgeBaseTask.export()` rendu `open` — 8 tests existants préservés.

*Niveaux de tests baby-step TDD* :
- RED : 1 fichier test créé (5 functional) → compile fail (`Unresolved reference 'fallbackZone'`).
- GREEN : 2 fichiers prod créés (1 nouvelle tâche + 1 modification `open`) → 5/5 functional + 1/1 wiring PASSED.
- REFACTOR : bug `validatePlugins` (`@DisableCachingByDefault` manquante) → corrigée. `check` VERT.

*Non-régression* :
- `ExportKnowledgeBaseTaskTest` 8/8 PASS (backward compat `open`).
- `CodexPluginTest` 7/7 PASS (incl. +1 wiring, 6 existants préservés).
- `check` BUILD SUCCESSFUL in 44s (Kover VERT, validatePlugins VERT).
- 306 tests 0 FAIL (baseline 300 → +6 : 5 functional + 1 wiring).

*Zéro bump BOM, zéro secret, zéro régression, backward compat total.*

'''
== Session 081 — CDX-5-1 Domaine codex.licence + PdfLicenseDetector + LicenseRouter (+16 unit, 300/300 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/licence/PdfLicenseDetectorTest.kt` | 12 JUnit5 | ✅ PASS (object pur — apache 2.0 full, SPDX apache-2.0, creative commons, CC-BY, CC0, © sign, copyright word, all rights reserved, no mention UNKNOWN, blank UNKNOWN, apache precedence over copyright, case-insensitive)
| `test/codex/licence/LicenseRouterTest.kt` | 4 JUnit5 | ✅ PASS (object pur — OSS→OSS/, CSS→office/, UNKNOWN→office/ precaution, no dir creation)
|===

*Baseline* : 284 tests (S-080) → *300 tests* (S-081), +16 unit, 0 FAIL.

== Session 080 — CDX-4-1 Domaine codex.enrichment + JsonLddEnricher + GraphifyFileResolver (+16 unit, 284/284 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/enrichment/JsonLddEnricherTest.kt` | 10 JUnit5 | ✅ PASS (object pur — empty, chunk matching, other section excluded, graphify resolved, density 0.5, zero density, entities extracted, ldd preserved, pure/deterministic, paragraph handled)
| `test/codex/enrichment/GraphifyFileResolverTest.kt` | 6 JUnit5 | ✅ PASS (adapter — empty, exact match, case-insensitive, no match, missing nodes field, multiple matches, `@TempDir`)
|===

*Baseline* : 268 tests (S-079) → *284 tests* (S-080), +16 unit, 0 FAIL.

== Session 079 — CDX-7-1 tests FontStyleDetector + TypographicAnalyzer (+44 unit, 255/255 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/tasks/FontStyleDetectorTest.kt` | 32 JUnit5 | ✅ PASS (characterization exhaustive objet existant)
| `test/codex/tasks/TypographicAnalyzerTest.kt` | 12 JUnit5 | ✅ PASS (objet extrait de ExtractBookStructureTask)
|===

*Baseline* : 211 tests (S-077) → *255 tests* (S-078), +44 unit, 0 FAIL.

*FontStyleDetectorTest* (32) — characterization de l'objet existant `FontStyleDetector` :
. 12 mots-clés monospace (courier, mono, consolas, typewriter, menlo, monaco,
  source code, fira code, jetbrains, droid sans mono, dejavu sans mono, liberation mono)
. Priorité monospace > bold (CourierNew-Bold → MONOSPACE)
. 5 variantes bold (bold, heavy, black, "bd " avec espace, BoldMT regex)
. 5 variantes italic (italic, oblique, slanted, "it " avec espace, ItalicMT regex)
. Combinaison BOLD_ITALIC (BoldItalic, Bold Italic, BoldOblique)
. Fallback NORMAL (Helvetica, Arial, Times-Roman, empty string)
. Case-insensitive (HELVETICA-BOLD, helvetica-italic, COURIER)
. Helpers enum `isMonospace`/`isBold`/`isItalic` pour les 5 valeurs

*TypographicAnalyzerTest* (12) — objet extrait de `ExtractBookStructureTask` :
. 4 thresholds dynamiques (large range > 4.0) : h1 = max - range×0.05, etc.
. 4 fallbacks (range < 1.0 → max×0.95, < 2.0 → max×0.75, < 3.0 → max×0.50, < 4.0 → max×0.35)
. Zero range → tous fallbacks
. Ordre h1 > h2 > h3 > h4 pour large range
. Plafond max (thresholds ≤ max)
. Range négative défensive → tous fallbacks

*Gate Kover* : VERT (préservé S-077). Kover 0.9.8 ne supporte pas offline
instrumentation — couverture plugin tests non comptée (limitation connue).

*Prochaines US de test (CDX-7)* :
. CDX-7-2 `ConvertToMarkdownTask` chaîné ~5 unit
. CDX-7-3 `SemanticChunker` extrait ~7 unit
. CDX-7-4 `CodexPipelineIntegrationTest` E2E PDF ~3 integration

'''
== Session 072 — CDX-13 OCR Pipeline (+32 tests, 167/167 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/ocr/LlmOcrEngineTest.kt` | 6 JUnit5 | ✅ PASS (fake OllamaChatClient)
| `test/codex/ocr/OcrPipelineTest.kt` | 6 JUnit5 | ✅ PASS (fallback chain)
| `test/codex/ocr/HttpOllamaChatClientTest.kt` | 5 JUnit5 | ✅ PASS (mock server JDK)
| `test/codex/tasks/CollectOcrTaskTest.kt` | 5 JUnit5 | ✅ PASS (ProjectBuilder)
| `test/resources/features/ocr-pipeline.feature` | 5 Cucumber | ✅ PASS (18 steps)
|===

Nouveaux tests détaillés :

*LlmOcrEngineTest* (6) :
. `LlmOcrEngine implements OcrEngine`
. `process returns OcrResult with structured AsciiDoc text from LLM response`
. `process returns empty result when LLM response is blank`
. `process returns empty result when client throws`
. `process uses custom prompt when provided in request`
. `process encodes image as base64 in Ollama chat payload`

*OcrPipelineTest* (6) :
. `OcrPipeline returns LLM result when LLM succeeds`
. `OcrPipeline falls back to Tesseract when LLM returns empty text`
. `OcrPipeline falls back to next engine when LLM confidence is zero`
. `OcrPipeline returns empty result when all engines fail`
. `OcrPipeline with single engine returns that engine result`
. `OcrPipeline metadata records which engine won`

*HttpOllamaChatClientTest* (5) :
. `chat sends POST to api chat endpoint and returns message content`
. `chat throws on non 2xx response`
. `chat supports legacy response field when message missing`
. `chat uses config endpoint when provided`
. `chat payload contains model and images fields`

*CollectOcrTaskTest* (5) :
. `plugin registers collectOcr task with correct type`
. `collectOcr task has correct group and description`
. `collectOcr task inherits Ollama conventions from extension`
. `collectOcr task produces empty output when no images found`
. `extension exposes ollama and ocr properties`

*ocr-pipeline.feature* (5 scénarios) :
. `LLM OCR engine produces structured AsciiDoc from image`
. `OCR pipeline falls back to Tesseract when LLM fails`
. `OCR pipeline returns empty result when all engines fail`
. `collectOcr task concatenates pages into a single AsciiDoc document`
. `collectOcr task produces empty output when no images found`

Fix : `OcrConfigTest.kt:12` — nom test contenant `:` illégal Kotlin (renommé).

== Session 067 — TesseractOcrEngineTest (5 tests JUnit5)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat
| `test/codex/ocr/TesseractOcrEngineTest.kt` | 5 JUnit5 | ✅ PASS
|===

Tests :
. `TesseractOcrEngine implements OcrEngine`
. `processes minimal image without throwing`
. `returns empty result on tesseract failure`
. `maps language codes to tesseract format`
. `returns result with sourceFormat from request`

Note : Tesseract 5.3.4 installé sur le système de test. Tests utilisent
un PNG 1x1 minimal (sans texte) — vérifient le plumbing sans crash, pas
l'extraction de texte réelle.

== Metrics Kover (après session 024 — CDX-14)

Rapport HTML : `build/reports/kover/html/index.html`

|===
| Metrique | Valeur | Objectif | Note
| Line coverage | **77.5%** (979/1263) | ≥ 85% | Stable — Kover ne capture pas les tests via ProjectBuilder (Gradle TestKit)
| Branch coverage | **60.8%** (397/653) | ≥ 75% | Stable
| Method coverage | **75.5%** (108/143) | ≥ 85% | +1% via Metadata.forBrooklyn()
| Class coverage | **68.6%** (35/51) | ≥ 85% | Stable
|===

[NOTE]
====
Les nouveaux tests JUnit5 pour CodexPlugin (8 tests), CodexExtension (4 tests), Metadata (7 tests)
et les extensions CodexVectorStore (+5 tests) utilisent ProjectBuilder ou la reflexion.
Kover ne peut pas instrumenter le bytecode charge dans un classloader Gradle TestKit separe,
ce qui explique que les metrics ne remontent pas. 105/105 tests PASS.
====

== Trous de couverture identifies

|===
| Package | Line cov | Classes critiques | Priorite | Tests ecrits
| `codex` | 26.6% | `CodexPlugin.kt`, `Metadata.kt`, `CodexExtension.kt` (0% Kover, mais testes) | 🟢 COUVERT | 8+4+7 = 19 nouveaux tests ✅
| `codex.store` | 14.3% | `CodexVectorStore.kt` | 🟡 MOYENNE | +5 tests (computeEmbedding, buildConnectionFactory, params) ✅
| `codex.tasks` | ~85% | ImportBookSqlTask, ExtractEpubStructureTask, etc. | 🟢 BASSE | Deja couverts
|===

== Couverture par Classe (JUnit5 — 105/105 PASS)

|===
| Classe | Tests | Statut
| `LicenseZoneDetectorTest` | 7 | ✅ OSS/CSS/UNKNOWN detection + Apache-2.0/PROPRIETARY mapping
| `ExtractTextTaskTest` | 3 | ✅ Simple PDF, multi-ligne, empty PDF
| `ExtractBookStructureTaskTest` | 10 | ✅ Bold/Italic/Monospace/Normal/BoldItalic detection, Headers+code block, Empty PDF
| `ExtractEpubStructureTaskTest` | 6 | ✅ EPUB synthetique, hierarchie multi-niveaux, code blocks, inline code, empty EPUB
| `AsciiDocToJsonLddTaskTest` | 4 | ✅ Simple hierarchy, Paragraphs, Empty doc, Nested children
| `ImportBookSqlTaskTest` | 4 | ✅ DDL+INSERT, Correct titles, Paragraph INSERT, Empty array
| `ConvertToMarkdownTaskTest` | 10 | ✅ Headers, Code blocks, Admonitions, Tables, Images, Links, Lists, Inline formatting
| `ChunkDocumentTaskTest` | 11 | ✅ Multi-headings, Code blocks, Deterministic IDs, Section hierarchy, Overlap, Licence
| `ExportKnowledgeBaseTaskTest` | 9 | ✅ 3 formats, JSON-L context/graph, Code blocks, Markdown hierarchy, AsciiDoc, Sorting
| `CodexRetrieveTaskTest` | 2 | ✅ Registration, Configurabilite
| `CodexVectorStoreTest` | 5 | ✅ Instanciation, Default params, RetrieveResult contract, searchBlocking, search
| `CodexIngestRetrieveIT` | 4 | ✅ End-to-end pgvector, Batching batchSize=1, Pipeline auto-detect PDF, Empty chunks
|===

== Cucumber (BDD)

✅ IMPLANTE Session 024 (CDX-14) :

|===
| # | Feature | Scenarios | Statut
| 1 | extract-chunk-pipeline.feature | 1 | ✅ Ecrit + steps implementees
| 2 | chunk-ingest-pipeline.feature | 1 | ✅ Ecrit + steps implementees
| 3 | ingest-retrieve-pipeline.feature | 2 | ✅ Ecrit + steps implementees
| 4 | pipeline-auto-detect.feature | 1 | ✅ Ecrit + steps implementees
| 5 | license-zone-detection.feature | 2 | ✅ Ecrit + steps implementees
| **Total** | **5 features** | **7 scenarios** | ✅
|===

== Infrastructure de test

- JUnit5 + TestContainers 1.21.4 + docker-java 3.7.0
- pgvector/pgvector:pg16 pour tests end-to-end
- Kover pour metrics de couverture (HTML + XML)
- slf4j 2.0.17 + logback 1.5.32

== Dette de couverture — Session 027 (OCR-0)

*Date* : 2026-05-30

|===
| Classe | Tests | Statut
| `codex.ocr.OcrRequest` | 0 | 🔴 Aucun test — session 028
| `codex.ocr.OcrResult` | 0 | 🔴 Aucun test — session 028
| `codex.ocr.OcrConfig` | 0 | 🔴 Aucun test — session 028
|===

3 regressions preexistantes a investiguer :
- `ExtractBookStructureTaskTest` : 1 echec (single line monospace devient inline code)
- `ExtractEpubStructureTaskTest` : 2 echecs (empty EPUB + note header)

== Session 029 — Publication Maven Central (aucun test modifie)

Aucun test modifié. Publication ops uniquement (version, nmcp, POM).

== Session 030 — Fix plugin ID + buildscript Gradle 9.x (aucun test OCR ajoute)

Aucun test OCR ajouté. Infrastructure build uniquement :
- Plugin ID synchronisé : `education.cccp.codex` dans build.gradle.kts et tests
- `plugins {}` déplacé avant `buildscript {}` (Gradle 9.5.1)
- Tests toujours 135/135 PASS

== Prochaine session (031)

- CDX-15 : TDD retroactif OcrRequest/OcrResult/OcrConfig
- Diagnostic des 3 regressions
- publishToMavenLocal (si autorise)
- CDX-13 : Suspension REST maintenue

== Session 073 — EPIC CDX-CR3 + US-CDX-13-1/2 (+37 tests, 204/204 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/codex/store/IngestStatementsTest.kt` | 10 JUnit5 | ✅ PASS (templates SQL paramétrés)
| `test/codex/store/ChunkIndexAssignmentTest.kt` | 6 JUnit5 | ✅ PASS (index locaux par document)
| `test/codex/tasks/CodexCompositeContextTaskTest.kt` | 7 JUnit5 | ✅ PASS (propriétés pg + defaults)
| `test/codex/ocr/OcrResultCacheTest.kt` | 13 JUnit5 | ✅ PASS (cache hit/miss, hash, dégradé)
| `test/codex/ocr/LlmOcrEngineIntegrationTest.kt` | 5 integration | ✅ PASS (vrai Ollama port 11437)
|===

*Nouveaux domaines testés* :
- `codex.store` — IngestStatements (templates SQL), IngestIndexing (index locaux)
- `codex.ocr` — OcrResultCache (cache hash), OllamaProbe (sonde disponibilité)

*Tests d'intégration* : 5 tests @Tag("integration") avec assumeTrue(ollamaReady())
— skip propre hors CI/local sans Ollama. En local (port 11437, gpt-oss:120b-cloud),
5/5 PASSED avec vrai contrat HTTP.

*Non-régression* : CodexIngestRetrieveIT 4/4 PASS (Testcontainers pgvector),
Cucumber OCR pipeline 5/5 PASS, tous tests existants préservés.

== Session 092 — CDX-7-4 CodexPipelineIntegrationTest E2E

*Ajout* : `CodexPipelineIntegrationTest` (3 tests `@Tag("integration")`)
dans `codex/tasks/`. Chaîne les 3 tâches (ExtractBookStructureTask →
ConvertToMarkdownTask → ChunkDocumentTask) sur PDF canonique réel
`office/books-collection/litterature/Manifeste des 60.pdf` (65 KB).

*Métriques* :
- Tests total : 482 → 485 (+3)
- Tests failed : 0
- Tests skipped : 90 → 93 (+3 integration skippés en CI sans corpus)
- `check` VERT 1m26s, Kover VERT, validatePlugins VERT

*Pattern* : `assumeTrue(pdfFile.exists())` + walk-up résolution. Le
corpus `office/` n'est jamais commité — les tests E2E se skippent
proprement hors workspace local (CI, fresh clone). System property
`codex.canonical.pdf` permet de pointer un PDF alternatif.

*Non-régression* : 485 tests 0 FAIL, 26 cucumber dédiés GREEN (8
enrichment + 3 ontology + 3 licence + 3 pipeline routing), zéro
production modifié.

== Session 096 — Cadrage CDX-OCR-BOUNDARY (aucun test modifié)

Gouvernance pure — zéro code, zéro test. Baseline vérifiée : 661 tests,
0 FAIL (214 skip intégration). L'EPIC cadré impactera la couverture dès
S-097 : characterization tests `OcrPipeline` (US-1), migration tests
intégration sur fake engine après purge pile IA (US-3).

== Session 097 — Cadrage CDX-RAG-SOCLE (aucun test modifié)

Gouvernance pure — zéro code, zéro test. Baseline vérifiée : 661 tests,
0 FAIL. Impact couverture futur : characterization tests `CodexVectorStore`/
`IngestStatements` migrés verbatim vers `codebase.store.RagVectorStore`
(RAG-1), tests codex re-pointés après bascule + purge (RAG-3), Cucumber BDD
canal Docs sur store socle (RAG-5).

== Session 098 — CDX-OCR-1 Injection port AI OCR (+6 tests, 667 exécutés 0 FAIL)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `test/kotlin/codex/tasks/CollectOcrAiEngineInjectionTest.kt` | 6 unit | ✅ 6/6 PASSED — propriété `aiEngine` exposée + orNull null par défaut ; plugin apply ne pré-câble AUCUN engine IA (wiring = responsabilité composition root) ; AsciiDoc structuré du fake engine → contenu exact `001-page-001.adoc` ; engine vide/confiance 0 → fallback Tesseract → marqueur `[page vide ou OCR échec]` (ordre préservé) ; legacy outputFile backward compat (`= OCR Book` + `== Page 1` + contenu injecté) ; short-circuit no-images → 0 appel engine (économie d'encre)
|===

*RED vérifié strictement* : 7× `Unresolved reference 'aiEngine'`
(compileTestKotlin FAILED) avant implémentation. GREEN 6/6 en 0.9s.

*Characterization cadrage US-1* : la mécanique pipeline (ordre fallback,
passthrough texte structuré) était déjà verrouillée par
`OcrPipelineTest` (S-072, 6 stub-based tests) — la nouvelle suite ajoute
la characterization *task-level* du point d'injection.

*Coverage impact* : baseline 661 (S-095..S-097) → 667 tests exécutés
(+6 unit). 214 skip inchangé (variations découverte cucumber CR-10 +
intégration assumeTrue). Cucumber dédiés : 52 scénarios PASSED (8
enrichment + 20 session-memory + 6 licence + 6 ontology + 6
pipeline-routing + 6 pipeline-unify). `check` VERT 1m27s, Kover VERT,
validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression.

*Impact futur* : CDX-OCR-3 (purge pile IA) migrera `LlmOcrEngineTest` +
`LlmOcrEngineIntegrationTest` sur fake/injected engine — la surface de
test est déjà prête via le port injectable.

== Session 099 — CDX-OCR-2 VisionOcrEngineAdapter (côté Queens, +4/-2 tests)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| `codebase: test/.../adapter/VisionOcrEngineAdapterTest.kt` | 4 unit | ✅ 4/4 PASSED (délégation bytes/mime/langue, structuredText provider, metadata miroir, model/maxTokens transmis)
| `codebase: test/.../ocr/OcrTaskAiOnlyBoundaryTest.kt` | 2 unit | ✅ 2/2 PASSED (tesseract rejeté message boundary ; double échec IA sans fallback software)
| `codebase: test/features/ocr.feature` | 2 scénarios réécrits | ✅ PASSED (boundary tesseract → codex ; erreur sans fallback)
| `codex: baseline inchangée` | 667 unit | ✅ zéro fichier codex modifié cette session
|===

*Coverage impact* : côté codebase +6 net (4 adapter + 2 boundary, -1
fichier legacy remplacé). Côté codex : zéro modification — la session
CDX-OCR-2 est entièrement cross-borough (Queens). Gate `check` VERT
41m26s. 3 échecs préexistants au baseline vérifiés par stash.

'''
== Session 100 — CDX-OCR-3 Purge pile IA codex (-28 tests, 639/639 PASS)

[cols="2,3,2"]
|===
| Fichier | Tests | Résultat

| *Supprimé* `test/kotlin/codex/ocr/LlmOcrEngineTest.kt` | 6 unit | ✅ SUPPRIMÉ (pile IA purgée)
| *Supprimé* `test/kotlin/codex/ocr/LlmOcrEngineIntegrationTest.kt` | 5 integration | ✅ SUPPRIMÉ (pile IA purgée)
| *Supprimé* `test/kotlin/codex/ocr/HttpOllamaChatClientTest.kt` | 5 unit | ✅ SUPPRIMÉ (pile IA purgée)
| *Supprimé* `test/kotlin/codex/ocr/OcrConfigTest.kt` | 5 unit | ✅ SUPPRIMÉ (pile IA purgée)
| `test/kotlin/codex/tasks/CollectOcrTaskTest.kt` | 3 unit (was 4) | ✅ PASS (2 tests fusionnés/renommés, setters ollama retirés)
| `test/kotlin/codex/tasks/CollectOcrPagesContractTest.kt` | 4 unit | ✅ PASS (setters ollama retirés, fallback par défaut sans aiEngine)
| `test/kotlin/codex/tasks/CollectOcrAiEngineInjectionTest.kt` | 6 unit | ✅ PASS (KDoc post-purge, tests inchangés)
| `test/kotlin/codex/bdd/OcrPipelineSteps.kt` | 18 step defs (was 23) | ✅ PASS (imports/step LlmOcrEngine retirés, step `primary engine` renommé)
| `test/resources/features/ocr-pipeline.feature` | 5 scénarios (was 6) | ✅ PASS (scénario LlmOcrEngine supprimé, 2 renommés)
|===

*Delta* : 667 → 639 tests (-28). 21 tests pile IA supprimés + 1 scénario
cucumber supprimé + 1 fusion `CollectOcrTaskTest` + 5 variations
découverte cucumber. Zéro régression — tests OCR restants
(`OcrPipelineTest`, `TesseractOcrEngineTest`, `OcrResultTest`,
`OcrRequestTest`, `OcrResultCacheTest`, `CollectOcrAiEngineInjectionTest`,
`CollectOcrPagesContractTest`, `CollectOcrTaskTest`) passent tous.

*Couverture* : Kover VERT (seuil préservé). La purge retire du code
non-couvert par construction (pile IA sans usage production), donc la
couverture relative augmente mécaniquement.

'''
== Session 101 — CDX-OCR-4 Cucumber BDD (EPIC CDX-OCR-BOUNDARY clôture)

*Delta* : 639 → 642 tests (+3 cucumber `@ocr-boundary`). `check` VERT
4m37s, 775 tests 0 FAIL (variations découverte cucumber incluses).

[cols="2,2,2", options="header"]
|===
| Fichier | Tests | Statut
| `test/kotlin/codex/bdd/OcrBoundarySteps.kt` (nouveau) | 11 step defs | ✅ PASS (counting engines `AtomicInteger`, in-memory `CacheStorage`, préfixe "boundary" anti-collision S-088)
| `test/kotlin/codex/bdd/CodexOcrCucumberRunner.kt` (nouveau) | 1 runner suite | ✅ PASS (7e runner codex, pattern S-082, filtre `@ocr-boundary`)
| `test/resources/features/ocr-pipeline.feature` | 10 scénarios (was 7) | ✅ PASS (3 scénarios `@ocr-boundary` ajoutés : AI injection + degraded + cache hit)
| `build.gradle.kts` | +1 tâche `cucumberTestOcr` | ✅ PASS (pattern S-082 inline)
|===

*Couverture* : Kover VERT (seuil préservé). Zéro production modifié —
pure BDD. Les 3 scénarios `@ocr-boundary` valident les contrats
boundary sans modifier le code de production (counting fake engines +
in-memory cache prouvent l'économie d'encre et la non-invocation de
Tesseract).
