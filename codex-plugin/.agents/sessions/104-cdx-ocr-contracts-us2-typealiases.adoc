= Session 104 — CDX-OCR-CONTRACTS US-2 : typealiases + extension functions
:session: 104
:date: 2026-08-24
:type: dev
:epic: CDX-OCR-CONTRACTS
:status: ✅ TERMINÉ (US-2)
:pattern: baby-step TDD strict

== Contexte

Suite cadrage S-103. US-1 MEMPHIS livrée en parallèle (ocr-contracts:0.0.1
publié mavenLocal, 14/14 unit PASSED). Cette session livre US-2 côté codex.

== Actions

. Dep `education.cccp:ocr-contracts:0.0.1` ajoutée (catalog + build.gradle.kts).
. `codex.ocr.OcrEngine`/`OcrRequest`/`OcrResult` remplacés par des
  *typealias* vers `contracts.ocr.*` (backward compat totale — imports
  existants résolvent transparentment).
. Jackson helpers `fromFile`/`writeTo` extraits du companion (mort avec le
  typealias) en *top-level functions* `OcrRequestJson.kt`/`OcrResultJson.kt`
  (`readOcrRequestFromJsonFile`/`writeOcrRequestToJsonFile`,
  `readOcrResultFromJsonFile`/`writeOcrResultToJsonFile`) — pattern :
  contrat pur N0, utilitaire sérialisation reste N2.
. Tests migrés : `OcrRequestTest`/`OcrResultTest` consomment les nouvelles
  fonctions top-level.
. Bump codex-plugin 0.0.5 → 0.0.6 (nouvelle dep N0 + typealiases).
. `publishToMavenLocal` VERT.

== Constat technique

Le typealias Kotlin ne propage PAS le `companion object` — l'approche
extension sur `OcrResult.Companion` compile pas (`Unresolved reference
'Companion'`). Solution : fonctions top-level nommées (plus explicite que
des extensions de companion fantômes).

== Résultats

* Gate `codex.ocr.*` + `CollectOcrTask*` + `CodexCompositeContextTask*` VERT.
* `compileKotlin`/`compileTestKotlin` VERT.
* Zéro régression (typealiases transparents), zéro bump BOM local,
  zéro secret.
* EPIC CDX-OCR-CONTRACTS : US-1 ✅ (MEMPHIS) + US-2 ✅ (codex). Reste
  US-3 codebase (bascule imports + retrait `libs.codex.plugin`) — livrée
  en parallèle côté Queens S-201 — et US-4 Cucumber BDD.

== Leçons

* Typealias = backward compat parfait pour data classes/fun interfaces,
  mais companion object non propagé → extraire les helpers en top-level.
* Le contrat N0 doit être pur (zéro Jackson) — les helpers restent N2.