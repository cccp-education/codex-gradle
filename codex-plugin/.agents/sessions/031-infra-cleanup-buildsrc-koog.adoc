= Session 031 — Infra cleanup : buildSrc supprimé, force() restauré, koog 1.0.0 entamé
:session: 031
:date: 2026-06-17
:type: chore+infra
:modele: deepseek-v4-flash:free (via opencode)
:status: PARTIEL

== Objectif

- Nettoyer buildSrc (inliner la convention plugin dans codex-plugin)
- Supprimer `eachDependency` redondant de codebase-plugin
- Mettre à jour koog 0.8.0 → 1.0.0

== Problèmes

- `eachDependency` dans codebase-plugin était redondant : la contrainte `annotations:13.0` vient du plugin `kotlin.jvm` qui ajoute `kotlin-stdlib` en `api`, propagée par `java-library` en contrainte publiée.
- `force("org.jetbrains:annotations:26.0.2-1")` dans codex est **nécessaire** : Gradle 9.5.1 embedded Kotlin pinne lui-même `annotations:{strictly 13.0}`, pas seulement codebase-plugin.
- koog 1.0.0 supprime `forwardTo` (deprecated API cleanup). 4 fichiers codebase-plugin impactés : `KoogAugmentedContextGraph.kt`, `KoogPlanningOrchestrator.kt`, `MultiChannelContextGraph.kt`, `VibecodingGraph.kt`. Migration à terminer en session suivante.

== Livrables

- `buildSrc/` supprimé de codex-gradle (convention `codex.gradle-plugin-conventions` inlinée)
- `eachDependency` supprimé de codebase-plugin (redondant)
- `validateDependencies` assoupli (log au lieu de require == "13.0")
- koog version passée de `0.8.0` à `1.0.0` dans workspace-bom, codex, codebase

== Prochaines étapes

- Remplacer `forwardTo` par l'API koog 1.0.0 de remplacement dans les 4 fichiers
- Compiler codebase-plugin avec koog 1.0.0 → republier en local
- Compiler codex-plugin avec codebase-plugin mis à jour
