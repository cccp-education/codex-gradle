= INDEX — codex-gradle
:toc:

_Regles absolues_ : voir `AGENT.adoc`
_Ontologie spatiale_ : voir `configuration/.agents/AGENT_GOVERNANCE.adoc`
_DAG_ : N2 — Plugin Gradle d'acquisition de documents READ + RAG (Regle 4bis)
_Boundary_ : Codex = READ + RAG. Document (New Orleans) = WRITE + PUBLISH. Paralleles N2, pas empiles.

_Derniere mise a jour_ : 2026-08-20 (session 084 — CDX-3-2 tâche `deriveOntology` + wiring `CodexPlugin` + sérialisation rétroactive `OntologyMapping` @Serializable, 4 functional, EPIC CDX-3 progression 2/3 US)
_Session terminee_ : 084 ✅ — CDX-3-2 `DeriveOntologyTask` (68 lignes, thin wrapper Gradle) — lit `sqlFile` (SQL LMD de `collectBookSql`) + `jsonFile` (LDD nodes de `transformToJsonLdd`), appelle `OntologyDeriver.derive(lddNodes, sqlScript)`, écrit `OntologyMapping` JSON via kotlinx.serialization. `@DisableCachingByDefault`, 3 properties (`@InputFile` × 2 + `@OutputFile`). Sérialisation rétroactive : `OntologyMapping` + 3 sous-types (`BoundedContext`, `Aggregate`, `ValueObject`) annotés `@Serializable` (modification additive, backward compat total). Wiring `CodexPlugin` : tâche `deriveOntology` groupe `transform`, `outputFile` default `build/codex/ontology-mapping.json`. Compteur KDoc 11 → 12 tâches. 4 functional tests (`DeriveOntologyTaskTest`) — ProjectBuilder + SQL DDL synthétique + assertions JSON. 337 tests 0 FAIL (baseline 333 → +4). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR. EPIC CDX-3 progression 2/3 US (reste CDX-3-3 Cucumber).
_Session precedente_ : 082 ✅ — CDX-5-2 `DeployKnowledgeBaseRoutedTask` (nouveau, 69 lignes) étend `ExportKnowledgeBaseTask` : détecte licence PDF via `PdfLicenseDetector` → route via `LicenseRouter` → rebind `outputDir` → délègue à `super.export()` (multi-format inchangé). 3 props Gradle : `pdfFile` (`@InputFile`), `baseDir` (`@OutputDirectory`), `fallbackZone` (`@Input @Optional`). `@DisableCachingByDefault` (validatePlugins). `ExportKnowledgeBaseTask.export()` rendu `open` (1 mot-clé, backward compat). Wiring `CodexPlugin` : task `deployKnowledgeBaseRouted` groupe `deploy`, `fallbackZone.convention(zone)` (apply-time). 6 tests (5 functional `DeployKnowledgeBaseRoutedTaskTest` : Apache→OSS, Copyright→office, Unknown+OSS fallback, Unknown+CSS fallback, CC-BY→OSS + 1 wiring `CodexPluginTest`). 306 tests 0 FAIL (baseline 300 → +6). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR.
_Prochaine session_ : 085 — Démarrer *CDX-3-3* Cucumber BDD `codex_derive_ontology.feature` 3 scénarios `@ontology` (clôture EPIC CDX-3). Baby-step BDD strict. Runner dédié pattern S-082 (`CodexOntologyCucumberRunner`) + `OntologySteps` + tâche `cucumberTestOntology`. Alternatives : CDX-4-2 (tâche `enrichJsonLdd` + wiring ~5 functional) ou CDX-4-3 (câblage Graphify ~3 unit) ou CDX-6-1/2 (DSL `licenceRouting`). Domaines purs P1 TERMINÉS (CDX-3-1/4-1/5-1 ✅). EPIC CDX-5 TERMINÉ 4/4 US. EPIC CDX-3 progression 2/3 US (CDX-3-2 ✅ S-084). Actions reportées : démarrer EPIC RUNTIME-CONTRACTS MEMPHIS (gate CDX-RC-04).
_Session en cours_ : —

'''
== REGLES ABSOLUES (resume)

=== 1. COMMITS/GIT — INTERDICTION FORMELLE sans permission
=== 2. PAS DE publishToMavenLocal automatique
=== 3. SECRETS via configuration/ — jamais en dur dans build.gradle.kts
=== 4. Codex est le contrat — types partages dans codex/
=== 5. DAG N2 — importable par N3 runner-gradle, jamais importe N3
=== 6. PROCEDURE DE FIN DE SESSION — 6 ETAPES OBLIGATOIRES
=== 7. BOUNDARY DOCUMENT — Codex READ + RAG, Document WRITE + PUBLISH (parallele N2, pas de dependance directe)

'''
== Sessions

|===
| # | Date | Type | Sujet | Score
| 0 | 2026-05-06 | init | Bootstrap gouvernance agent codex + scaffold plugin | —
| 001 | 2026-05-07 | design | JSON LDD, SQL LMD, Cartographie Ontologique | —
| 002 | 2026-05-07 | design | Separation des Concerns & Bouclage | —
| 003 | 2026-05-07 | design | Le Vrai MVP : Boucle d'Autonomie | —
| 004 | 2026-05-07 | chore | Migration chemins foundry/ → foundry/OSS/ | —
| 005 | 2026-05-09 | impl | CDX-1b asciiDocToJsonLdd via AsciidoctorJ AST | —
| 006 | 2026-05-10 | impl | CDX-1c importBookSql — JSON LDD → DDL + INSERT PostgreSQL | —
| 007 | 2026-05-10 | impl | CDX-2 detection typographique → hierarchie multi-niveaux + code | —
| 008 | 2026-05-10 | impl | CDX-3 convertToMarkdown (AsciiDoc → Markdown) | —
| 009 | 2026-05-10 | impl | CDX-4 chunkDocument (chunking semantique par section) | —
| 010 | 2026-05-10 | impl | CDX-5 exportKnowledgeBase (JSON-L/Markdown/AsciiDoc export) | —
| 011 | 2026-05-10 | impl | CDX-6 arborescence OSS/CSS + routing licence | —
| 012 | 2026-05-10 | test | CDX-7 Tests unitaires + integration (62/62 PASS) | ✅
| 013 | 2026-05-10 | impl | CDX-8 codexIngest ONNX → pgvector R2DBC | ✅
| 014 | 2026-05-11 | impl | CDX-9 codexRetrieve — recherche semantique cosine similarity | ✅
| 015 | 2026-05-11 | ci/cd | CDX-CI — GitHub Actions test.yml (JDK 24, publishToMavenLocal + check) | ✅
| 016 | 2026-05-11 | impl | CDX-10 codex-epub — extraction EPUB via Tika → AsciiDoc (70/70 PASS) | ✅
| 017 | 2026-05-11 | integration | CDX-11 codexEngine — piping codexRetrieve → runner-gradle N3 (75/75 PASS) | ✅
| 018 | 2026-05-12 | test | CDX-12 Tests integration pgvector end-to-end (79/79 PASS) | ✅
| 019 | 2026-05-12 | impl | EPIC D Uniformisation Build (Java 25→24 + kotlinx.serialization DocNode) | ✅
| 020 | 2026-05-12 | review | Code Review Globale (audit passif) | ✅
| 021 | 2026-05-19 | refact | EPIC K K-1 Renommage taxonomie (10 collect/transform/deploy) | ✅
| 022 | 2026-05-19 | doc | EPIC K K-5 Documentation — Mise a jour INDEX.adoc | ✅
| 023 | 2026-05-22 | bugfix+cleanup | Fix compilation bugs (5 fichiers) + cleanup packages | 9/10
| 024 | 2026-05-22 | test | CDX-14 Couverture tests (A) JUnit5 + (B) Cucumber — 105/105 PASS, 5 features Cucumber | ✅
| 025 | 2026-05-22 | doc | KDoc API documentation (18 classes, ~200 lignes) | ✅
| 026 | 2026-05-22 | investigate | Kover offline instrumentation — limitation documentee (Kover 0.9.1) | ✅
| — | 2026-06-08 | review | Architecture boundary review Codex↔Document — boundary READ+RAG vs WRITE+PUBLISH | ✅
| 030 | 2026-06-17 | bugfix+build | Fix plugin ID + buildscript Gradle 9.x — 135/135 PASS | ✅
| 031 | 2026-06-17 | chore+infra | Infra cleanup (buildSrc supprimé, eachDependency retiré, koog 1.0.0 entamé) | ✅ CADUQUE (S-076 — koog supprimé, migration sans objet)
| 032 | 2026-06-17 | bugfix+build | Fix Gradle 9 build — standalone sub-build + codebase 0.0.2 | ✅
| 067 | 2026-06-23 | impl+boundary | EPIC OCR-2 Tesseract fallback — OcrEngine + TesseractOcrEngine, boundary codex↔codebase harmonisé, 5 tests JUnit5, publishToMavenLocal 0.0.1 | ✅
| 068 | 2026-06-24 | doc+i18n | README multilingues — 20 fichiers (10 langues × consumer + plugin), badges Maven Central/Plugin Portal/CI/License, switcher i18n, AR/UR RTL | ✅
| **071** | **2026-07-03** | **dev** | **CNV-6 Migration — convention plugins education.cccp.build.* v0.0.1 appliquées (pattern bakery gradle-plugin + publishing), build.gradle.kts 200→139 lignes (-61), variantes gardées (buildscript annotations force, freeCompilerArgs -Xjsr305=strict, kover, kotlin.serialization), compileKotlin SUCCESS** | **✅ TERMINE**
| **072** | **2026-07-04** | **impl+test** | **CDX-13 OCR Pipeline — LlmOcrEngine (Ollama vision) + OcrPipeline (fallback LLM→Tesseract) + HttpOllamaChatClient (port/adapter) + CollectOcrTask (collectOcr → AsciiDoc, DOC-11 alignement), 167 tests (+32), baby-step TDD/BDD** | **✅ TERMINE**
| **073** | **2026-08-19** | **bugfix+impl+test** | **EPIC CDX-CR3 TERMINÉ 3/3 US (CDX-CR3-1 SQL injection, CDX-CR3-2 hardcode localhost:5432, CDX-CR3-3 chunk_index dupliqués) + US-CDX-13-1 cache OCR hash image (Loi Économie d'Encre) + US-CDX-13-2 tests intégration réelle Ollama (OllamaProbe + assumeTrue), 204 tests (+37), baby-step TDD strict, `check` VERT** | **✅ TERMINE**
| **074** | **2026-08-19** | **publish+governance** | **PUB-3.09 Publication Maven Central codex-plugin:0.0.3 (bundle `3b4d1915`, AUTOMATIC, propagé) + synchronisation amont cross-borough (MEMPHIS BOM 0.0.22 bundle `f913774d` + Queens codebase-gradle sync codex 0.0.3 + BOM 0.0.22 catch-up) + PUB-2.2 ✅ CADUQUE + 6 points PROMPT_REPRISE résolus, 204 tests préservés, zéro code prod modifié** | **✅ TERMINE**
| **075** | **2026-08-19** | **governance+impl+test** | **(1) MEMPHIS rattrapage 0.0.19→0.0.22 (4 versions hors-session, bug catalogue `libs.versions.toml` 0.0.20→0.0.22 corrigé, INDEX/SESSIONS_HISTORY/BACKLOG sync). (2) CDX-13-3 réactivé — split-brain N2↔N2 résolu : `CollectOcrTask` produit pages individuelles `NNN-<pageId>.adoc` dans `outputDir` (sortie primaire, alignée `PageOrder.fromFileName`), `outputFile` legacy backward compat. Baby-step TDD/BDD RED→GREEN→REFACTOR. 4 unit + 3 scénarios Cucumber `@n2-bridge`. 211 unit + 186 cucumber = 397 tests, 0 FAIL. `check` VERT. Zéro régression, zéro secret, zéro bump BOM.** | **✅ TERMINE**
| **076** | **2026-08-19** | **governance+cleanup+publish** | **6 éléments actionnables (ordre 6,1,2,3,5,4) : (1) Cadrage US CDX-RC-04 pont RAG `SessionMemoryContract` dans BACKLOG (gouvernance pure, table `codex_learner_profiles`, domaine `codex.profile` 12e, gate MEMPHIS runtime-contracts:0.0.1). (2) koog cleanup — `koog-agents:1.0.0` supprimée (zéro usage src). (3) BOM bump 0.0.21→0.0.22. (4) Session 031 🟡 ✅ CADUQUE. (5) CDX-15 EPIC fantôme ✅ CADUQUE. (6) Publication codex-plugin 0.0.4 Central (bundle `8556eea3`) + BOM 0.0.23 Central (bundle `8dc8e513`, constraint codex 0.0.4). 211 tests 0 FAIL, `check` VERT, zéro régression, zéro secret.** | **✅ TERMINE**
| **077** | **2026-08-19** | **governance** | **Cadrage 5 EPICs P1 « Enrichissement Croisé du Corpus » (gouvernance pure, zéro code, pattern S-057/S-063) — doc `CADRAGE_EPIC_P1_CORPUS_ENRICHMENT.adoc` créé (12 constats, 13 décisions) : CDX-3 `deriveOntology` (domaine `codex.ontology` 12e), CDX-4 `enrichJsonLdd` (domaine `codex.enrichment` 13e), CDX-5 `exportKnowledgeBase` routing (domaine `codex.licence` 14e), CDX-6 OSS/CSS routing (DSL `licenceRouting`), CDX-7 tests (FontStyleDetector/TypographicAnalyzer/SemanticChunker/E2E PDF canonique). Bump BOM 0.0.22→0.0.23. Sync AGENTS.adoc racine ligne 480 (CDX-15 ✅ CADUQUE) + entrée Brooklyn alignée S-073→S-076. PLAN_GLOBAL.adoc 5 EPICs P1 ✅ Cadrés S-077. BACKLOG.adoc 5 EPICs P1 ajoutés. 211 tests 0 FAIL, zéro code prod modifié, zéro secret, zéro bump BOM.** | **✅ TERMINE**
| **078** | **2026-08-19** | **dev** | **CDX-7-1 tests FontStyleDetector (32 unit characterization exhaustive) + TypographicAnalyzer extrait de ExtractBookStructureTask (objet pur codex.tasks.TypographicAnalyzer + HeaderThresholds data class publique, délégation -11 lignes) + TypographicAnalyzerTest (12 unit thresholds dynamiques/fallbacks/ordre/plafond). 255 tests 0 FAIL (baseline 211 → +44). Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR.** | **✅ TERMINE**
| **079** | **2026-08-19** | **dev** | **CDX-3-1 domaine `codex.ontology` (12e) + `OntologyMapping` (4 data classes immutables) + `OntologyDeriver` object pur (169 lignes, parsing SQL DDL par regex). 3 requêtes archétypales : bounded contexts (BFS connectivité FK), aggregates (CASCADE edges par racine, self-ref exclue), value objects (tables sans FK sortantes). LDD title nomme le bounded context. 13 unit tests (OntologyDeriverTest). 268 tests 0 FAIL (baseline 255 → +13). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR.** | **✅ TERMINE**
| **080** | **2026-08-19** | **dev** | **CDX-4-1 domaine `codex.enrichment` (13e) + `JsonLddEnricher` object pur (90 lignes) + `GraphifyResolver` `fun interface` port + `GraphifyFileResolver` adapter (70 lignes, lit `graph.json` via kotlinx.serialization, zéro dépendance compile graphify). `EnrichedLddNode` data class composition de `LddNode` (ldd + ragChunks + graphifyNodes + semanticDensity + entities). Jointure par dernier segment `sectionPath` (case-insensitive). Densité = joined/total. Entités regex capitalized words (peuple `DocumentChunk.entities` placeholder). Dégradé silencieux (fichier manquant/JSON invalide → empty). 284 tests 0 FAIL (baseline 268 → +16 : 10 `JsonLddEnricherTest` + 6 `GraphifyFileResolverTest`). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR.** | **✅ TERMINE**
| **081** | **2026-08-19** | **dev** | **CDX-5-1 domaine `codex.licence` (14e) + `PdfLicenseDetector` object pur (scanne contenu PDF : 5 patterns OSS `apache license 2.0`/`apache-2.0`/`creative commons`/`cc-by`/`cc0` + 3 patterns CSS `©`/`copyright`/`all rights reserved`, precedence OSS > CSS > UNKNOWN, case-insensitive, `detect(text)` pure + `detect(pdfFile)` via `Loader.loadPDF` + `PDFTextStripper`, dégradé silencieux UNKNOWN sur erreur, additif à `LicenseZoneDetector` path-based) + `LicenseRouter` object pur `route(zone, baseDir): File` (OSS→`OSS/`, CSS/UNKNOWN→`office/` closed by default, ne crée pas de dirs). 16 unit tests (12 `PdfLicenseDetectorTest` + 4 `LicenseRouterTest`). 300 tests 0 FAIL (baseline 284 → +16). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR.** | **✅ TERMINE**
| **082** | **2026-08-19** | **dev** | **CDX-5-2 `DeployKnowledgeBaseRoutedTask` (nouveau, 69 lignes) étend `ExportKnowledgeBaseTask` : détecte licence PDF via `PdfLicenseDetector` → route via `LicenseRouter` → rebind `outputDir` → délègue à `super.export()` (multi-format inchangé). 3 props Gradle : `pdfFile` (`@InputFile`), `baseDir` (`@OutputDirectory`), `fallbackZone` (`@Input @Optional`). `@DisableCachingByDefault` (validatePlugins). `ExportKnowledgeBaseTask.export()` rendu `open` (1 mot-clé, backward compat). Wiring `CodexPlugin` : task `deployKnowledgeBaseRouted` groupe `deploy`, `fallbackZone.convention(zone)`. 6 tests (5 functional `DeployKnowledgeBaseRoutedTaskTest` : Apache→OSS, Copyright→office, Unknown+OSS fallback, Unknown+CSS fallback, CC-BY→OSS + 1 wiring `CodexPluginTest`). 306 tests 0 FAIL (baseline 300 → +6). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR.** | **✅ TERMINE**
| **083** | **2026-08-19** | **dev** | **CDX-5-3 Cucumber BDD `codex_export_routed.feature` 3 scénarios `@licence @routing` + runner dédié `CodexLicenceCucumberRunner` (pattern S-082, 1re application codex) + `LicenceRoutingSteps` (6 step defs PDFBox + ProjectBuilder) + tâche Gradle `cucumberTestLicence` dédiée (excludeEngines junit-jupiter, filter runnerClass, override `cucumber.features` via systemProperty). Catalogue +1 version `junit-platform = "1.14.3"` + +1 library `junit-platform-suite`. Build cucumber-junit-platform-engine `testRuntimeOnly` → `testImplementation`. 333 tests 0 FAIL (baseline 306 → +27 : 6 cucumber dédiés × 2 découvertes). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Pure BDD — zéro production modifié. Baby-step BDD strict RED → GREEN → REFACTOR. EPIC CDX-5 TERMINÉ 4/4 US. Domaine `codex.licence` (14e) clos.** | **✅ TERMINE**
| **084** | **2026-08-20** | **dev** | **CDX-3-2 `DeriveOntologyTask` (68 lignes, thin wrapper Gradle) — lit `sqlFile` (SQL LMD de `collectBookSql`) + `jsonFile` (LDD nodes de `transformToJsonLdd`), appelle `OntologyDeriver.derive(lddNodes, sqlScript)`, écrit `OntologyMapping` JSON via kotlinx.serialization. `@DisableCachingByDefault`, 3 properties (`@InputFile` × 2 + `@OutputFile`). Sérialisation rétroactive : `OntologyMapping` + 3 sous-types (`BoundedContext`, `Aggregate`, `ValueObject`) annotés `@Serializable` (modification additive, backward compat total). Wiring `CodexPlugin` : tâche `deriveOntology` groupe `transform`, `outputFile` default `build/codex/ontology-mapping.json`. Compteur KDoc 11 → 12 tâches. 4 functional tests (`DeriveOntologyTaskTest`) — ProjectBuilder + SQL DDL synthétique + assertions JSON. 337 tests 0 FAIL (baseline 333 → +4). `check` VERT, Kover VERT, validatePlugins VERT. Zéro bump BOM, zéro secret, zéro régression, backward compat total. Baby-step TDD strict RED → GREEN → REFACTOR. EPIC CDX-3 progression 2/3 US (reste CDX-3-3 Cucumber).** | **✅ TERMINE**
|===

'''
== Roadmap — Etat Actuel

|===
| EPIC | Description | Statut | Priorite
| CDX-0 | Bootstrap gouvernance agent codex | ✅ TERMINE | P0
| CDX-1a | Task extractBookStructure — PDFBox → AsciiDoc | ✅ TERMINE | P0
| CDX-1b | Task asciiDocToJsonLdd — AST AsciidoctorJ → JSON LDD | ✅ TERMINE | P0
| CDX-1c | Task importBookSql — JSON LDD → DDL + INSERT PostgreSQL | ✅ TERMINE | P0
| CDX-2 | Detection typographique → hierarchie multi-niveaux + blocs de code | ✅ TERMINE | P0
| CDX-3 | Task convertToMarkdown avec blocs de code | ✅ TERMINE | P0
| CDX-4 | Task chunkDocument — strategie semantique | ✅ TERMINE | P0
| CDX-5 | Task exportKnowledgeBase — JSON-L / Markdown / AsciiDoc | ✅ TERMINE | P1
| CDX-6 | Integration arborescence OSS/CSS + routing licence | ✅ TERMINE | P1
| CDX-7 | Tests unitaires + integration (62/62 PASS, 9 classes de test) | ✅ TERMINE | P1
| CDX-8 | Task codexIngest — ONNX → pgvector R2DBC (schema codex_documents/codex_chunks, vector(384)) | ✅ TERMINE | P2
| CDX-9 | Task codexRetrieve — cosine similarity dans pgvector → corpus requetable | ✅ TERMINE | P2
| CDX-CI | GitHub Actions CI/CD — test.yml JDK 24 | ✅ TERMINE | P2
| CDX-10 | Task extractEpubStructure — EPUB via Tika → AsciiDoc | ✅ TERMINE | P3
| CDX-11 | Integration runner-gradle N3 : codexRetrieve → assembleCompositeContext | ✅ TERMINE | P2
...
| CDX-13 | Exposition N2↔N2 : collectOcr produit pages individuelles pour document-gradle DOC-11 bookPipeline | ✅ TERMINÉ (session 075 — outputDir + NNN-<pageId>.adoc, contrat PageOrder.fromFileName aligné) | P1
| CDX-14 | Couverture tests (A JUnit5 + B Cucumber + C Kover metrics) | ✅ TERMINE (A+B, C analyse Kover: ProjectBuilder non instrumentable) | P0
| EPIC K | Renommage taxonomie + Metadata.kt + documentation | ✅ TERMINE | P0
| EPIC PUB | Publication Maven Central codex-plugin (0.0.1 S-029, 0.0.2 S-070, 0.0.3 S-074, 0.0.4 S-076) | ✅ TERMINE (S-076) | P0
| EPIC OCR-2 | Tesseract fallback — OcrEngine + TesseractOcrEngine (boundary codex↔codebase) | ✅ TERMINE (session 067) | P1
| EPIC OCR-3 | LLM OCR Pipeline — LlmOcrEngine (Ollama vision) + OcrPipeline (fallback) + collectOcr task (DOC-11 alignement) | ✅ TERMINE (session 072) | P1
| EPIC CDX-CR3 | Revue CR-GLOBALE-003 : SQL injection + hardcode localhost + chunk_index dupliqués | ✅ TERMÉ (session 073) | P0
| US-CDX-13-1 | Persistance OcrResult par page (cache disque hash image) — Loi Économie d'Encre | ✅ TERMINE (session 073) | P1
| US-CDX-13-2 | Tests intégration réelle Ollama (assumeTrue + OllamaProbe) | ✅ TERMINE (session 073) | P2
| EPIC README-I18N | README multilingues (10 langues × consumer + plugin) | ✅ TERMINE (session 068) | P2
| EPIC CDX-RC-04 | Pont RAG SessionMemoryContract (cross-borough MEMPHIS N0 → codex N2) — cadrage S-076 | 🟡 Cadré (gated par MEMPHIS runtime-contracts:0.0.1) | P1
| EPIC CDX-15 | TDD rétroactif (EPIC fantôme mentionné AGENTS racine) | ✅ CADUQUE (S-076 — jamais cadré) | P3
| EPIC CDX-3 (P1) | `deriveOntology` — SQL LMD → cartographie ontologique (domaine `codex.ontology` 12e) — cadrage S-077 | 🟡 En cours (CDX-3-0 ✅, CDX-3-1 ✅ TERMINÉ S-079, CDX-3-2 ✅ TERMINÉ S-084, CDX-3-3 🟡 À FAIRE Cucumber) | P1
| EPIC CDX-4 (P1) | `enrichJsonLdd` — Croisement JSON LDD + RAG pgvector + Graphify (domaine `codex.enrichment` 13e) — cadrage S-077 | 🟡 En cours (CDX-4-0 ✅, CDX-4-1 ✅ TERMINÉ S-080, CDX-4-2/3/4 🟡 À FAIRE) | P1
| EPIC CDX-5 (P1) | `exportKnowledgeBase` routing licence (domaine `codex.licence` 14e) — cadrage S-077 | ✅ TERMINÉ (CDX-5-0 ✅ S-077, CDX-5-1 ✅ S-081, CDX-5-2 ✅ S-082, CDX-5-3 ✅ S-083) | P1
| EPIC CDX-6 (P1) | Intégration OSS/CSS + routing licence (DSL `licenceRouting`) — cadrage S-077 | 🟡 Cadré (CDX-6-0 ✅, CDX-6-1/2/3 🟡 À FAIRE) | P1
| EPIC CDX-7 (P1) | Tests unitaires + intégration (FontStyleDetector/TypographicAnalyzer/SemanticChunker/E2E PDF) — cadrage S-077 | 🟡 En cours (CDX-7-0 ✅, CDX-7-1 ✅ TERMINÉ S-078, CDX-7-2/3/4 🟡 À FAIRE) | P1
|===

'''
== Fichiers Associes

|===
| Fichier | Role | Type
| `AGENT.adoc` | Directives completes | EAGER
| `PROMPT_REPRISE.adoc` | Mission session en cours | EAGER
| `.agents/SESSIONS_HISTORY.adoc` | Historique des sessions | EAGER
| `.agents/TEST_COVERAGE_ANALYSIS.adoc` | Analyse couverture tests | LAZY
| `.agents/archives/` | Archives taches | LAZY
| `.agents/sessions/` | Archives par session | LAZY
|===
